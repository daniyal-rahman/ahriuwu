# ahriuwu WM — update since briefing (2026-07-13)

Continues `WORLD_MODEL_BRIEFING.md`. Data/facts only.

## Checkpoints compared
- **135**: `/mnt/storage/data/ahriuwu/checkpoints/dynamics_v7_accel_resume/dynamics_latest.pt` — global_step 11017, use_actions=True, has action keys.
- **179**: `/mnt/storage/data/ahriuwu/checkpoints/dynamics_v7_yt578_hudfix/dynamics_latest.pt` — global_step 6526, use_actions=False, 0 action keys.

## Step 0 — rollout sweep
`rollout_check.py`, match NA1_5549995114, ctx 6, horizon 16, seed 0 (same window both models). 135 fed real recorded actions; 179 run `--no-actions`. Latent-space rollout PSNR (dB):

| run | h1 | h2 | h4 | h8 | h16 | mean |
|---|---|---|---|---|---|---|
| 135 act, num_steps 1 | 21.2 | 17.5 | 17.1 | 15.3 | 12.4 | 14.8 |
| 135 act, num_steps 4 | 19.5 | 14.8 | 12.9 | 10.8 | 10.7 | 11.6 |
| 135 act, num_steps 16 | 18.3 | 13.9 | 12.2 | 10.4 | 10.4 | 11.3 |
| 179 noact, num_steps 1 | 17.5 | 14.5 | 12.0 | 9.1 | 6.9 | 9.3 |
| 179 noact, num_steps 4 | 17.0 | 13.9 | 11.6 | 8.6 | 6.3 | 8.6 |
| 179 noact, num_steps 16 | 15.0 | 11.9 | 9.4 | 6.9 | 5.3 | 7.2 |

Control "135 `--no-actions`": failed to load — `ARCH MISMATCH`, unexpected keys `no_action_embed, action_embed.movement.weight, action_embed.movement.bias, action_embed.Q.weight, action_embed.W.weight` (checkpoint has action params; model built use_actions=False).

Pixel-space decode, num_steps 1, same window (dB, frame 1 → frame 16):
- tokenizer ceiling (decode of true latents): 28.3 → 28.5 (both)
- persistence (hold last real frame): 24.2 → 21.5 (both)
- 135 dream: 26.6 → 22.5
- 179 dream: 26.3 → 17.0

## Teacher-forced denoising τ-sweep (`eval_denoising_psnr`)
τ = 0.1 / 0.3 / 0.5 / 0.7 / 0.9 (dB):
- 135 (log gs10400): 11.9 / 14.5 / 16.8 / 19.8 / 25.3   (gs8800: τ0.9 = 24.9)
- 179 (gs6526): 9.4 / 10.6 / 12.0 / 14.1 / 18.4

## Sampler / eval config facts
- `rollout_check`: `model.rollout(num_steps=N, k_max=N)` → step size d=1; `tau_ctx=0.1`.
- 179 sbatch has no `--num-eval-videos` → default 0 → eval batch drawn from **training** data. No held-out latents exist (holdout dirs `/scratch/ahriuwu/action_labeled*holdout*` are PNG only, 0 `.pt`).

## Loss-code facts (`scripts/train_dynamics.py`)
- `is_yt = all(not str(v).startswith("NA1_") for v in batch["video_id"])` — evaluated per batch.
- `rms_dict = {"x_pred": RunningRMS(), "pixel": RunningRMS()}` — two separate normalizers.
- Routing (`_forward_standard`): YT batch AND tokenizer loaded → `pixel_hud_masked_loss`, rms_key "pixel". Otherwise → `x_prediction_loss` (latent MSE × ramp), rms_key "x_pred".
- `pixel_hud_masked_loss`: picks k = min(pixel_frames, T) = min(4,128) = 4 **random** frames; decodes z_pred (gradient-checkpointed) and z_0 (no_grad) through the frozen v7 decoder; MSE over `hud_mask==1` pixels only; × `ramp_weight(τ)`; gradient flows through the frozen decoder into z_pred only. Latent target z_0 never directly constrained on YT batches.
- Tokenizer/decoder loaded only when `--pixel-hud-loss` set (else `ts.tokenizer=None` → latent loss on all batches).
- 578 games = 453 YT + 125 replay.
- Edge: batch straddling YT+replay videos → `all(...)`=False → whole batch (incl. YT frames) uses latent loss.

## Step 1′ (job 181) — pixel-HUD loss removed, everything else held
- sbatch `slurm/slurm_dyn_s1prime.sbatch` = copy of `slurm_dyn_yt578.sbatch` with `--pixel-hud-loss`, `--pixel-loss-frames`, `--tokenizer-ckpt`, `--hud-mask`, `--wandb` lines removed; `--eval-interval` 200→50.
- Checkpoint dir `dyn179_s1prime_latentonly`, seeded with a copy of 179's `dynamics_latest.pt`.
- Config: `--packed` (actions off), 578 games, stride 64, medium, alternating 128/256, batch 2/1, grad-accum 8/8/16, lr 3e-4, WSD warmup 3000. 179 untouched.
- Resumed at epoch 3, global_step 6526.
- Training loss (latent, all batches): 0.0203 (batch 0) → 0.0074 (batch 150).
- **Eval at gs6550 (24 optimizer steps after resume):** τ-sweep 13.8 / 15.9 / 17.5 / 20.0 / **24.6**.
- (179 baseline at gs6526 same eval: 9.4 / 10.6 / 12.0 / 14.1 / 18.4.)
- Job still running. Log at `/mnt/storage/data/ahriuwu/logs/slurm_dyn_yt578_181.out` (sbatch `--output` template unchanged; filename says yt578 not s1prime).
- Not yet run: rollout eval on the Step 1′ checkpoint.

## Concurrent / other
- 179: paused (scancel) before Step 0; still paused.
- Phase-2 BC (1060, watchdog job): running. Last heartbeat Epoch 0 [~12450/70377], loss≈2.13, bc≈2.34 (abil≈0.046, move≈2.29), rew≈0.05; checkpoints every 20 min at `data/phase2_bc_garen/`. Backbone = 179 snapshot gs6120. ability-pos-weight 5.0.
- LR anneal probe (job 180, lr 3e-5 resume of 179): submitted earlier; no separate result recorded; log under `slurm_dyn_yt578_180.out`.
- Files this session: `dream_psnr_plateau.png`, `dream_plateau.png` (179 @ gs6526, num_steps 8, horizon 32).
