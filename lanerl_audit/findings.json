[
  {
    "severity": "high",
    "check": "config-silent-fallback",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/cfg_retreat.json:3",
    "what": "LANERL_BOT_CONFIG target does not exist: /srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/hp25.json. LanerlConfig.Load() (LanerlConfig.cs:161) ignores a missing file silently, so this arm runs the DEFAULT bot and the comparison is vacuous.",
    "fix": "create the config, or make LanerlConfig.Load() throw when LANERL_BOT_CONFIG is set but File.Exists() is false",
    "extra": {
      "path": "/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/hp25.json"
    }
  },
  {
    "severity": "high",
    "check": "config-silent-fallback",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/cfg_tune.json:2",
    "what": "LANERL_BOT_CONFIG target does not exist: /srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned.json. LanerlConfig.Load() (LanerlConfig.cs:161) ignores a missing file silently, so this arm runs the DEFAULT bot and the comparison is vacuous.",
    "fix": "create the config, or make LanerlConfig.Load() throw when LANERL_BOT_CONFIG is set but File.Exists() is false",
    "extra": {
      "path": "/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned.json"
    }
  },
  {
    "severity": "high",
    "check": "config-silent-fallback",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/cfg_tune.json:3",
    "what": "LANERL_BOT_CONFIG target does not exist: /srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned_r150.json. LanerlConfig.Load() (LanerlConfig.cs:161) ignores a missing file silently, so this arm runs the DEFAULT bot and the comparison is vacuous.",
    "fix": "create the config, or make LanerlConfig.Load() throw when LANERL_BOT_CONFIG is set but File.Exists() is false",
    "extra": {
      "path": "/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned_r150.json"
    }
  },
  {
    "severity": "high",
    "check": "config-silent-fallback",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/cfg_tune2.json:3",
    "what": "LANERL_BOT_CONFIG target does not exist: /srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned_look600.json. LanerlConfig.Load() (LanerlConfig.cs:161) ignores a missing file silently, so this arm runs the DEFAULT bot and the comparison is vacuous.",
    "fix": "create the config, or make LanerlConfig.Load() throw when LANERL_BOT_CONFIG is set but File.Exists() is false",
    "extra": {
      "path": "/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned_look600.json"
    }
  },
  {
    "severity": "high",
    "check": "config-silent-fallback",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/cfg_tune2.json:4",
    "what": "LANERL_BOT_CONFIG target does not exist: /srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned_look800.json. LanerlConfig.Load() (LanerlConfig.cs:161) ignores a missing file silently, so this arm runs the DEFAULT bot and the comparison is vacuous.",
    "fix": "create the config, or make LanerlConfig.Load() throw when LANERL_BOT_CONFIG is set but File.Exists() is false",
    "extra": {
      "path": "/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned_look800.json"
    }
  },
  {
    "severity": "high",
    "check": "config-silent-fallback",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/cfg_tune2.json:5",
    "what": "LANERL_BOT_CONFIG target does not exist: /srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned_look400_r150.json. LanerlConfig.Load() (LanerlConfig.cs:161) ignores a missing file silently, so this arm runs the DEFAULT bot and the comparison is vacuous.",
    "fix": "create the config, or make LanerlConfig.Load() throw when LANERL_BOT_CONFIG is set but File.Exists() is false",
    "extra": {
      "path": "/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned_look400_r150.json"
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/cfg_confirm.json:3",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"anchor_diamond\":  {\"LANERL_BOT\": \"blue\", \"LANERL_BOT_CONFIG\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/anchor_diamond.json\"},"
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/cfg_confirm.json:4",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"anchor_gold\":     {\"LANERL_BOT\": \"blue\", \"LANERL_BOT_CONFIG\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/anchor_gold.json\"},"
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/cfg_confirm.json:5",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"anchor_bronze\":   {\"LANERL_BOT\": \"blue\", \"LANERL_BOT_CONFIG\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/anchor_bronze.json\"}"
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/cfg_curriculum.json:2",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"anchor_diamond\": {\"LANERL_BOT\": \"blue\", \"LANERL_BOT_CONFIG\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/anchor_diamond.json\"},"
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/cfg_curriculum.json:3",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"anchor_gold\":    {\"LANERL_BOT\": \"blue\", \"LANERL_BOT_CONFIG\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/anchor_gold.json\"},"
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/cfg_curriculum.json:4",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"anchor_bronze\":  {\"LANERL_BOT\": \"blue\", \"LANERL_BOT_CONFIG\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/anchor_bronze.json\"}"
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/cfg_final.json:4",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"noq\":        {\"LANERL_BOT\": \"blue\", \"LANERL_BOT_CONFIG\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/noq.json\"}"
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/cfg_retreat.json:3",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"retreat_hp25\":        {\"LANERL_BOT\": \"blue\", \"LANERL_BOT_CONFIG\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/hp25.json\"},"
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/cfg_tune.json:2",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"tuned_r80\":   {\"LANERL_BOT\": \"blue\", \"LANERL_BOT_CONFIG\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned.json\"},"
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/cfg_tune.json:3",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"tuned_r150\":  {\"LANERL_BOT\": \"blue\", \"LANERL_BOT_CONFIG\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned_r150.json\"},"
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/cfg_tune.json:4",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"tuned_look400\": {\"LANERL_BOT\": \"blue\", \"LANERL_BOT_CONFIG\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned_look400.json\"}"
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/cfg_tune2.json:2",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"look400_r80\":  {\"LANERL_BOT\": \"blue\", \"LANERL_BOT_CONFIG\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned_look400.json\"},"
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/cfg_tune2.json:3",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"look600_r80\":  {\"LANERL_BOT\": \"blue\", \"LANERL_BOT_CONFIG\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned_look600.json\"},"
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/cfg_tune2.json:4",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"look800_r80\":  {\"LANERL_BOT\": \"blue\", \"LANERL_BOT_CONFIG\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned_look800.json\"},"
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/cfg_tune2.json:5",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"look400_r150\": {\"LANERL_BOT\": \"blue\", \"LANERL_BOT_CONFIG\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned_look400_r150.json\"}"
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/instr.json:6",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"LANERL_BOT_STATS\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/bench/out/instr.jsonl\""
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/qab4.json:4",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"LANERL_BOT_CONFIG\": \"/mnt/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/noq.json\""
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/qab4.json:60",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"LANERL_BOT_CONFIG\": \"/mnt/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/qon_fixed.json\""
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/qab5.json:4",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"LANERL_BOT_CONFIG\": \"/mnt/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/noq.json\""
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/qab5.json:74",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"LANERL_BOT_CONFIG\": \"/mnt/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/qon_fixed.json\""
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/rep_confirm.json:118",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"LANERL_BOT_CONFIG\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/anchor_gold.json\""
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/rep_confirm.json:175",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"LANERL_BOT_CONFIG\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/anchor_bronze.json\""
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/rep_confirm.json:61",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"LANERL_BOT_CONFIG\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/anchor_diamond.json\""
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/rep_final.json:145",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"LANERL_BOT_CONFIG\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/noq.json\""
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/rep_retreat.json:40",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"LANERL_BOT_CONFIG\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/hp25.json\""
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/rep_tune.json:41",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"LANERL_BOT_CONFIG\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned_r150.json\""
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/rep_tune.json:5",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"LANERL_BOT_CONFIG\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned.json\""
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/rep_tune.json:77",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"LANERL_BOT_CONFIG\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned_look400.json\""
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/rep_tune2.json:134",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"LANERL_BOT_CONFIG\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned_look400_r150.json\""
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/rep_tune2.json:48",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"LANERL_BOT_CONFIG\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned_look600.json\""
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/rep_tune2.json:5",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"LANERL_BOT_CONFIG\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned_look400.json\""
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/rep_tune2.json:91",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"LANERL_BOT_CONFIG\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned_look800.json\""
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/reset_smoke.json:7",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"LANERL_RECORD\": \"/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/bench/out/reset_smoke.jsonl\""
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/qab_configs.json:2",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"q_OFF_baseline\": {\"LANERL_BOT_CONFIG\": \"/mnt/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/noq.json\"},"
    }
  },
  {
    "severity": "high",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/qab_configs.json:3",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"q_ON_after_fix\": {\"LANERL_BOT_CONFIG\": \"/mnt/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/qon_fixed.json\"}"
    }
  },
  {
    "severity": "high",
    "check": "tests-not-collected",
    "where": "ahriuwu-lanerl/pyproject.toml",
    "what": "testpaths=['tests'] does not include ahriuwu-lanerl/lanerl_rl/tests: a bare `pytest` at the repo root never collects these tests, so their failures are invisible",
    "fix": "add \"lanerl_rl/tests\" to testpaths",
    "extra": {}
  },
  {
    "severity": "high",
    "check": "tests-not-collected",
    "where": "ahriuwu-lanerl/pyproject.toml",
    "what": "testpaths=['tests'] does not include ahriuwu-lanerl/lanerl_bot/tests: a bare `pytest` at the repo root never collects these tests, so their failures are invisible",
    "fix": "add \"lanerl_bot/tests\" to testpaths",
    "extra": {}
  },
  {
    "severity": "high",
    "check": "unchecked-returncode",
    "where": "ahriuwu-lanerl/lanerl_rl/control_smoke.py:47",
    "what": "subprocess.Popen(...) and nothing in this file ever reads returncode/poll(): a server that dies in 1s is indistinguishable from a clean 10-minute run",
    "fix": "capture rc = proc.wait(); treat rc not in (0, -9, -15) OR an implausibly short wall time as a hard error, not an empty result",
    "extra": {}
  },
  {
    "severity": "high",
    "check": "unconditional-done",
    "where": "ahriuwu-lanerl/lanerl/run_final.sh:61",
    "what": "prints a success banner unconditionally in a script without `set -e` -- this is the build-check bug verbatim",
    "fix": "gate the banner on the real exit status, or add `set -e`",
    "extra": {
      "line": "echo \"=== done: $(ls -lh \"$OUT/gameplay_final.mp4\" 2>/dev/null | awk '{print $5}') ===\""
    }
  },
  {
    "severity": "high",
    "check": "unconditional-done",
    "where": "ahriuwu-lanerl/lanerl/scaling_test.sh:22",
    "what": "prints a success banner unconditionally in a script without `set -e` -- this is the build-check bug verbatim",
    "fix": "gate the banner on the real exit status, or add `set -e`",
    "extra": {
      "line": "echo \"N=$N done ($(grep -lc LANERL_TPS \"$OUT\"/n${N}_i*.log 2>/dev/null | wc -l) instances reported)\""
    }
  },
  {
    "severity": "high",
    "check": "unconditional-done",
    "where": "ahriuwu-lanerl/lanerl/stage_display.sh:50",
    "what": "prints a success banner unconditionally in a script without `set -e` -- this is the build-check bug verbatim",
    "fix": "gate the banner on the real exit status, or add `set -e`",
    "extra": {
      "line": "echo \"CAPTURE OK: $(ls -lh \"$OUT/display_test.png\" | awk '{print $5}')\""
    }
  },
  {
    "severity": "high",
    "check": "unconditional-done",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/run_all.sh:9",
    "what": "prints a success banner unconditionally in a script without `set -e` -- this is the build-check bug verbatim",
    "fix": "gate the banner on the real exit status, or add `set -e`",
    "extra": {
      "line": "echo \"=== ALL DONE ===\""
    }
  },
  {
    "severity": "high",
    "check": "unconditional-done",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/run_confirm.sh:4",
    "what": "prints a success banner unconditionally in a script without `set -e` -- this is the build-check bug verbatim",
    "fix": "gate the banner on the real exit status, or add `set -e`",
    "extra": {
      "line": "echo \"=== CONFIRM DONE ===\""
    }
  },
  {
    "severity": "high",
    "check": "unconditional-done",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/run_curriculum.sh:4",
    "what": "prints a success banner unconditionally in a script without `set -e` -- this is the build-check bug verbatim",
    "fix": "gate the banner on the real exit status, or add `set -e`",
    "extra": {
      "line": "echo \"=== CURRICULUM DONE ===\""
    }
  },
  {
    "severity": "high",
    "check": "undefined-name",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/bench_process_restart.py:27",
    "what": "undefined name `_VENDOR`: raises NameError on import -- this module has never run",
    "fix": "define it (the sibling files derive it from Path(__file__).resolve().parents[N])",
    "extra": {}
  },
  {
    "severity": "high",
    "check": "undefined-name",
    "where": "ahriuwu-lanerl/lanerl_bot/tests/test_last_hit.py:201",
    "what": "undefined name `_VENDOR`: raises NameError the moment that function is called",
    "fix": "define it (the sibling files derive it from Path(__file__).resolve().parents[N])",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "config-silent-fallback",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/rep_retreat.json:40",
    "what": "LANERL_BOT_CONFIG target does not exist: /srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/hp25.json. LanerlConfig.Load() (LanerlConfig.cs:161) ignores a missing file silently, so this arm runs the DEFAULT bot and the comparison is vacuous. This is a recorded result: it is not reproducible.",
    "fix": "create the config, or make LanerlConfig.Load() throw when LANERL_BOT_CONFIG is set but File.Exists() is false",
    "extra": {
      "path": "/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/hp25.json"
    }
  },
  {
    "severity": "medium",
    "check": "config-silent-fallback",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/rep_tune.json:41",
    "what": "LANERL_BOT_CONFIG target does not exist: /srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned_r150.json. LanerlConfig.Load() (LanerlConfig.cs:161) ignores a missing file silently, so this arm runs the DEFAULT bot and the comparison is vacuous. This is a recorded result: it is not reproducible.",
    "fix": "create the config, or make LanerlConfig.Load() throw when LANERL_BOT_CONFIG is set but File.Exists() is false",
    "extra": {
      "path": "/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned_r150.json"
    }
  },
  {
    "severity": "medium",
    "check": "config-silent-fallback",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/rep_tune.json:5",
    "what": "LANERL_BOT_CONFIG target does not exist: /srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned.json. LanerlConfig.Load() (LanerlConfig.cs:161) ignores a missing file silently, so this arm runs the DEFAULT bot and the comparison is vacuous. This is a recorded result: it is not reproducible.",
    "fix": "create the config, or make LanerlConfig.Load() throw when LANERL_BOT_CONFIG is set but File.Exists() is false",
    "extra": {
      "path": "/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned.json"
    }
  },
  {
    "severity": "medium",
    "check": "config-silent-fallback",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/rep_tune2.json:134",
    "what": "LANERL_BOT_CONFIG target does not exist: /srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned_look400_r150.json. LanerlConfig.Load() (LanerlConfig.cs:161) ignores a missing file silently, so this arm runs the DEFAULT bot and the comparison is vacuous. This is a recorded result: it is not reproducible.",
    "fix": "create the config, or make LanerlConfig.Load() throw when LANERL_BOT_CONFIG is set but File.Exists() is false",
    "extra": {
      "path": "/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned_look400_r150.json"
    }
  },
  {
    "severity": "medium",
    "check": "config-silent-fallback",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/rep_tune2.json:48",
    "what": "LANERL_BOT_CONFIG target does not exist: /srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned_look600.json. LanerlConfig.Load() (LanerlConfig.cs:161) ignores a missing file silently, so this arm runs the DEFAULT bot and the comparison is vacuous. This is a recorded result: it is not reproducible.",
    "fix": "create the config, or make LanerlConfig.Load() throw when LANERL_BOT_CONFIG is set but File.Exists() is false",
    "extra": {
      "path": "/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned_look600.json"
    }
  },
  {
    "severity": "medium",
    "check": "config-silent-fallback",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/rep_tune2.json:91",
    "what": "LANERL_BOT_CONFIG target does not exist: /srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned_look800.json. LanerlConfig.Load() (LanerlConfig.cs:161) ignores a missing file silently, so this arm runs the DEFAULT bot and the comparison is vacuous. This is a recorded result: it is not reproducible.",
    "fix": "create the config, or make LanerlConfig.Load() throw when LANERL_BOT_CONFIG is set but File.Exists() is false",
    "extra": {
      "path": "/srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/configs/tuned_look800.json"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/diag.sh:7",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "ROOT=/mnt/storage/lanerl; NFS=/mnt/nfs/projects"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/fix_case.sh:7",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "ROOT=/mnt/storage/lanerl; NFS=/mnt/nfs/projects"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/fix_ime.sh:7",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "ROOT=/mnt/storage/lanerl; NFS=/mnt/nfs/projects; OUT=$NFS/ahriuwu-lanerl/lanerl/logs"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/fix_rads.sh:10",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "NFS=/mnt/nfs/projects"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/fix_rads2.sh:14",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "NFS=/mnt/nfs/projects"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/fix_rads3.sh:10",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "NFS=/mnt/nfs/projects"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/fix_rads4.sh:11",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "NFS=/mnt/nfs/projects"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/fix_rads5.sh:11",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "NFS=/mnt/nfs/projects"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/fix_rads6.sh:11",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "NFS=/mnt/nfs/projects"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/patch_freerun.py:11",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "game = pathlib.Path(\"/srv/nfs/projects/lanerl-vendor/LoLServer/GameServerLib/Game.cs\")"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/patch_server.py:13",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "ROOT = pathlib.Path(\"/srv/nfs/projects/lanerl-vendor/LoLServer\")"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/patch_toponly.py:9",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "p = pathlib.Path(\"/srv/nfs/projects/lanerl-vendor/LoLServer/Content/\""
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/record_headless.sh:7",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "NFS=/srv/nfs/projects"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/render_ab.sh:10",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "ROOT=/mnt/storage/lanerl; NFS=/mnt/nfs/projects"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/render_cd.sh:8",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "ROOT=/mnt/storage/lanerl; NFS=/mnt/nfs/projects"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/run_WORKING.sh:13",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "ROOT=/mnt/storage/lanerl; NFS=/mnt/nfs/projects"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/run_direct.sh:9",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "ROOT=/mnt/storage/lanerl; NFS=/mnt/nfs/projects"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/run_final.sh:10",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "ROOT=/mnt/storage/lanerl; NFS=/mnt/nfs/projects"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/run_game.sh:11",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "NFS=/mnt/nfs/projects"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/run_gpu.sh:8",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "ROOT=/mnt/storage/lanerl; NFS=/mnt/nfs/projects"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/run_real_x.sh:8",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "ROOT=/mnt/storage/lanerl; NFS=/mnt/nfs/projects"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/run_video.sh:10",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "ROOT=/mnt/storage/lanerl; NFS=/mnt/nfs/projects"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/scaling_test.sh:6",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "NFS=/mnt/nfs/projects"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/serve_for_windows.sh:5",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "NFS=/srv/nfs/projects; OUT=$NFS/ahriuwu-lanerl/lanerl/logs"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/setup_server.sh:8",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "VENDOR=/srv/nfs/projects/lanerl-vendor"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/stage_display.sh:19",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "OUT=/mnt/nfs/projects/ahriuwu-lanerl/lanerl/logs"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/test_wined3d.sh:5",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "ROOT=/mnt/storage/lanerl; NFS=/mnt/nfs/projects"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/try_d3dx9.sh:8",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "ROOT=/mnt/storage/lanerl; NFS=/mnt/nfs/projects; OUT=$NFS/ahriuwu-lanerl/lanerl/logs"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl/try_wined3d_dx9.sh:6",
    "what": "literal /mnt/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "ROOT=/mnt/storage/lanerl; NFS=/mnt/nfs/projects; OUT=$NFS/ahriuwu-lanerl/lanerl/logs"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/run_all.sh:4",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "cd /srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/bench"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/run_confirm.sh:2",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "cd /srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/bench"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/run_curriculum.sh:2",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "cd /srv/nfs/projects/ahriuwu-lanerl/lanerl_bot/bench"
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_rl/env.py:273",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "\"/srv/nfs/projects/lanerl-vendor/LoLServer/GameServerConsole/bin/Release/net6.0\""
    }
  },
  {
    "severity": "medium",
    "check": "hardcoded-mount",
    "where": "ahriuwu-lanerl/lanerl_rl/env.py:275",
    "what": "literal /srv/nfs -- resolves on one Slurm node only",
    "fix": "derive from Path(__file__).resolve().parents[N] (py) or ${BASH_SOURCE[0]} (sh); never write the mount point",
    "extra": {
      "line": "DEFAULT_DOTNET_ROOT = Path(\"/srv/nfs/projects/lanerl-vendor/dotnet\")"
    }
  },
  {
    "severity": "medium",
    "check": "pipeline-masks-failure",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/run_all.sh:6",
    "what": "pipes into tail/head/grep/wc without pipefail: the producer's exit status is discarded",
    "fix": "set -o pipefail, or capture ${PIPESTATUS[0]}",
    "extra": {
      "line": "$PY measure_all.py --only reset 2>&1 | tail -20"
    }
  },
  {
    "severity": "medium",
    "check": "pipeline-masks-failure",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/run_all.sh:8",
    "what": "pipes into tail/head/grep/wc without pipefail: the producer's exit status is discarded",
    "fix": "set -o pipefail, or capture ${PIPESTATUS[0]}",
    "extra": {
      "line": "$PY replicate.py --configs out/cfg_final.json --seeds 8 --port0 6100 --out rep_final.json 2>&1 | tail -8"
    }
  },
  {
    "severity": "medium",
    "check": "pipeline-masks-failure",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/run_confirm.sh:3",
    "what": "pipes into tail/head/grep/wc without pipefail: the producer's exit status is discarded",
    "fix": "set -o pipefail, or capture ${PIPESTATUS[0]}",
    "extra": {
      "line": "/home/dani/miniconda3/envs/ml/bin/python replicate.py --configs out/cfg_confirm.json --seeds 6 --port0 6400 --out rep_confirm.json 2>&1 | tail -8"
    }
  },
  {
    "severity": "medium",
    "check": "pipeline-masks-failure",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/run_curriculum.sh:3",
    "what": "pipes into tail/head/grep/wc without pipefail: the producer's exit status is discarded",
    "fix": "set -o pipefail, or capture ${PIPESTATUS[0]}",
    "extra": {
      "line": "/home/dani/miniconda3/envs/ml/bin/python replicate.py --configs out/cfg_curriculum.json --seeds 6 --port0 6300 --out rep_curriculum.json 2>&1 | tail -8"
    }
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl/diag.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl/fix_case.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl/fix_ime.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl/fix_rads.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl/fix_rads2.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl/fix_rads3.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl/fix_rads4.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl/fix_rads5.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl/fix_rads6.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl/record_headless.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl/render_ab.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl/render_cd.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl/run_WORKING.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl/run_direct.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl/run_final.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl/run_game.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl/run_gpu.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl/run_real_x.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl/run_video.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl/scaling_test.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl/serve_for_windows.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl/setup_client.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl/setup_client2.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl/stage_display.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl/start_xorg.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl/test_wined3d.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl/try_d3dx9.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl/try_wined3d_dx9.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/run_all.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/run_confirm.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-errexit",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/run_curriculum.sh:1",
    "what": "no `set -e`: a failing command does not stop the script, so later steps run against whatever the failed step left behind",
    "fix": "add `set -euo pipefail` (and an explicit `|| true` where a failure really is acceptable)",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-pipefail",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/run_all.sh:1",
    "what": "no `set -o pipefail`: `cmd | tail` reports tail's status, so a failing cmd is invisible",
    "fix": "add `set -o pipefail`",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-pipefail",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/run_confirm.sh:1",
    "what": "no `set -o pipefail`: `cmd | tail` reports tail's status, so a failing cmd is invisible",
    "fix": "add `set -o pipefail`",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "shell-no-pipefail",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/out/run_curriculum.sh:1",
    "what": "no `set -o pipefail`: `cmd | tail` reports tail's status, so a failing cmd is invisible",
    "fix": "add `set -o pipefail`",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "skip-hides-failure",
    "where": "ahriuwu-lanerl/lanerl_bot/tests/conftest.py:103",
    "what": "pytest.skip() inside a fixture/test body: a crashed or unresponsive server is reported as a SKIP and the suite exits 0",
    "fix": "pytest.fail() when the subject was supposed to be available; reserve skip for a genuinely absent prerequisite",
    "extra": {
      "line": "pytest.skip(\"server unavailable\")"
    }
  },
  {
    "severity": "medium",
    "check": "skip-hides-failure",
    "where": "ahriuwu-lanerl/lanerl_bot/tests/conftest.py:91",
    "what": "pytest.skip() inside a fixture/test body: a crashed or unresponsive server is reported as a SKIP and the suite exits 0",
    "fix": "pytest.fail() when the subject was supposed to be available; reserve skip for a genuinely absent prerequisite",
    "extra": {
      "line": "pytest.skip(\"server unavailable\")"
    }
  },
  {
    "severity": "medium",
    "check": "skip-hides-failure",
    "where": "ahriuwu-lanerl/lanerl_bot/tests/conftest.py:95",
    "what": "pytest.skip() inside a fixture/test body: a crashed or unresponsive server is reported as a SKIP and the suite exits 0",
    "fix": "pytest.fail() when the subject was supposed to be available; reserve skip for a genuinely absent prerequisite",
    "extra": {
      "line": "pytest.skip(\"server did not reach the self-test dump\")"
    }
  },
  {
    "severity": "medium",
    "check": "skip-hides-failure",
    "where": "ahriuwu-lanerl/lanerl_rl/tests/conftest.py:19",
    "what": "pytest.skip() inside a fixture/test body: a crashed or unresponsive server is reported as a SKIP and the suite exits 0",
    "fix": "pytest.fail() when the subject was supposed to be available; reserve skip for a genuinely absent prerequisite",
    "extra": {
      "line": "pytest.skip(f\"recording not found at {RECORDING}\")"
    }
  },
  {
    "severity": "medium",
    "check": "skip-hides-failure",
    "where": "ahriuwu-lanerl/lanerl_rl/tests/test_obs.py:23",
    "what": "pytest.skip() inside a fixture/test body: a crashed or unresponsive server is reported as a SKIP and the suite exits 0",
    "fix": "pytest.fail() when the subject was supposed to be available; reserve skip for a genuinely absent prerequisite",
    "extra": {
      "line": "pytest.skip(f\"recording not found at {RECORDING}\")"
    }
  },
  {
    "severity": "medium",
    "check": "swallowed-exception",
    "where": "ahriuwu-lanerl/lanerl_rl/env.py:421",
    "what": "broad except with a silent pass/continue",
    "fix": "narrow the exception type and log what was swallowed",
    "extra": {}
  },
  {
    "severity": "medium",
    "check": "swallowed-exception",
    "where": "lanerl-vendor/LoLServer/GameServerLib/Lanerl/LanerlBot.cs:288",
    "what": "C# catch {} with an empty, uncommented body -- a throwing engine call degrades to a no-op that the caller records as done",
    "fix": "log the exception, or set a failure flag the caller checks",
    "extra": {
      "line": "try { qSpell.Cast(_champ.Position, qKillable.Position, _champ); } catch { }"
    }
  },
  {
    "severity": "medium",
    "check": "swallowed-exception",
    "where": "lanerl-vendor/LoLServer/GameServerLib/Lanerl/LanerlBot.cs:446",
    "what": "C# catch {} with an empty, uncommented body -- a throwing engine call degrades to a no-op that the caller records as done",
    "fix": "log the exception, or set a failure flag the caller checks",
    "extra": {
      "line": "catch { }"
    }
  },
  {
    "severity": "medium",
    "check": "swallowed-exception",
    "where": "lanerl-vendor/LoLServer/GameServerLib/Lanerl/LanerlEpisode.cs:140",
    "what": "C# catch {} with an empty, uncommented body -- a throwing engine call degrades to a no-op that the caller records as done",
    "fix": "log the exception, or set a failure flag the caller checks",
    "extra": {
      "line": "try { champ.Inventory.RemoveItem(slot, champ); } catch { }"
    }
  },
  {
    "severity": "medium",
    "check": "swallowed-exception",
    "where": "lanerl-vendor/LoLServer/GameServerLib/Lanerl/LanerlEpisode.cs:228",
    "what": "C# catch {} with an empty, uncommented body -- a throwing engine call degrades to a no-op that the caller records as done",
    "fix": "log the exception, or set a failure flag the caller checks",
    "extra": {
      "line": "catch { }"
    }
  },
  {
    "severity": "medium",
    "check": "swallowed-exception",
    "where": "lanerl-vendor/LoLServer/GameServerLib/Lanerl/LanerlEpisode.cs:237",
    "what": "C# catch {} with an empty, uncommented body -- a throwing engine call degrades to a no-op that the caller records as done",
    "fix": "log the exception, or set a failure flag the caller checks",
    "extra": {
      "line": "catch { }"
    }
  },
  {
    "severity": "medium",
    "check": "swallowed-exception",
    "where": "lanerl-vendor/LoLServer/GameServerLib/Lanerl/LanerlEpisode.cs:278",
    "what": "C# catch {} with an empty, uncommented body -- a throwing engine call degrades to a no-op that the caller records as done",
    "fix": "log the exception, or set a failure flag the caller checks",
    "extra": {
      "line": "try { ai.SetTargetUnit(null, true); } catch { }"
    }
  },
  {
    "severity": "medium",
    "check": "swallowed-exception",
    "where": "lanerl-vendor/LoLServer/GameServerLib/Lanerl/LanerlEpisode.cs:375",
    "what": "C# catch {} with an empty, uncommented body -- a throwing engine call degrades to a no-op that the caller records as done",
    "fix": "log the exception, or set a failure flag the caller checks",
    "extra": {
      "line": "try { m.SetTargetUnit(null, true); } catch { }"
    }
  },
  {
    "severity": "medium",
    "check": "swallowed-exception",
    "where": "lanerl-vendor/LoLServer/GameServerLib/Lanerl/LanerlHooks.cs:225",
    "what": "C# catch {} with an empty, uncommented body -- a throwing engine call degrades to a no-op that the caller records as done",
    "fix": "log the exception, or set a failure flag the caller checks",
    "extra": {
      "line": "try { _stats?.Flush(); _stats?.Dispose(); _stats = null; } catch { }"
    }
  },
  {
    "severity": "low",
    "check": "ambiguous-grep",
    "where": "ahriuwu-lanerl/lanerl/diag.sh:60",
    "what": "grep used as a check with no branch on its exit status: a pattern that matches neither success nor failure reads as pass",
    "fix": "assert on the positive marker AND on the absence of the negative one, and fail when neither appears",
    "extra": {
      "line": "echo \"not-in-manifest now: $(grep -c 'not in the manifest' \"$OUT/diag_client.log\" 2>/dev/null)\""
    }
  },
  {
    "severity": "low",
    "check": "ambiguous-grep",
    "where": "ahriuwu-lanerl/lanerl/fix_rads.sh:38",
    "what": "grep used as a check with no branch on its exit status: a pattern that matches neither success nor failure reads as pass",
    "fix": "assert on the positive marker AND on the absence of the negative one, and fail when neither appears",
    "extra": {
      "line": "echo \"    RADS errors: $(grep -c 'RADS' \"$log\" 2>/dev/null)\""
    }
  },
  {
    "severity": "low",
    "check": "ambiguous-grep",
    "where": "ahriuwu-lanerl/lanerl/fix_rads2.sh:67",
    "what": "grep used as a check with no branch on its exit status: a pattern that matches neither success nor failure reads as pass",
    "fix": "assert on the positive marker AND on the absence of the negative one, and fail when neither appears",
    "extra": {
      "line": "echo \"server: $(grep -c 'Game is ready' \"$OUT/server_c.log\") ready\""
    }
  },
  {
    "severity": "low",
    "check": "ambiguous-grep",
    "where": "ahriuwu-lanerl/lanerl/fix_rads2.sh:75",
    "what": "grep used as a check with no branch on its exit status: a pattern that matches neither success nor failure reads as pass",
    "fix": "assert on the positive marker AND on the absence of the negative one, and fail when neither appears",
    "extra": {
      "line": "echo \"RADS errors: $(grep -c 'RADS' \"$OUT/rads_C.log\" 2>/dev/null)\""
    }
  },
  {
    "severity": "low",
    "check": "ambiguous-grep",
    "where": "ahriuwu-lanerl/lanerl/fix_rads3.sh:45",
    "what": "grep used as a check with no branch on its exit status: a pattern that matches neither success nor failure reads as pass",
    "fix": "assert on the positive marker AND on the absence of the negative one, and fail when neither appears",
    "extra": {
      "line": "echo \"not-in-manifest errors: $(grep -c 'not in the manifest' \"$OUT/rads_D.log\" 2>/dev/null)\""
    }
  },
  {
    "severity": "low",
    "check": "ambiguous-grep",
    "where": "ahriuwu-lanerl/lanerl/fix_rads3.sh:55",
    "what": "grep used as a check with no branch on its exit status: a pattern that matches neither success nor failure reads as pass",
    "fix": "assert on the positive marker AND on the absence of the negative one, and fail when neither appears",
    "extra": {
      "line": "echo \"not-in-manifest: $(grep -c 'not in the manifest' \"$OUT/rads_D.log\")\""
    }
  },
  {
    "severity": "low",
    "check": "ambiguous-grep",
    "where": "ahriuwu-lanerl/lanerl/fix_rads4.sh:55",
    "what": "grep used as a check with no branch on its exit status: a pattern that matches neither success nor failure reads as pass",
    "fix": "assert on the positive marker AND on the absence of the negative one, and fail when neither appears",
    "extra": {
      "line": "echo \"not-in-manifest errors: $(grep -c 'not in the manifest' \"$OUT/rads_E.log\" 2>/dev/null)\""
    }
  },
  {
    "severity": "low",
    "check": "ambiguous-grep",
    "where": "ahriuwu-lanerl/lanerl/fix_rads4.sh:65",
    "what": "grep used as a check with no branch on its exit status: a pattern that matches neither success nor failure reads as pass",
    "fix": "assert on the positive marker AND on the absence of the negative one, and fail when neither appears",
    "extra": {
      "line": "echo \"not-in-manifest: $(grep -c 'not in the manifest' \"$OUT/rads_E.log\")\""
    }
  },
  {
    "severity": "low",
    "check": "ambiguous-grep",
    "where": "ahriuwu-lanerl/lanerl/fix_rads5.sh:60",
    "what": "grep used as a check with no branch on its exit status: a pattern that matches neither success nor failure reads as pass",
    "fix": "assert on the positive marker AND on the absence of the negative one, and fail when neither appears",
    "extra": {
      "line": "echo \"not-in-manifest errors: $(grep -c 'not in the manifest' \"$OUT/rads_F.log\" 2>/dev/null)\""
    }
  },
  {
    "severity": "low",
    "check": "ambiguous-grep",
    "where": "ahriuwu-lanerl/lanerl/fix_rads5.sh:70",
    "what": "grep used as a check with no branch on its exit status: a pattern that matches neither success nor failure reads as pass",
    "fix": "assert on the positive marker AND on the absence of the negative one, and fail when neither appears",
    "extra": {
      "line": "echo \"not-in-manifest: $(grep -c 'not in the manifest' \"$OUT/rads_F.log\")\""
    }
  },
  {
    "severity": "low",
    "check": "ambiguous-grep",
    "where": "ahriuwu-lanerl/lanerl/fix_rads6.sh:73",
    "what": "grep used as a check with no branch on its exit status: a pattern that matches neither success nor failure reads as pass",
    "fix": "assert on the positive marker AND on the absence of the negative one, and fail when neither appears",
    "extra": {
      "line": "echo \"not-in-manifest: $(grep -c 'not in the manifest' \"$OUT/rads_F.log\")\""
    }
  },
  {
    "severity": "low",
    "check": "ambiguous-grep",
    "where": "ahriuwu-lanerl/lanerl/run_WORKING.sh:53",
    "what": "grep used as a check with no branch on its exit status: a pattern that matches neither success nor failure reads as pass",
    "fix": "assert on the positive marker AND on the absence of the negative one, and fail when neither appears",
    "extra": {
      "line": "echo \"mp4: $(ls -lh \"$OUT/gameplay_final.mp4\" 2>/dev/null | awk '{print $5}')  crashes: $(grep -c c0000005 \"$OUT/client_w.log\")\""
    }
  },
  {
    "severity": "low",
    "check": "ambiguous-grep",
    "where": "ahriuwu-lanerl/lanerl/run_gpu.sh:47",
    "what": "grep used as a check with no branch on its exit status: a pattern that matches neither success nor failure reads as pass",
    "fix": "assert on the positive marker AND on the absence of the negative one, and fail when neither appears",
    "extra": {
      "line": "echo \"surface segfaults: $(grep -c c0000005 \"$OUT/client_g.log\")\""
    }
  },
  {
    "severity": "low",
    "check": "ambiguous-grep",
    "where": "ahriuwu-lanerl/lanerl/run_real_x.sh:50",
    "what": "grep used as a check with no branch on its exit status: a pattern that matches neither success nor failure reads as pass",
    "fix": "assert on the positive marker AND on the absence of the negative one, and fail when neither appears",
    "extra": {
      "line": "echo \"surface segfaults: $(grep -c c0000005 \"$OUT/client_g.log\")\""
    }
  },
  {
    "severity": "low",
    "check": "port-literal",
    "where": "ahriuwu-lanerl/lanerl/scaling_test.sh:17",
    "what": "hardcoded port 5200: safe alone, collides under any parallel run",
    "fix": "take the port from an argument with no shared default",
    "extra": {}
  },
  {
    "severity": "low",
    "check": "port-literal",
    "where": "ahriuwu-lanerl/lanerl_bot/bench/measure_all.py:67",
    "what": "hardcoded port 5410: safe alone, collides under any parallel run",
    "fix": "take the port from an argument with no shared default",
    "extra": {}
  },
  {
    "severity": "low",
    "check": "port-literal",
    "where": "ahriuwu-lanerl/lanerl_bot/tests/conftest.py:119",
    "what": "hardcoded port 5902: safe alone, collides under any parallel run",
    "fix": "take the port from an argument with no shared default",
    "extra": {}
  },
  {
    "severity": "low",
    "check": "port-literal",
    "where": "ahriuwu-lanerl/lanerl_bot/tests/conftest.py:93",
    "what": "hardcoded port 5901: safe alone, collides under any parallel run",
    "fix": "take the port from an argument with no shared default",
    "extra": {}
  },
  {
    "severity": "low",
    "check": "port-literal",
    "where": "ahriuwu-lanerl/lanerl_bot/tests/test_cs_baseline.py:34",
    "what": "hardcoded port 5911: safe alone, collides under any parallel run",
    "fix": "take the port from an argument with no shared default",
    "extra": {}
  },
  {
    "severity": "low",
    "check": "port-literal",
    "where": "ahriuwu-lanerl/lanerl_bot/tests/test_cs_baseline.py:41",
    "what": "hardcoded port 5912: safe alone, collides under any parallel run",
    "fix": "take the port from an argument with no shared default",
    "extra": {}
  },
  {
    "severity": "low",
    "check": "port-literal",
    "where": "ahriuwu-lanerl/lanerl_bot/tests/test_cs_baseline.py:69",
    "what": "hardcoded port 5913: safe alone, collides under any parallel run",
    "fix": "take the port from an argument with no shared default",
    "extra": {}
  },
  {
    "severity": "low",
    "check": "port-literal",
    "where": "ahriuwu-lanerl/lanerl_bot/tests/test_cs_baseline.py:99",
    "what": "hardcoded port 5914: safe alone, collides under any parallel run",
    "fix": "take the port from an argument with no shared default",
    "extra": {}
  }
]