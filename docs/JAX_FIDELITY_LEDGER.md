# JAX simulator fidelity ledger

**Purpose.** This is the durable, central record of behavior in the JAX
training simulator that is not proven identical to the vendored C# server.
Read it before interpreting an RL failure as a learning failure.  A local code
comment or an audit can contain the detailed evidence, but every behavior that
can change trajectories, observations, rewards, or legal actions must also have
an entry here.

This document is a ledger, not a backlog.  Entries remain after they are fixed;
change their status to `VERIFIED` and add the test/evidence.  New approximations
must be entered in the same commit that introduces them.

## Status vocabulary

| status | meaning |
|---|---|
| `MISSING` | Server behavior has no implementation. |
| `APPROX` | Implemented deliberately, but not server-equivalent in at least one known case. |
| `UNOBSERVABLE` | The reset/parity input does not expose enough server state to reconstruct it exactly. |
| `BOUNDED` | Equivalent inside an explicit fixed-shape bound; exceeding the bound is a diagnostic failure. |
| `VERIFIED` | Source-port and tests or differential evidence support parity for the stated scope. |

## Phase 1 gate dashboard (canonical results, 2026-09-18)

This is the short authoritative answer to “is Phase 1 done?” Detailed history
remains in `JAX_REWRITE_PLAN.md`, but stale exploratory numbers must not replace
the canonical rows below.

| gate | status | canonical evidence | remaining work |
|---|---|---|---|
| 1. whole-corpus one-step parity | **OPEN against a criterion that can now be met.** `GATE1-002` sets the pass condition, because the literal §3 wording (`exact` on every field) has no reachable pass state -- several of the quantities are not observable exactly through this dump, and a test cannot certify what its instrument cannot measure. The new condition is STRICTER on the simulator (zero unexplained rows) and honest about the instrument. Remaining work is attribution, not reduction | Same recorded server trace throughout (`lanerl_jax/runs/tier1_full/server/instance000.log`, job 882's log), re-differentiated with `--existing-log` so every number below is the identical corpus: **19,800 tick-pairs, 910,216 unit injections, 19,800/19,800 on diagnostic NetId identity, 0 legacy-proximity fallbacks.** Baseline job **981** reproduced job 943 field-for-field, so the concurrent `profiles.py`/`init.py` work in the tree does not move gate 1 and the before/after below is attributable to the two fixes alone. **Superseded in part on 2026-09-18 by `ORDER-005` + `INJ-003` (job 1020 -> 1043, same log): LaneMinion move order 4,791 -> 1,388, LaneTurret move order 2 -> 0 (100.00%), minion target 736 -> 522, waypoints 333 -> 317, `aa_fire` 976 -> 966, `is_attacking` 1,534 -> 1,525, `has_auto_attacked` 1,968 -> 1,964, `aa_cooldown` 11,700 -> 11,691, everything else unchanged and nothing regressed.** **Current canonical state of the whole corpus, job 1059, on today's HEAD (`ORDER-005` + `INJ-003` + `AA-002` together, same log, 19,800 tick-pairs): LaneMinion move order **354** (99.91%), `aa_hit` **7** (100.00%), hp **263** (99.93%), `has_auto_attacked` **931**, `is_attacking` **1,006**, target **522**, waypoints **317**, `aa_fire` **967**, `aa_cooldown` **11,691**, position L-inf **15**; LaneTurret move order **0**, hp 33 -> **7**; champion 100% on every field. The three fixes compose -- no field is worse than either measured alone.** **SUPERSEDED 2026-09-19 by the first full-corpus run since `AA-003` (the `aa_cooldown` grid snap), same 575 MB log, same 19,800 tick-pairs, 19,800/19,800 on diagnostic NetId identity, 0 legacy-proximity fallbacks -- and notably the first one run on the LOGIN node rather than `desktop`, which was `DOWN+NOT_RESPONDING`, at 0.9 GB peak instead of ~26 GB (see the memory note below): LaneMinion `aa_cooldown` **11,691 -> 2,070** (99.48%), and EVERY other field bit-identical to job 1059 -- `aa_fire` 967 (99.76%), `aa_hit` 7 (100.00%), `has_auto_attacked` 931, `is_attacking` 1,006, hp 263 (99.93%), move order 354 (99.91%), target 522 (99.87%), waypoints 317 (99.92%), position L-inf <=1/16 **15 of 395,409 = 100.00%**; LaneTurret move order / position / waypoints / `aa_hit` all 100.00%, hp 7, target 30, `aa_cooldown` 44 **all at exactly 1.1836 s**, i.e. one whole turret swing period -- a contained off-by-one-swing, not a spread; Champion **100.00% on every field** over 39,600 ticks. The -82.3% on `aa_cooldown` reproduces the 400-pair window A/B's predicted -81.5% at corpus scale with zero collateral, which is what makes the attribution safe to quote.** **Two further results from the same run that change how three residuals must be read.** (1) **hp is fully confined to the missile blind spot**: 74.3% of ticks (14,715/19,800) carry a missile in flight, 1.7% of those also carry an HP disagreement, and the HP-disagreement rate on the 5,085 missile-FREE ticks is **0/5,085 = 0.00%**. Every one of the 263 is a `RESET-004` tick, so hp is `BOUNDED`, not a damage-formula error. (2) **The damage asymmetry is one-sided and that is a real signal, not noise**: on HP-change agreement the sim deals damage on **228** ticks where the server does not, against **15** the other way, and on death agreement the sim kills a minion the server keeps alive on **21** ticks against **1** the other way. **CORRECTED the same day by `AA-004`: these are NOT gate 3's over-farming seen at one-tick resolution, and reading them that way was wrong.** Both asymmetries are measured on INJECTED ticks, and `AA-004` shows the injected swing gate is opened early by the dump's own clamp on 100% of the rows. Gate 3 runs FREE, with no injection, so the clamp cannot act there at all. The two must be explained separately, and `GATE3-001` already says what the free-running root is: POSITION at the wave clash, eight seconds before HP. Wave spawning: 6 of 120 spawn ticks disagreed on count.** Counts below are misses, job 981 -> job 999 (`HOLD-001` + `INJ-002`): **LaneMinion position L-inf <=1/16 1,742 -> 15 (-99.1%; 393,655/395,397 -> 395,382/395,397 = 100.00%)**, Euclidean <=1/16 1,843 -> **39**, bit-exact 8,325 -> 6,966 with p95 error **5.2250 -> 0.0625 u** (i.e. the whole tail is gone and what is left is the dump's own quantisation); **waypoints 800 -> 333** (-58%, and better than the 716 that preceded `ORDER-002`); move order 4,807 -> **4,791**; `aa_fire` 984 -> **976**; `is_attacking` 1,559 -> **1,534**; `has_auto_attacked` 1,973 -> **1,968**; `aa_cooldown` 11,725 -> **11,700**. Unchanged by design: hp 1,127 -> **1,126**, target 736 -> **736**, `aa_hit` 1,041 -> **1,041**, every LaneTurret field, and minion death agreement (89/11/33 both runs). Champion stays **100% on every field**. Nothing regressed. | **One artefact blocks it.** The canonical corpus predates all four emissions (`aacdbits`, `aagate`/`status`, `aibuffs`, the branch stream), so its residuals can be counted but not observed. Regenerate it with `tier1_full --instrumented` at the same seed and 420 s, verify by hash that it reproduces the historical corpus byte for byte (every emission is behaviour-neutral by construction and measured so), then attribute each field in turn under `GATE1-002`'s five clauses. **The residual list is shorter than it looked, and two of the four were never simulator deviations.** (1) **Position was a harness bug** (`INJ-002`): `inject.py` clamped the injected `CurrentWaypointKey` to `Count - 1`, so a unit the server had stopped with `ResetWaypoints` (`Waypoints=[Position]`, `key=1`) was handed a live one-waypoint route back to where it already stood, walked back to it, and cancelled that tick's collision escape. Movement and the collision pass were both already exact: post-fix, minions with **zero colliding neighbours are 237,414/237,414 (100.000%)** and the old crowding gradient (100%/99.63%/99.33%/98.65% by 0/1/2/3+ neighbours) flattens to 100.000%/99.994%/99.987%/99.992%. (2) **hp and `aa_hit` are one mechanism, not two**: 1,042 of the 1,127 hp misses are exactly one hit of damage (12.0 x814 melee, 23.0 x228 caster) landing on a different tick, and there are 1,041 `aa_hit` misses -- the same event counted in two fields. `RESET-004`'s missile blind spot is **refuted as the cause**: only 47/1,127 coincide with a missile leaving the field at all and only **12 (1.1%)** involve a missile the injector could not restore (449 of 44,870 missiles dropped, 1.00%). That 1.1% is the measured unobservable floor. (3) **`aa_cooldown` 11,700 is bimodal and adds nothing new**: 82.2% sit at <=2 StatQ quanta (sub-tick rounding; the tick stride is 1024/60 = 17.0667 quanta, not an integer) and **0.0%** of those coincide with an `aa_fire`/`aa_hit` miss, while 8.9% sit at >40 quanta -- p95 802 and max 1512, i.e. exactly the 0.7832 s and 1.4766 s minion periods -- and **93.5%** of those are already counted by `aa_fire` or `aa_hit`. (4) **The booleans add 69 unit-ticks, not 3,502**: `has_auto_attacked` is **1,968/1,968 contained** in `aa_fire` U `aa_hit` and `is_attacking` is 1,465/1,534, leaving **69 uncovered**. (5) **target 736 is `ORDER-003`**, the serial-vs-fixed-phase floor, and it is one-sided: `server ACQUIRED from none, sim did not` **253** against the mirror `sim ACQUIRED from none, server did not` **0**. So what gate 1 still owes is: one swing-completion phase residual (`aa_hit`/hp/`aa_cooldown`'s tail, ~1,041 events, melee-specific at 1.073 mismatched ticks per completion against ranged 0.060), `ORDER-003`'s target residual (736 -> **522**), 317 waypoints and 15 positions. **Move order is no longer on that list as an independent item.** `ORDER-005` and `INJ-003` took it 4,791 -> **1,388**, and the whole-corpus containment join now returns **1,388 of 1,388 (100.0%)** sharing a tick with another scored disagreement against 1,084 of 4,791 (22.6%) before -- **0 move-order-only unit-ticks, down from 3,707**. 1,041 of the 1,388 coincide with an `aa_hit` miss, and there are exactly 1,041 `aa_hit` misses in the corpus, so what is left of this row is `AA-002`'s swing-completion phase seen through a second field, plus `ORDER-003`'s fixed-phase sweep. It is now genuinely symmetric where it used to be absolute: `sim WROTE AttackTo->Hold, server held` **536** against `server WROTE AttackTo->Hold, sim held` **536**, from 22 against 4,769. **Update 2026-09-18, `AA-002` (job 1018 -> 1039, same `--existing-log`, 19,800 pairs; job 1018 reproduced job 999 field-for-field first, so the before/after is attributable to this fix alone):** LaneMinion `aa_hit` **1,041 -> 7 (100.00%)**, hp **1,126 -> 263 (99.93%)**, `has_auto_attacked` **1,968 -> 935**, `is_attacking` **1,534 -> 1,015**, `LaneTurret.hp` **33 -> 7**. Downstream: HP-change agreement sim-only **651 -> 228** / server-only **439 -> 15**, minion death disagreement **11 sim-alive + 33 sim-dead -> 1 + 21**, and the `RESET-004` missile split **7.0%/1.3% -> 1.7%/0.0%**. Unmoved: `aa_cooldown` 11,700, target 736, move order 4,791, waypoints 333, positions 15, every LaneTurret field but hp. Moved the wrong way by one: `aa_fire` **976 -> 977**. Champion stays 100% on every field. So gate 1 now owes `aa_fire`/`aa_cooldown` (one mechanism, `RESET-002`'s cooldown quantum), `ORDER-003`'s 736, 4,791 move-order labels, 333 waypoints, 15 positions and a 263-miss hp remainder that is no longer melee-shaped. |
| 2. free-running divergence characterized | **PASS** | Four 600 s scenarios are recorded in `TIER2_DIVERGENCE.md`; divergence begins around wave interaction and is explicitly large. | None for Phase 1; retain as a regression corpus. |
| 3. oracle last-hitter CS@10 | **OPEN; chain traced to one unexplained death** | Canonical routed run, 18,000 decisions: sim CS=3 / attacks=54 / deaths=2 / level 7 against server CS=4 / attacks=86 / deaths=1 / level 8. **Everything reduces to one event, and that event is `COLL-003`.** (1) 82.1% of the 3,203 excess approach decisions is the sim's second death landing mid-walk. (2) The level gap is exactly five missing melee XP grants, all in the window holding both deaths; grant sizes, the 82 red-minion deaths and the level curve all match the server exactly. (3) Splitting exposure by phase: **engaged in lane, the two engines are identical -- 9,395 attacker-decisions against 9,381 (1.001x)**; walking in, 4,210 against **60**. 100% of the exposure gap and 83% of the damage gap is one respawn walk in which the champion travels 800 units of path for **1 unit of net displacement** over 811 decisions, wedged at the exact sum of the collision radii. Standing in lane the sim is *less* crowded than the server. Ruled out with numbers: acquisition census (96.1% vs 95.5% with no alternative), acquisition radii (475/700, identical by source and table), blue attrition (blue lives *longer* in the sim, 61.5 s vs 59.4 s), clash midpoint (0.50 both), movement speed (11.49 vs 11.50 u/dec), `MOVE-001` (never binds), route fallback (324/6,400 decisions, still at full speed), `AA-001` (1.009x swing rate in rollout, not 2.19x), and `TURRET-001` (right size, **wrong sign**). | **The 394.3 s death is explained, and the mechanism is not clumping and not an aggro rule.** The arrival process was measured (`parity/gate3_arrivals.py`, 2026-09-18, analysis-only on the two on-disk occupancy series plus the `MRT` log) and it **refutes the clumping lead**. (a) The `0.80x` on ">=1 attacker" is normalisation: the sim is engaged 11,698 decisions against 14,794, and per engaged decision ">=1 attacker" is **24.2% against 23.9%**. (b) Arrivals are bursty on BOTH engines and the server is the burstier: index of dispersion 2.61/2.80/3.33/3.40/4.02 (sim) against 2.78/3.09/3.50/4.47/4.26 (server) over 0.5/1/2/5/10 s windows, interval CV 2.87 against 3.03 (Poisson = 1.00), largest single-decision clump 6 on both. The shared burst mechanism is `TargetJustDied()`: several red minions share one blue minion, it dies, and `LaneMinionAI.OnUpdate`'s first trigger term fires for all of them on the same tick. (c) What differs is **hold length**: 51 holds / mean 267 decisions against 45 / 211, i.e. **1.26x**, and uniformly -- by attacker type the split is identical (22 melee / 28 caster / 1 cannon against 21 / 23 / 1, mean holds 152 / 362 / 122 against 136 / 287 / 21) as is the acquisition distance (median 450 u against 461 u). Longer overlapping holds are what raise concurrency: per engaged decision, >=3 attackers is 15.8% against 9.2% (1.71x) and >=8 is 0.73% against 0.26%. See `CFH-001`. **That differential now exists and has been run** (`parity/tier15.py`, the Tier-1.5 rows in "Canonical commands"). It rules the collision pass *out* as the cause and it **refutes the hold-length and attacker-distance leads outright**: from one identical engaged state the attacker sets are identical tick by tick (5.4743 mean on both, 2,343 attacker-ticks on both, six holds open on both, zero closed on either) and the sim's attackers stand **closer** to their own wave, not further (1,229 u against 1,347 u; 90.7% against 93.3% beyond 475 u). It also found and removed the reason the first run of it looked catastrophic -- `INJ-001`, an injected champion standing in its own fountain and healing 100 HP/s -- and named `ITEM-001`, a 7.2% champion move-speed gap that makes any post-81 s displacement comparison meaningless until the shop is turned off. What the differential points at instead is a **one-tick stop/go phase error**: minions are bit-identical for 33 ticks, then one goes a tick out of phase and separates linearly; the champion holds to 0.04 u for 68 ticks and then takes one extra 5.75 u step because the server went HOLD a tick earlier. That is `AA-001`'s shape -- and `AA-001` is already fixed (`a0c5916`), which these runs were measured against, so the remaining one-step residuals (move order 4,807, `aa_fire` 984, `is_attacking` 1,559) are where to look next, not the collision pass. The contact statistic to watch is 38.1% of ticks inside the 70 u trigger against the server's 0.00%. Then re-run and re-attribute rather than assuming the chain unwinds. Two residuals to revisit afterwards, both currently downstream: per-hit damage +7.8% (armour, i.e. the level gap) and champion regeneration 1,869 HP against 2,661. **Both of those, and every HP-denominated number in this row, were measured against a sim champion carrying 6-12% too much max HP and 1.2 HP/s of an item's regen it should never have had -- see `STAT-001`, fixed 2026-09-18. The sim was the tougher engine, so the extra death is *more* anomalous, not less; re-measure before re-arguing them.** **Re-measured 2026-09-18 on today's HEAD (`68f4d87`), and the row above is now stale in its DIRECTION, not just its magnitude.** Server, two fresh boots on different ports plus the two historical runs: `cs=4 attacks=86 holds=14,717 moves=0 approach=3,197 deaths=1`, **every field identical in all four** -- the server is bit-reproducible. Sim, 8 seeds: `cs=7 attacks=71 holds=13,588 moves=0 approach=4,341 deaths=2 vis_mean=4.825`, **every field identical in all 8**, because the simulator consumes no randomness at all (`jax.random` appears once in `sim/`, creating a key `empty_state` stores and no dynamics code ever reads). **So gate 3's noise floor is zero on both sides and `CS_TOLERANCE = 0` is achievable by construction** -- a concern that the gate was applying a zero tolerance to a small-count statistic was raised and is refuted by this measurement. The gap is therefore real signal, and it has changed sign: the sim now scores **7 against the server's 4**, where the canonical run in this row recorded 3 against 4. The whole causal chain above -- extra death -> mid-walk wedge -> 82% of approach excess -> 5 missing XP grants -> level gap -> under-farming -- was built on a sim that farmed too LITTLE, and no longer describes the failure. It is retained as history; do not reason from it. (The sim's CS has been 7 before: this test's own docstring records `cs=7 attacks=72 approach=1,768 deaths=1`. Today's 7 comes from a materially different trajectory -- approach 4,341, deaths 2 -- which is itself evidence that CS is a coarse summary of this fixture.) **The sharp remaining number is conversion.** The sim spends **71 attack-decisions for 7 kills (10.1 per CS)**; the server spends **86 for 4 (21.5 per CS)**. Both drivers re-issue ATTACK every decision while a swing is in flight, so that ratio should agree unless the two engines disagree about which swings land or who gets the kill. Ruled out already, each by experiment rather than argument: `lethal_epsilon` asymmetry (both drivers pass 0.0); fog starvation (the sim champion sees 4.825 enemy minions per decision); and the observation wire's integer HP (every one of 33,029 damaged-minion HP samples on the wire is an integer and `LanerlWire` truncates, so the server oracle reads HP <= truth and can commit an unkillable swing -- but flooring minion HP in the sim moved CS, attacks, deaths and approach by **exactly zero**). **Measured, and the conversion framing above is superseded by it -- the ratio was a mean over a count of four, hiding a bimodal distribution.** Grouping attack-decisions into maximal runs on one target uid (both engines through identically wrapped `decide`): sim **7 attempts**, lengths `[10,10,10,10,10,10,11]`, **7/7 converted**; server **5 attempts**, lengths `[10,10,10,11,45]`, **4/5 converted**. Four of the server's five attempts are 10-11 decisions, exactly the sim's cadence -- which agrees with the independent check that the two engines' attack-speed multipliers match to within the wire's 2-decimal rounding (sim 1.02088/1.04278/1.06568 against a dumped 1.020/1.040/1.070 at levels 2-4). **One** 45-decision attempt inflated the server's attack-decision total from ~41 to 86 and produced the entire apparent 10.1-vs-21.5 difference. There is no cadence difference and no general conversion difference. So the 3-CS gap decomposes as **2 CS of missing opportunity** (the sim gets 7 last-hit attempts, the server 5) plus **1 CS of one failed attempt** (the 45-decision run, about one full attack cycle spent on a target that never died). That single failure is the signature the wire's integer HP predicts -- the server oracle reads floored HP, believes a minion is one-shot killable, commits, and it survives. The sim-side flooring experiment above refuted that as an explanation of the CS *level* and correctly so, but it could not test this, because the sim has no non-converting attempt to perturb; the hypothesis is live again on those narrower grounds. **The larger half is the opportunity count**: both champions hold at a fixed lane position, so the open question is why 7 minions enter the sim's one-shot band and only 5 the server's. **Measured 2026-09-18** (`parity/reach_hp_mortality.py`, new and read-only; reproduces this row's own cs=7/4, attacks=71/86, holds=13,588/14,717, approach=4,341/3,197, deaths=2/1 exactly, both on a dirty tree at digest `96004d935a0b` and again after committing the module at `30ce1359ac5a` -- three runs, all bit-identical). **Champion position is REFUTED first, as the cheapest check**: both champions occupy essentially the same spot -- mean (2,578.8, 13,137.8) sim against (2,538.3, 13,117.0) server, similar spread (x span ~600 u both sides) -- so this is not a mismatched-lane-position artifact. **Total enemy-minion mortality over the same 600 s is IDENTICAL**: 82 deaths on both engines, with an identical melee/caster/cannon split (39/39/4). Wave attrition as a population statistic is not broken, and **this is NOT the old 2-blue-vs-28-red collapse** (`sim/init.py`'s all-24-turrets placement and spawn-symmetry fixes already closed that one; a real collapse would show up here as a gross mortality-count asymmetry, and none exists). **The full "above-lethal, in-reach" HP distribution is bimodal on both engines (a spike at each type's own max HP plus a declining tail) and the asymmetry lives entirely in MELEE.** Caster HP-at-arrival is statistically the same engine to engine (mean 249.0 sim / 271.8 server, fraction-near-max 0.64/0.65, n=1,349/1,088) -- but melee is not (fraction-near-max **0.68 sim / 0.40 server**, median **455 [at max HP] sim / 386 server**, n=4,037/3,321). Turret-attributed kills (100% at the OUTER tier, the only one ever in range at this lane position) are **30/82 in the sim against 17/82 on the server** -- roughly double -- meaning more melee minions survive PAST the champion's held position and die to blue's own outer turret instead of to blue's wave; this is the same fact as the HP-at-arrival gap, seen from the other end of the lane. Ranked candidates: **(b) minion-vs-minion target acquisition/engagement is the best-supported lead** -- melee sits at the BOTTOM of the `ClassifyUnit` priority table (`sim/targeting.py`'s `base_priority`: melee 9, below caster's 8 and cannon/super's 7), so it is structurally the type most exposed to any small difference in how reliably a wave re-engages a target, and it is exactly the type that is asymmetric here while caster is not. **(a) a blanket "minions deal less damage to each other in the sim" is NOT supported alone** -- it predicts caster would move too, and caster does not. **(c) population/composition differences are not ruled out, but the equal TOTAL mortality argues against a large one.** **(d) champion position is REFUTED** (above). No sim change is proposed here -- this was measurement only; the next step is a Tier-1-style injected differential on one melee minion's own target/engagement history at the point it enters the champion's reach, the same method that settled `CFH-001` and `COLL-003`. |
| 4. full-loop throughput >=56k decisions/s | **PASSES without `SmoothPath`; 9.2% short with it** | RTX 5080, canonical command. **The routed delta was never the hop table.** Profiled 2026-09-18: routing is 4.349 ms of a 74.4 ms step, and inside it `closest_terrain_exit` is **90.3%** while the 231 MiB table's gathers are **0.3%**. Three independent refutations of the table: the route loop runs 6 trips at unroll 8 (1.74% body utilisation); 8,192 gathers cost 14.1 us from the 231 MiB table against 13.6 us from 16 KiB and an 11.8 us empty-kernel floor; and it achieves 51.5 GB/s, 5% of peak. After the spiral unroll and the `CastCircle` restructure (`d47ab44`): **no-route control 59,042 · routed without `SmoothPath` 58,255 (PASS, +3.2%) · routed with `SmoothPath` 51,270 (short 9.18%) · with `SmoothPath` at a 64-step line bound 54,741 (short 3.03%)**. The prior 55,050 and the stale 57,378 no-route figure both predate this. | **This is now a fidelity/throughput decision, not a bug.** `SmoothPath` costs about 12% and takes waypoint-count agreement from 6.0% to 55.0%. Three options, all measured: ship the control and fail gate 1's waypoint clause; ship `SmoothPath` and miss gate 4 by 9.2%; or lower `SMOOTH_CAST_LINE_STEPS` to 64 for -3.03%, which fails **closed** (less smoothing, never a wrong route) on the 8.2% of segments that exceed it and is reported through `smooth_exhausted`. Not yet tried: the line bound measured on the *real* action lattice rather than a uniform +/-50-cell corpus, which is likely far tighter than 128 since gameplay clicks are viewport-bounded. |
| 5. compile under two minutes | **PASS** | Routed compile 20.9-21.4 s across the three 2026-09-18 runs (earlier record: 12.9-13.6 s; it has grown with the routing work but remains far inside the 120 s gate). | None. Watch it: it moved 60% without anyone noticing, which a gate with this much headroom will not catch. |
| 6. reset cost small | **PASS** | 2.17% of a step at 512 envs and 1.74% at 2,048. | None. |

Phase 1 is therefore **not complete**, but as of 2026-09-18 it is no longer
open in the way it was. **Gate 4 passes** on the simulator without
`SmoothPath`, and what is left there is a stated fidelity/throughput trade, not
a missing lever. **Gate 3's chain is fully traced and its last link is
*not* a bug**: engaged in lane the two engines are identical to 0.1%, and the
deficit is one respawn walk spent wedged in a wave -- by a collision rule that
`COLL-003` now verifies as faithful. The extra death at 394.3 s that put the
champion there is now traced too, and its leading suspect was wrong: the
arrival process is bursty on **both** engines and the server is the burstier
(`CFH-001`), call-for-help propagation and release are verified faithful in
the sim (32 of 33 holds release within 3 decisions of the first valid call),
and what actually differs is that the sim's attackers stand 589 u from the
nearest blue minion against the server's 389 u -- outside the 475 u broadcast
radius -- so nothing ever recalls them and holds run 1.26x long. Where they
stand is a lane-state question and is the one thing still open. **Gate 1**'s
three named mechanisms are now fixed and measured: `AA-001` (every sim swing
one tick early), `TURRET-001` (acquisition wider than retention) and
`ORDER-002` (Hold written during a windup). Together they took minion move
order from 34,295 misses to 4,807, minion AA fire from 2,880 to 984, and
turret target from 1,508 to 30, with the champion still exact on every field.
**Then, later the same day, two more (`HOLD-001`, `INJ-002`) took minion
position from 1,742 misses to 15 and waypoints from 800 to 333.** The
important part of that is not the size but the kind: `INJ-002` was a bug in
the *parity harness*, not in the simulator. `inject.py` clamped the injected
`CurrentWaypointKey` below the value a stopped unit actually carries, which
let an injected minion walk back over its own collision escape -- so the
largest gate-1 residual, the one that read as "movement itself is not exact",
was the instrument. With it gone, minions with no colliding neighbour are
exact on **237,414 of 237,414** one-step pairs and the crowding gradient is
flat. That is the second injector artifact found in one day (`INJ-001` was the
first), and the standing lesson is to probe a new residual against the harness
before attributing it to `sim/`.

A fourth injector artifact and a fourth unported writer landed later the same
day, and between them they are the largest single move in gate 1 since
`ORDER-002`. `ORDER-005`: **an auto-attack is a cast, and `Spell.FinishCasting`
writes the caster's move order** -- `UpdateMoveOrder(OrderType.Hold, true)`,
route reset included -- on the tick a wind-up runs out. The sim had ported
those exact three lines for Garen's R and never routed the auto-attack through
them. `INJ-003`: the dump rounds `minionActionTimer` to 1/1024 ms, the server
crosses its `>= 250f` trigger by **0.0000305 ms**, and the rounding error is
**0.00035 ms in the wrong direction** -- so **the 250 ms sweep fired on 0 of
395,366 injected minion tick-pairs** against the server's own 26,537. Every
Tier-1 minion-controller number before this was taken with the regular sweep
switched off. Together, on the identical log: move order **4,791 -> 1,388**,
turret move order **2 -> 0**, minion target **736 -> 522**, waypoints
333 -> 317, and nothing worse anywhere. The standing lesson from `INJ-001` and
`INJ-002` now has a third case and a sharper edge: **an injector artifact need
not perturb a value, it can disable a controller**, and that is invisible to
every field-level rate because the controller's *output* is re-injected each
tick.

What is genuinely left is shorter still, because `AA-002` closed on
2026-09-18 and it was **the third harness bug of the day, not a simulator
deviation**. `aa_hit`, the 92.5% of hp that was one hit of damage landing a
tick out, `has_auto_attacked` and `is_attacking` were one event: the injector
wrote the dump's *rounded* windup into a sim that tests `windup - dt <= 0`,
and for the two minion profiles whose windup is a near-exact whole number of
ticks (blue melee 20.0016, red cannon 33.0000) the true remainder on the last
windup tick is 2.67e-5 s, which the dump's 1/1024 s quantiser publishes as a
flat zero. That -- not anything about melee damage application -- is the whole
of the 18x melee/ranged asymmetry, and recovering the tick index `k` from the
rounded value took `aa_hit` from 1,041 misses to **7** and hp from 1,126 to
**263**. It also drained `RESET-004`'s missile confound to nothing: the
HP-disagreement rate on missile-free ticks is now **0/5,085**. The residual
that is left is `aa_fire` (977) and the >40-quantum tail of `aa_cooldown`,
which this proved belong to each other and **not** to `aa_hit`: fixing every
hit tick moved `aa_cooldown` by zero. Minion target (736) is `ORDER-003` and
is a **design floor**, not a bug:
the server's `ObjectManager.Update` is serial and can react to a kill or a
call for help mid-tick, our vectorised step cannot, and the residual is
one-sided to a clean zero (253 server-first acquisitions against 0 sim-first).
936 of the remaining `aa_fire` cases are still the `RESET-002` cooldown quantum
and are not fixable from the dump. Gate 4 is unchanged and independent, and
remains the one gate with no identified lever.

## Canonical commands

**Read this before quoting any number below.** Every figure in the dashboard
came from one of these commands. A number produced any other way is a
diagnostic, not gate evidence, and must say so where it is written down.

This section exists because its absence is what cost the most: the routed
53,605 dec/s gate-4 figure came from a script that was never committed, so it
went stale without anyone noticing, and `benchmark.py`'s own `__main__` could
only run the no-route control. Two `slurm/*.sbatch` files pointed at worktrees
that had been deleted. `lanerl_jax/tests/test_canonical_commands.py` is the
mechanical guard against that rotting again; it runs in 0.15 s.

| gate | command | where |
|---|---|---|
| 1 | `sbatch slurm/parity.sbatch python -m lanerl_jax.parity.tier1_full` | `desktop`, CPU |
| 1 (residual attribution) | `sbatch slurm/parity.sbatch python -m lanerl_jax.parity.tier1_residual_drill --existing-log lanerl_jax/runs/tier1_full/server/instance000.log --max-pairs 20000` — names the mechanism behind every gate-1 field that is short of 100%: move-order/waypoint confusions, the position residual against its **crowding denominator**, the target residual split into retention vs acquisition against the shared pre-tick target, `aa_hit` split melee vs ranged, the `aa_cooldown` bimodality, the `is_attacking`/`has_auto_attacked` containment join, and the missile census. For move order specifically it also prints the full confusion matrix with each cell beside its transpose, the split against the **shared injected pre-tick order** (which is what separates "the server wrote and we held" from the mirror, and is how `ORDER-005` was found after a single-window count had read the same residual as symmetric jitter), concentration by minion class / game phase / swing phase, the in-range margin at both observable instants, and the containment join against every other scored field. Quote a gate-1 residual's *mechanism* from here, never from `tier1_full`'s rates alone | `desktop`, CPU; ~6 min |
| 1 (waypoints) | `python -m lanerl_jax.parity.movement_parity` — scores the host A*+SmoothPath port **and** the production device router on the same 20 Moves; boots a real server | `desktop`, CPU |
| 1.5 (fixture) | `sbatch slurm/parity.sbatch python -m lanerl_jax.parity.tier15 record --out-dir lanerl_jax/runs/tier15_noshop --decisions 12000 --port-base 45300 --no-autobuy --repeat 2` — records the driven 400 s fixture twice and compares the server's own state-hash stream tick by tick. **That determinism check is what licenses the whole tier** (it came back 24,002/24,002 identical), so re-run it whenever the server build changes. `--no-autobuy` is not optional: see `ITEM-001` | `desktop`, CPU; ~3 min |
| 1.5 (`COLL-003`) | `sbatch slurm/parity.sbatch python -m lanerl_jax.parity.tier15 run --fixture lanerl_jax/runs/tier15_noshop --predicate champ_near_minion --radius 90 --min-hp-frac 0.5 --decisions 600 --out lanerl_jax/runs/tier15_noshop/q1_champ_into_wave.json` | `desktop`, CPU |
| 1.5 (gate 3) | `sbatch slurm/parity.sbatch python -m lanerl_jax.parity.tier15 run --fixture lanerl_jax/runs/tier15_noshop --predicate engaged --min-attackers 3 --min-hp-frac 0.5 --decisions 600 --out lanerl_jax/runs/tier15_noshop/q2_engaged.json` | `desktop`, CPU |
| 2 | `sbatch slurm/parity.sbatch python -m lanerl_jax.parity.tier2_batch --engine server` then `--engine sim`, then `python -m lanerl_jax.parity.tier2 compare --a ... --b ... --out ...` | `desktop`, CPU |
| 3 | `pytest lanerl_jax/parity/tests/test_last_hit_gate.py::test_oracle_scores_the_same_cs_in_sim_and_server` | boots a real server; several minutes |
| 4, 5 | `srun -p gpup --gres=gpu:1 --chdir=/mnt/nfs/projects/ahriuwu-lanerl-jax bash -lc '.venv-gpu/bin/python -m lanerl_jax.train.benchmark --envs 4096'` | **`desktop` only** — the 5080 is the gate hardware |
| 6 | same, with `--envs 512 2048 --reset-bench` | `desktop` |

Gate-4 rules that are part of the measurement, not preferences:

* **Routing is the default and is the gate.** `--no-route-table` prints
  "NOT gate evidence" in its own header. Do not quote it as a result.
* **The initial state must be warm.** `--warm-s 150` is what produces the live
  44-entity minion-bearing state; the run prints its entity count. A cold
  `init_lane` has no minions, and a 59,153 dec/s figure was once discarded for
  exactly that.
* **60 timed steps after 5 warmups.** Short 20/30-step samples above 56k were
  measured to be unstable and discarded.
* **Repeat it.** Three runs of the identical canonical configuration on
  2026-09-18 gave 55,054 / 55,565 / 55,049 dec/s. Run-to-run spread is about
  1%, which is comparable to the remaining gap, so a single run cannot settle
  the gate in either direction.

## Active fidelity entries

| ID | status | subsystem | server behavior | JAX behavior / deviation | likely symptom | evidence and resolution trigger |
|---|---|---|---|---|---|---|
| PATH-001 | `APPROX` | champion move routing | `NavigationGrid.GetPath` runs radius-aware, closed-on-enqueue A* from the champion's exact float position to the exact float click, then `SmoothPath`. | Production `run_train` loads the radius-aware local table and reconstructs a static-terrain route. `SmoothPath` is ported as of `de5cc2b` and agrees with the host port 400/400; what remains is the **A* tie-break**. Its deterministic reverse BFS reproduces the server's own cell path only 17/400 times. A caller omitting the table still gets `[position, destination]`, explicitly exposed as `--no-route-table`. On the fresh 20-Move fixture generated from actual legal 96x54 non-minimap bin centres, every lookup is READY; the host server-algorithm port agrees with the server 20/20. Local waypoint counts agreed 0/20 before smoothing (server always 3, local 6-9); on the 400-route artifact corpus smoothing takes count agreement from 6.0% to 55.0% and mean polyline length from 1.177x the server's to 1.049x. | A different safe side/curve around terrain changes arrival and trade timing; disabled routing causes wall entry/ejection. | `data/local_route_artifact.py`, `sim/local_pathing.py`, exact host reference in `data/navgrid.py`. The bounded device `CastCircle` agrees with the host on the 20-click corpus (10 clear, 10 blocked) and, checked for the first time at *smoothing* length rather than A*-neighbour length, 4,000/4,000 on segments up to 60 cells with zero bound exhaustion. Integrating `SmoothPath` exposed two float-precision port bugs on the host side; see the SmoothPath section below. Earlier route decomposition independently showed both missing LOS smoothing and BFS/A* path choice. |
| PATH-002 | `APPROX` | policy click geometry / HUD | The wire accepts a world destination, but the intended control surface is the locked follow-camera viewport with minimap clicks forbidden. | The 96x54 bin centres now use the same calibrated perspective equations and side-canonical lane reflection as `lanerl_rl.projection` / the real environment. Bins in the measured 352px minimap rectangle (`x>=275`, `y>=240`) decode semantic Move to NOOP, preventing a live global minimap order. The parity recorder also selects the nearest point from this exact legal grid instead of using an artificial radius clamp. Because x/y heads are factored, the joint rectangle cannot be removed from both marginal distributions; invalid pairs can still be sampled and wasted as NOOP. Other HUD regions are not yet jointly masked. | Correct local geometry, but some sampled actions are wasted; an inaccurate HUD rectangle could suppress valid floor clicks or permit HUD clicks. | `train/actions.py`, `parity/record.py`, their focused tests, and `INFERENCE_FAILURE_ANALYSIS.md` M11/H100. Resolve the remaining approximation with a joint/autoregressive spatial head or a full conditional 2-D action mask calibrated to the deployed client. |
| PATH-003 | `APPROX` | local route endpoint quantization | A* priorities and the first/final swept-circle edges use exact fractional source and goal coordinates. | The local artifact selects raw hops by source/goal cells; runtime preserves the exact terrain-projected float destination but the first branch is cell-centre-derived. Two positions in the same 50-unit cell can therefore choose the same branch when the server would not. | Rare different side around a corner, then large accumulated position divergence. | `data/local_route_artifact.py`, `data/route_artifact.py`; resolve with a server waypoint corpus and endpoint bins or an exact bounded runtime search. |
| PATH-004 | `BOUNDED` | waypoint storage | Server waypoint lists are dynamic and its `SmoothPath` corpus measured a maximum of 19 for 1800-unit clicks. | State stores at most 64 waypoints and reconstruction at most 128 raw hops. The local reverse-BFS router only removes collinear cells: among 110,890 random valid bounded Map1 routes, p99 was 24 and the maxima were 45 waypoints / 101 raw hops. Both overflows remain explicit statuses. | An exceeded bound uses the labelled two-point fallback rather than silently stopping early. | `sim/state.py`, `sim/local_pathing.py`; retain adversarial/random bound tests and raise the cap if either overflow status occurs in rollout diagnostics. |
| PATH-005 | `APPROX` | route coverage/fallback | Server attempts A* for every legal world click and uses its raw two-point fallback only if `GetPath` returns null. | The production Map1/Garen v2 artifact covers 47,477 radius-walkable source cells and +/-50-cell goal offsets in a 231.0 MiB packed-uint4 hop table. An optional v3 sidecar adds 462 MiB of uint8 same-direction runs and jumps only across collinear raw hops, retaining exact existing turns, raw-hop overflow accounting, and statuses; it is not the production default pending a throughput win. Fractional endpoints are anchored to the nearest covered centre within 3 cells while the exact float goal is retained. Table-disabled, source/goal unanchored, goal-outside-window, graph-no-route, raw-hop overflow, and waypoint overflow are distinct statuses; all use a two-point fallback and persist in `LaneState.route_status`. Training reports `route_nonready`. On the exact projected action lattice, the prior +/-44 artifact routed 98,433/115,848 unmasked spawn/lane-reference clicks (85.0%); only 37 exceeded its window, and most other non-ready cases matched Map1 edge/null-path quirks. The maximum anchored endpoint delta was 49 cells, setting the production +/-50 bound. | Terrain behavior can revert to the approximation at coverage/window boundaries; nearest-centre anchoring can pick a different first/final edge from the server. | Production artifact manifest plus `sim/local_pathing.py`; drive `route_nonready` to the server's own null-path cases on representative rollouts and inspect every nonzero class rather than averaging it away. |
| PATH-006 | `APPROX` | Gate-3 scripted drivers | Server-driven parity trajectories path every Move through `GetPath`. | `last_hit_drive`, `hp_band` and now `isolation` all load production `map1_garen_r35_o50_v2` by default; `table_disabled=True` / `--table-disabled` is the explicitly named two-point isolation control. `isolation.py` was the outstanding one and it mattered most: it is the module the gate-3 death gap was handed off to, and it drove the sim on the raw path while the gate itself ran routed, so its measured tail effect described a run that no longer happened. Its 2026-09-16 table (1.7x/1.9x isolation tails) was stale on two counts -- raw path, and measured before sequential collision landed -- and has now been **re-measured with both sides re-run the same day**: tails 1.23x/1.40x, mean nearest-ally +31%, and no-ally-alive-anywhere 11.4% sim against 10.2% server, which rules out wave survival. The medians no longer agree (395.6 vs 305.9, +29%), so that module's headline reading -- "typical position is right, only the tail is wrong" -- does not hold under the mode the gate runs; notably it was the RAW path whose median agreed. A 2,100-decision approach check was raw=1,190 versus routed=1,378 decisions (+6.27 s), so the control cannot be silently substituted for the gate. The local artifact still has the PATH-001/003 approximation, not server-exact A*. | Route choice/timing can still differ from the server; raw control results must not be called canonical, and a raw-path diagnostic cannot explain a routed gate. | Re-run routed `isolation.py` and replace its stale table. Retain raw only for causal isolations. The per-walk `walks` field on `SimRun`/`ServerRun` is what makes the approach cost attributable; use it rather than the `approach_decisions` total. |
| COLL-001 | `VERIFIED` | dynamic unit blocking | Minions/champions collide dynamically through `CollisionHandler`; they are not obstacles in terrain A*. Collision handling is sequential and can re-project an escape from terrain. | Dynamic bodies are handled in `sim/collision.py`, separate from static path selection. Garen E ghosting and frozen per-pass collision cache are represented. A champion-versus-two-minion test pins that clear static terrain still produces creep block with the server's trigger/resolution radii. | If regressed: wrong creep block, phasing, or push direction. | `sim/tests/test_collision.py::test_creep_block_is_dynamic_collision_not_a_static_route_obstacle` plus the collision source audit. Add a longer multi-minion choke/trading-pattern trajectory corpus before calling gameplay parity complete. |
| COLL-002 | `BOUNDED` | collision passes | Server collision loops are data-dependent. | Fixed pass/iteration caps are used for JAX. Exhaustion is intended to remain observable. | Dense packs can retain overlap or diverge by order. | `sim/collision.py`; measure maximum on adversarial creep packs and raise bounds rather than accepting exhaustion. |
| MOVE-001 | `BOUNDED` | waypoint consumption per tick | Server consumes waypoints until movement budget is exhausted. | At most 8 waypoint transitions are consumed per tick. | High speed or near-coincident points can end a tick short. | `sim/movement_jax.py`; `max_steps_used` and parity tests. |
| OBS-001 | `APPROX` | fog / brush visibility | Server visibility includes its complete vision, brush, and reveal rules. | The lane observation uses the simplified implementation in `obs/fog.py`; it is not a complete server vision system. | Target/action availability or enemy memory changes on brush edges. | `obs/fog.py` and observation tests. Resolve with server visibility traces around every top-lane brush boundary. |
| RESET-001 | `APPROX` | target and minion-AI internals | The canonical hash/dump intentionally omit target incumbency and private minion-AI maps. | `LANERL_STATE_DUMP_INTERNALS=1` emits diagnostic-only target NetIds, AI clocks, ignore/help maps, and the lane waypoint cursor; injector recovery is exact when that stream is present and explicitly labelled inference/default otherwise. The stream is outside the canonical hash. | A legacy/canonical-only reset can still retarget on the next tick; a diagnostic trace can isolate that rule. | `parity/trace.py`, `parity/inject.py`, injection notes. Keep canonical-only reports separate from diagnostic-internal one-step reports. |
| RESET-002 | `APPROX` | autoattack phase | The canonical hash/dump omit exact attack cooldown, windup, and attack flags. | The diagnostic stream carries those clocks/flags and the injector restores them; a canonical-only trace retains an explicit unknown/default rather than fabricating phase. Pending effects outside the fixed model remain a separate limitation. | A legacy reset can fire one tick early/late; diagnostic recovery makes the remaining simulator discrepancy measurable. | `parity/inject.py`, `parity/tests/test_inject.py`; do not call canonical-only replay exact. **The windup half of this row is now recovered rather than approximated** (`AA-002`, 2026-09-18): the dumped `aawindup` is rounded to 1/1024 s, but the server's cast clock only ever takes the values `W - k*dt` (`Spell.cs:1643-1647`), so `k` is recoverable exactly and `inject.py::snap_windup_to_tick_grid` re-derives the true remainder. The cooldown half is unchanged and is still the sub-quantum residue described above. |
| RESET-003 | `UNOBSERVABLE` | buffs and casts | Buff callbacks, fractional phase/power, generic cast/channel metadata, and every script-private field are not fully serialized. | Fixed lanes cover implemented Garen buffs/casts; recovery cannot recreate unknown script-private state. | Divergence at expiry, spell completion, or immediately after reset. | `sim/spells.py`, `sim/buffs.py`, hidden-state audit. Extend oracle before expanding champion/content scope. |
| RESET-004 | `APPROX` | missiles and collision cache | The canonical hash/dump omit live missile integration state and the pre-move collision-cache position. | Diagnostic internals restore modelled missile owner/target/position/speed/damage and exact cached collision position; raw float32 position bits avoid injecting a rounded wire coordinate. These fields remain outside the canonical hash. Stable NetIds produced 2,400/2,400 identity-clean pairs. The full quantized run reached 28,526/28,570 (99.85%) componentwise position parity; a 300-pair raw-float pre-clash sample reached 2,160/2,170 (99.54%), with ten remaining perpendicular collision residuals. **Those ten are resolved as of 2026-09-18 and were never a collision bug** (`parity/tier1_same_wave_collision.py`): the current tree is **2,170/2,170**, and reverting *only* `MINION_SPAWN` to its rounded pre-fix value reproduces exactly 10. All ten are freshly spawned minions sitting on the rounded barracks coordinate with **zero colliding neighbours**, five spawn events by two teams, displaced by precisely the two constants' rounding deltas. No collision pass ever touched them. Free-running wave creation now matches the source's red/Chaos-before-blue/Order insertion order; diagnostic injection already reconstructed that order from NetIds, so this source fix correctly leaves the ten residuals unchanged. | A canonical-only/legacy trace can still schedule ranged damage or collision from the wrong state; the last ten collision cases are explained: an already-fixed wrong spawn constant, misattributed to collision ordering. | `parity/trace.py`, `parity/inject.py`, `parity/diagnostic_identity.py`, `sim/tests/test_lane.py::test_map1_top_wave_creates_red_before_blue_for_collision_order`; ~~resolve the same-wave collision residual before closing Gate 1~~ **done 2026-09-18**; keep `tier1_same_wave_collision.py --rounded-spawn` as the regression control, since it reproduces the failure on demand. **The missile blind spot is now measured rather than inferred, and it is 1.1%, not 7.0%.** The 7.0%-with-a-missile against 1.3%-without split is a **confound**, not a mechanism: a tick with a missile in flight is a tick with active combat, which is also a tick with minions swinging. `parity/tier1_residual_drill.py` re-cut the same 1,127 hp misses against the missile aimed at **that unit** rather than against any missile anywhere (job 1005, whole corpus): 684 have an inbound missile that did **not** leave the field that tick, 396 have no inbound missile at all, and only **47** coincide with a missile leaving the field -- of which only **12 (1.1%)** involve a missile the injector could not restore. Across the corpus the injector restores **44,421 of 44,870** missiles from the diagnostic stream and drops **449 (1.00%)**, always because the owner or target NetId does not map to an injected slot. So `RESET-004`'s unobservable floor on hp is **1.1% of the residual**; the other 92.5% is one hit of damage (12.0 x814 melee, 23.0 x228 caster) landing one tick out, i.e. the same swing-completion phase as `aa_hit` -- see `AA-002`. **Update 2026-09-18, after `AA-002`:** with the windup grid recovered, the hp residual falls 1,126 -> **263** and the missile split collapses with it -- 7.0% with a missile in flight -> **1.7%**, and the missile-free rate 1.3% -> **0.0% (0/5,085)**. That is the confound draining away: what looked like a missile blind spot was the melee swing-completion tick, and the true missile-side floor on hp is what is left of the 263. The 263 are now **cleanly isolated to the missile path**: 263 of 263 have a missile in flight and **0 have none**, 259 of them have a missile aimed at *that unit*, and the magnitudes are the caster minion's (23.0 x230). 24 coincide with a missile leaving the field, of which 12 were dropped by the injector. So this row's real floor is measurable at last, on a residual that is no longer contaminated by `AA-002`. |
| PROG-001 | `BOUNDED` | experience | Server exposes level but not fractional XP or level-up scheduling phase. | Injection records the exact recoverable within-level interval `[XP(level), XP(next level))` and uses a deterministic representative, never claiming the fractional value is observed. | A reset can level at a different subsequent tick. | `parity.inject.xp_bounds_for_level`; retain the interval in reports and do not use a cross-level reset as a mechanics verdict. |
| ORDER-001 | `VERIFIED` | move/target interaction | A `Move` packet does not clear `TargetUnit`; `RefreshWaypoints` can resume `AttackTo`. The real wire has no `Stop` action. | Move preserves the target; the internal `STOP` enum is not emitted and behaves as a no-op. | If regressed: the policy gains a disengage action the server lacks. | `sim/orders.py`, `sim/tests/test_orders.py`, `PORT_AUDIT_AI.md`. |
| SPELL-001 | `APPROX` | E damage snapshot AD | Server snapshots the caster's live attack damage. | Production uses profile base plus level growth, but modifiers outside the implemented buff/item slice are absent; table-free unit tests use a level-one placeholder. | Spin damage wrong after future items/buffs are added. | `_ad_placeholder` in `sim/orders.py`. Remove the fallback when all callers provide parameters and extend the live stat pipeline with content scope. |
| SCOPE-001 | `APPROX` | supported game content | The server supports the full map roster, items, runes, neutrals, objectives, all champions, and modern client rules. | The JAX scope is a 1v1 Garen top-lane training slice with lane minions and turrets. Unsupported content is absent, not approximately simulated. | Policies exploit missing pressure or fail when transferred beyond the slice. | `JAX_REWRITE_PLAN.md` scope. Add one ledger row per newly admitted content family before implementation. |
| AA-001 | `VERIFIED` | auto-attack cooldown ordering | `ObjAIBase.Update` calls `UpdateTarget()` (`ObjAIBase.cs:1101`) and only **then** decrements `_autoAttackCurrentCooldown` (`:1103-1105`). A unit therefore cannot swing on the tick its cooldown expires, and a swing tick ends one tick *into* the cooldown it just set. | **Fixed 2026-09-18.** `sim/autoattack.py` now evaluates the swing gate against the cooldown as it stands on entry and decrements last; the module docstring, which asserted the opposite order as established fact and was the reason the code read the way it did, is corrected and now carries the dumped evidence. Before the fix every sim swing started one tick (16.67 ms) early. | The period was never wrong -- both orderings see `period - k*step` at the k-th gate, so the 97-tick drain of a 1.6 s cooldown is identical either way. That is precisely why it survived: it is a **phase** error and every existing test pinned a **rate**. Reconciled against a rollout before the fix: the corpus's 2.19x swing ratio was a per-pair injection artifact, not a rate error (free-running, the swing rate was 1.009x), so this was never the cause of gate 3. | Corpus job 943 against job 882's baseline: minion `aa_fire` misses 2,880 -> **984**, `is_attacking` 3,455 -> 1,559, `aa_cooldown` 14,935 -> 11,725 with p95 1.4932 s -> 0.7832 s; sim fires 5,121 -> 3,225 against a server 2,337. The mismatch *shape* changed with it: the drilled examples went from `aa_cooldown q sim=819` (a whole 0.8 s period) to `sim=802` (0.8 s minus one tick), i.e. the sim's swing tick now ends on the server's own value. The 936 that remain are the `RESET-002` sub-quantum cooldown, matched 39.9% against 40.0% by a scan of all 2,306 server swing starts in the raw log (see the gate-1 row). Regression guard: `sim/tests/test_update_order.py` -- `test_a_unit_cannot_swing_on_the_tick_its_cooldown_expires`, `test_a_swing_tick_ends_one_tick_into_its_own_new_cooldown`, and `test_the_swing_period_is_unchanged_by_the_reordering` as the control that shows why a rate test cannot catch this. |
| CFH-001 | `VERIFIED` (the port) / gate-3 cause is **supply**, not the rule | call-for-help propagation and release | `ObjAIBase.TakeDamage` (`ObjAIBase.cs:1127-1158`) broadcasts to every live ally that is not the victim, whose AI script has `HandlesCallsForHelp` (lane minions alone), and that is inside the **victim's** `AcquisitionRange` of BOTH the victim and the attacker. `LaneMinionAI.OnCallForHelp` (`:108-118`) keeps `Math.Min` of the priorities; `FoundNewTarget(true)` (`:157-215`) consumes the map in the `OnUpdate` trigger and `callsForHelpMayBeCleared` wipes it at the end of that same update, so a call lives exactly one tick. It is the ONLY channel that can re-target a still-valid incumbent. **There is no rate limit**: `CallForHelpVariables.cs`'s `cfh_Delay` / `Stick` / `Radius` / `Duration` / `MeleeRadius` / `RangedRadius` are parsed by `GlobalData.cs:138-144` and read by nothing (`grep -r CallForHelpVariables` over the tree returns only the declaration and the parser), and Map1 ships no `Constants.json` that sets them. | `sim/targeting.py::call_for_help_map` + `sim/minion_ai.py::step_minion_ai` reproduce all of it, and the behaviour is now **audited against the server's own broadcast condition rather than against the source text** (`parity/gate3_arrivals.py::release_audit`). Reconstructing the broadcast from the sim's own trace -- a red ally damaged this decision, the holder inside the victim's range of both victim and attacker, and (separately, because the radii differ: melee 475, caster 700) the attacker inside the holder's own range -- of the **33** champion-holds that ever saw one strictly valid call, **32 released within 3 decisions of the last one**; the single exception released 23 decisions later and sits inside the error of recovering the attacker from `u_target`. Dropping the listener-side radius inflates the opportunity count 3x and manufactures six phantom misses on the fatal clump alone, so that term is load-bearing. The defensive direction works too: of the **147** (listener, decision) pairs where a blue minion was an eligible responder to the champion's own call, **134** were already on an eligible red attacker, **5** switched onto one and **8** did not respond. Acquisition side: the sim takes the champion off a still-valid incumbent **0/51** times, the server **1/45** (`MRT` `cfh=1`); 44/45 server acquisitions are `from=none`. | **None from the rule. The defect is that the calls never arrive.** While the champion is held, a red minion within 475 u of it takes damage on **3.96%** of decisions in the sim against **7.28%** on the server (0.54x); mean blue minions within 475 u 1.13 against 1.81; **zero** blue minions within 475 u on **1,713 / 2,830 (60.5%)** held decisions against **1,289 / 3,530 (36.5%)**. Measured on the attackers instead of the champion: the distance from each red minion within 600 u of the champion to the nearest blue minion is a median **589 u** (sim) against **389 u** (server), **71.8%** beyond the 475 u melee broadcast radius against **44.7%**. A red minion that far from the blue wave cannot hear a call, so nothing recalls it -- hence the 1.26x holds and, through overlap, the concurrency. This is **not** downstream of the death: in the first engagement (decisions 3,631-4,400, t=121-147 s, before either engine has lost a champion) it is already 522 u / 59.4% against 344 u / 24.9%, and the two engines' first four acquisitions land on the *same four decisions* (3,631 / 3,655 / 3,679 / 3,680) and then diverge only on release -- sim holds 164/417/116/634/626/512 against server 178/153/432/128/99/674. | `parity/gate3_arrivals.py` (`--sim runs/g3_sim_swarm.npz --server runs/g3_srv_swarm.npz --server-log runs/g3_srv_swarm_log/instance000.log`). **Do not look for a call-for-help bug.** **Checked 2026-09-18, and the global framing does not hold:** over the whole
episode the two waves are the same. Live blue 8.81 vs 8.47, live red 9.19 vs
8.85; blue front 0.5328 vs 0.5413 (110 u, not 360); wave separation median
**35 u vs 39 u**; and the champion is ahead of its own blue front by a median
**2 u vs 16 u**, ahead on **50.0% vs 51.6%** of decisions. So the sim's blue
wave does **not** systematically lag. The 612-vs-315 figure is conditional on
*being held*, and being far from your own wave is precisely what gets you held
-- so that number cannot separate cause from effect. **Not settled: why the sim's attackers stand further from the blue wave.** While held, the champion is ahead of its own blue front by a median **612 u** in the sim against **315 u** on the server, and ahead at all on **95.7%** of held decisions against **76.2%**; the red front sits at the identical lane fraction (0.5332 both) and it is the blue front that is ~360 u further back. **Measured against the differential 2026-09-18, and the conditional difference does NOT reproduce.** Measured with `parity/tier15.py` (Tier 1.5: one identical state injected into the sim, the server's own deterministic recorded tail as the other side, identical order stream, no re-injection). Fixture `lanerl_jax/runs/tier15_noshop`, 400 s driven, recorded twice and **24,002/24,002 snapshots hash-identical**, so the recorded tail IS the server's free run. Sim side measured at `a0c5916`, i.e. **after** `AA-001`/`TURRET-001`/`ORDER-002` landed, not before. From one identical engaged state the two engines' attacker sets are **identical tick by tick**: mean attackers per tick 5.4743 against 5.4743 over 428 ticks, 2,343 attacker-ticks against 2,343, six holds open on both at the start and still six on both at the end, zero closed on either -- so there is no hold-length difference at all from a shared state. The two geometry numbers this row is about come out the **other way round** from the free-running pair: median attacker distance to the nearest own-team minion is **1,229 u (sim) against 1,347 u (server)** -- the sim's attackers stand *closer* to the blue wave, not 200 u further -- and the fraction of attacker-ticks beyond the 475 u broadcast radius is **90.7% against 93.3%**, not 71.8% against 44.7%. The 589/389 and 71.8/44.7 figures therefore describe two runs that had drifted into different lane states, not a difference in the rule. What keeps this row open rather than closed: the window is 7.1 s (428 ticks), bounded by the server champion's death, from one start state. Widening it needs a start whose champion survives 600 decisions, or a batch over many predicate hits -- `tier15.py --predicate index` in a loop is the cheap next step. |
| COLL-003 | `VERIFIED` (the port) / gate-3 cause **NOT** collision | The trigger and the resolution use **different radii**, verified at four independent source sites: `IsCollidingWith` sums `CollisionRadius` (`GameObject.cs:226-229`), `OnCollision` escapes with `GetCircleEscapePoint(Position, PathfindingRadius + 1, collider.Position, collider.PathfindingRadius)` (`AttackableUnit.cs:312`), `Champion` does **not** override `OnCollision`, and `CollisionHandler.cs:163-165` calls `obj.OnCollision(obj2)` so each object moves **itself**, sequentially. | The sim reproduces all of that. Consequence, measured with the real radii: champion (coll 30, path 35) against a **melee/caster** minion (coll 40, path 35.7) triggers at 70 and escapes to 71.7 -- a 1.8u push against 5.75u of travel per tick, so it walks through. Against a **cannon** (coll 40, path **55.7**) it triggers at 70 and escapes to **91.7** -- a **21.8u push against 5.75u of travel**. Head-on, that is a ratchet: the champion is thrown back faster than it can walk. Content confirms the asymmetry is real, not a loading error: `PathfindingCollisionRadius` 55.7437 for cannon, 35.7437 for melee, 35 for Garen, while `Minion.cs`/`Champion.cs` hard-code the 40/30 trigger radii. | **This is the mechanism of the gate-3 wedge, and it is FAITHFUL.** The sim's walk-1 shows it plainly -- 2,543 decisions at 6.17 u/decision against 11.50 on its clean walks, **686 direction reversals**, 23% of decisions under 1u, in contact 72% of the time at a median 68.1u. But the server would ratchet identically from the same geometry, so this does **not** make the sim wrong. It makes the sim *unlucky*: its champion walks into a wave and the server's does not. | **Negative result: do not look for a collision bug here.** The divergence is upstream. The server's walk-1 has a median nearest minion of **536.7u** and **zero** reversals -- it never met a wave, because it died at 525.6 s where the sim died at 394.3 s and drew a different lane state. The open question is therefore the **394.3 s death**, not the walk. **The differential has now been run, and the collision pass is still not the cause.** Measured with `parity/tier15.py` (Tier 1.5: one identical state injected into the sim, the server's own deterministic recorded tail as the other side, identical order stream, no re-injection). Fixture `lanerl_jax/runs/tier15_noshop`, 400 s driven, recorded twice and **24,002/24,002 snapshots hash-identical**, so the recorded tail IS the server's free run. Sim side measured at `a0c5916`, i.e. **after** `AA-001`/`TURRET-001`/`ORDER-002` landed, not before. Two windows, both truncated by the server champion's death: `champ_near_minion` (start index 7,431, t=123.843 s, nearest red minion 87.3 u, champion moving, 66.1% HP) ran 290 ticks, and `engaged` (start 7,267, t=121.110 s, 3 attackers) ran 428 ticks. Injection floor: 44/38 units, complete diagnostic NetId identity, 0 untrustworthy movement, **t+1 champion position error 0.017 u and matched-minion max 0.000 u** -- an essentially exact start. Per-tick champion displacement, sim against server: 1.606/1.478 and 2.100/1.484 u per tick; total path 466/429 and 899/635 u; **direction reversals 12/0 and 45/3**; fraction of ticks within the 70 u collision trigger 3.45%/0.34% and 38.1%/**0.00%**; median nearest enemy minion 89.7/92.7 and 70.7/96.5 u. So the sim's champion really does end up wedged in the wave where the server's never touches it -- **but not first, and not from the same geometry.** In the `engaged` window the two champions stay within **0.04 u of each other for 68 ticks** while their *minions* have already separated: matched-minion max position error sits at the dump's own 1/16 u quantisation through tick 33, then steps to 5 u at tick 34 and 25 u at tick 38 -- one minion goes one tick out of phase on its stop/go decision and then walks away linearly at one tick of travel per tick. The champion's own split is a single tick of stop latency at tick 68 (server `move_order` is HOLD at 68, the sim is still MOVE_TO and takes one more 5.75 u step). Only *after* that does the sim's champion sit at a median 70.7 u -- exactly the trigger radius -- and start ratcheting. **Verdict: the collision pass is not refuted and is not the cause; the compounding channel is a one-tick stop/go phase error, and it SURVIVES `a0c5916`.** That commit fixed the three `ObjAIBase.Update` orderings and cut the one-step move-order misses 34,295 -> 4,807 and `aa_fire` 2,880 -> 984, and this differential ran on the fixed sim and still finds a minion a whole tick out of phase at tick 34 of 428. So the residual 4,807/984/1,559 (`is_attacking`) one-step misses are not cosmetic: they are the seed of the compounding. **That was done on 2026-09-18 and the answer is `HOLD-001`.** The tick-34 minion is **NetId 1073744061**. Every controller field agreed -- move order ATTACK_TO on both, target 1073743317 on both, `is_attacking` true on both, auto-attack cooldown and windup equal to four decimals -- and the ONLY fields that disagreed were `pos.x`/`pos.y`. What differed was `Waypoints.Count`: server **1**, sim **2**. The server had been stopped by `RefreshWaypoints`' in-range `UpdateMoveOrder(OrderType.Hold)` -> `StopMovement()` -> `ResetWaypoints()`, and it stayed at (3831.250, 13191.562) for the rest of the window; the sim, which wrote the order but not the stop, kept stepping 5.427 u/tick and was 48.8 u away eight ticks later. Across the whole differential the sim's minion `n_waypoints` was 2 on 4,979 of 4,980 disagreeing unit-ticks and **never 1**, while the server's was 1 on 2,068 of them: the sim could not represent "stopped" at all. **And the contact statistic did collapse.** Re-running the identical `engaged` window after `HOLD-001`+`INJ-002` (job 1000 against job 974): ticks inside the 70 u trigger **38.08% -> 1.10%** (server 0.22%), median nearest enemy minion **70.73 -> 98.11 u** (server 96.49), direction reversals **45 -> 13** (server 3), champion path 898.6 -> 719.3 u, max champion position error **91.09 -> 47.77 u**, max champion HP error 58.61 -> 36.38, champion HP lost 610.5 -> **584.7** against the server's 587.4. The truncation reason flipped: the window used to end because the **sim's** champion died first (deaths 2 against 1 at tick 429) and now ends 26 ticks later because the **server's** does (tick 455, sim still alive on 1 death). Next: re-run the gate-3 last-hit chain itself, which has not been re-measured since. |

| INJ-001 | `FIXED` 2026-09-18 | Tier-1 injector: champion spawn point | `Fountain.Update` heals a champion **inside its own fountain** 15% of max HP on a 1 s pulse; `step.tick`'s block `0b` reproduces it and decides "inside" by `(x - spawn_x)^2 + (y - spawn_y)^2 <= 1000^2`. | `inject.inject_snapshot` set a champion's `spawn_x/spawn_y` to its **current position**, with a comment asserting the only consumer was respawn. It is also the fountain test, so every injected champion was standing in its own fountain. | **Invisible in Tier 1 and decisive in any free run.** One 16.667 ms tick can never reach the 1,000 ms pulse period, so the whole 19,800-pair corpus is unaffected and its champion-field numbers stand. In a Tier-1.5 free run the injected champion was healed ~100 HP per second: under six attacking minions it **lost 11.6 HP over 7.6 s where the server's lost 587.4**, and champion HP error reached 411 u of the 583 total divergence. Before the fix the gate-3 window read as "the sim's champion is unkillable"; after it, the same window reads sim 610.5 HP lost against server 553.7 (+10%), and max champion HP error over 428 ticks falls from 584 to **58.6**. | Fixed: `spawn_x/spawn_y` now take `sim.init.CHAMPION_SPAWN[team]`. Regression test `parity/tests/test_inject.py::test_an_injected_champion_in_lane_is_not_standing_in_its_own_fountain` asserts an injected lane champion is outside `_FOUNTAIN_RADIUS` of its recorded spawn. Anything measured with a free-running injected champion before 2026-09-18 carries this and must be re-run. |
| STAT-001 | `VERIFIED` | champion max HP, and its per-level slope | **Settled, from source and confirmed on 13 dumped data points.** The extra health is not runes -- the page in `lanerl/cfg/garen1v1.json` lists 9x`5245` (AD), 9x`5317` (armour), 9x`5289` (MR), 3x`5335` (AD) and **no rune carries health at all**. It is two *masteries*, and they enter `Stat.Total = ((BaseValue+BaseBonus)*(1+PercentBaseBonus) + FlatBonus)*(1+PercentBonus)` (`Stat.cs:68`) at different places: `Veteran's Scars` (talent `4222`, rank 3) is `HealthPoints.FlatBonus = 12*rank = 36`, and `Juggernaut` (talent `4232`) is `HealthPoints.PercentBonus = 0.03` (`Content/LeagueSandbox-Scripts/Talents/Defense/`; of the config's 16 talents only 4 have scripts, the rest resolve to `EmptyTalentScript`). So max HP is `(616.28 + 96*growth_sum(L) + 36) * 1.03`, and because `Stats.LevelUp` (`Stats.cs:267-268`) adds `GetLevelUpStatValue(HealthPerLevel)` to `HealthPoints.**BaseValue**`, the 3% multiplies **every per-level increment too** -- which is the whole slope difference. Predicted against the dump at all eight levels: 671/743/817/895/977/1062/1150/1242 (the wire truncates `HealthPoints.Total` to an integer), matching `runs/g3_server.npz` (`clevel`/`cmhp`, 600 s) and `runs/tier15_noshop/drive_obs.jsonl` exactly. | **FIXED** (`sim/profiles.py`, `sim/init.py`). `RUNE_HP_BONUS = 754.248046875 - 616.28` is gone, replaced by `MASTERY_HP_FLAT_BONUS = 36.0` and `MASTERY_HP_PERCENT_BONUS = 0.03`; the percentage is applied to the `max_hp` column AND folded into `hp_per_level` (`step.py` grows max HP as `hp_per_level * d(growth_sum)`, so that column is the only place the factor can live without editing `step.py`). The sim now reproduces the ladder above at every level. **The old constant was wrong twice over**: it carried the auto-bought Doran's Shield (754.248046875 is exactly `(616.28+36+80)*1.03`, an item the sim does not model and every parity instrument now turns off), and being a single additive delta it could not carry the slope. `DORANS_SHIELD_HP_REGEN`'s +1.2 HP/s was removed from `hp_regen` for the same reason and the same item; the constants survive, documented and unapplied, in `init.DORANS_SHIELD_HP`. | The pre-fix sim was **tougher** than the server by 82.4 HP at level 1 falling to 68.4 at level 7 (12.3% -> 5.9%), so every survivability comparison in the gate-3 chain was biased in the sim's favour and its extra death was *more* anomalous than it looked, not less. Any HP-denominated gate-3 number measured before this commit (the `hp_band` in-band counts, the 1,869-vs-2,661 regeneration residual, the death at 394.3 s) is measured against a champion with the wrong pool and should be re-run before it is re-argued. | **One residual, stated rather than hidden**: `Stats.LevelUp` raises CURRENT health by the *unscaled* `statsLevelUp.HealthPoints.BaseValue` (`Stats.cs:279`) while max health rises by that times 1.03, so the server's champion lands ~2.07 HP below its new maximum on each level-up. `step.py` adds one `hp_growth` to both, so the sim now over-credits current HP by that much per level (12.4 HP by level 7, ~1% of the pool, refilled by regen in a couple of seconds). The exact fix is a second column read only by the current-HP term, which is a `step.py` edit. Pinned by `sim/tests/test_lane.py::test_champion_max_hp_matches_the_servers_dumped_ladder`. **Two parity tests now fail and are NOT this session's to edit** (`lanerl_jax/parity/**` is owned elsewhere): `parity/tests/test_patch.py::test_champion_spawn_hp_exceeds_the_content_base_curve` (asserts the 137.72 gap; the item-free gap is 55.568 and the observed spawn HP is 671, not 754) and `::test_the_shield_gives_regen_but_its_hp_is_already_in_the_rune_delta` (asserts `max_hp == 754.248046875` and `hp_regen == 1.568 + 1.2`; both are now 671.848 and 1.568, and its whole premise -- that our 754 already contained the item -- is correct but is exactly the thing that was wrong). |
| ITEM-001 | `APPROX` | items / the server's auto-shop | `LanerlHooks.AutoBuyUndriven` buys `LanerlConfig.BuildPath` = `1054, 2003, 2003, 1001, 3047, 3134, 3068, 3035` out of ambient gold, on by default (`LANERL_AUTOBUY != 0`). Purchases are **fountain-gated** (`BuyRequiresFountain`, radius 1800) and `StartingGold` is 475, so Doran's Shield (440) plus a potion land on the **boot tick, before the first dumped frame exists**, and nothing further lands until a respawn walk. In a 400 s fixture that is Doran's + 2 potions + Boots at t=81.1 s and **nothing after**, ending at 302 gold -- not the full Steelcaps/Dirk/Sunfire build the path implies. | The sim has **no item model at all**. Measured server-side bias with the shop on: **+82.4 max HP** from t=0 (`80 x 1.03`, +12.3% at level 1, +7.2% at level 7), **+1.2 HP/s regen** on a base of 1.568 (**+77%**, up to +720 HP of healing over 600 s), and **+25 move speed** from t=81.1 s (345 -> 370, **+7.25%**; measured travel follows exactly, 6.17 vs 5.750 u/tick). | Comparing against a server champion carrying items is not a parity measurement -- it is two different champions. **`run_oracle_on_server` now defaults `autobuy=False`**, inverting the server's own default on purpose; every existing caller already passed `False`, and the old `True` default was a trap waiting for the next one. It is also how `RUNE_HP_BONUS` came to be measured off a shop-ON dump and to carry Doran's +80 silently for weeks (`STAT-001`). | `parity/last_hit_drive.py`; `tier15 record --no-autobuy` produces the clean fixture and `tier15` raises `MOVE_SPEED_MISMATCH` if the two sides disagree. **Scope statement, and it matters**: production RL runs DO use the auto-shop, so gate 3 validates the sim against a server configured the way the sim can *represent*, not against the production configuration. Closing that gap needs an item model, not a flag. |
| STAT-002 | `FIXED` | champion attack damage, per-level slope | `Brute Force` (talent `4122`, rank 3 in `lanerl/cfg/garen1v1.json`) sets `AttackDamagePerLevel.**FlatBonus** = 0.55` (`Talents/Offense/Brute Force.cs`, `statPerRank = {0.22, 0.39, 0.55}`), and `Stats.LevelUp` grows `AttackDamage.FlatBonus` through the same curve as `.BaseValue` (`Stats.cs:270-271`). So the server's Garen gains `(3.5 + 0.55) * (0.65 + 0.035*L)` AD per level, not `3.5 * (...)`. `PORT_AUDIT_COMBAT` already noted `AttackDamagePerLevel` is a full `Stat` and concluded "only `.BaseValue` is nonzero ... for Garen with no such item in this project's scope" -- true of items, false of masteries, which is how it was missed. | `profiles.py`'s `ad_per_level` column is Content's `DamagePerLevel = 3.5` alone. | **Measured, exact, and not yet fixed.** `runs/tier15_noshop/drive_obs.jsonl`'s dumped `ad` field (`Stats.AttackDamage.Total`) reads 78.14 / 81.05 / 84.11 / 87.31 at levels 1-4. `3.5` predicts 80.65 / 82.90 / 85.97; `4.05` predicts 81.05 / 84.11 / 87.31 -- all three to 0.01. The gap is +0.55*growth_sum(L): +0.40 AD at level 2 rising to +2.66 at level 7 (3.4%). Level-1 AD is unaffected and already exact (78.134765625 = 57.88 + 9x0.945 + 3x2.25 runes + 5.0 from `Martial Mastery`). | Gate 3's last-hit oracle reads its "would this attack kill" threshold straight off champion AD (`parity/hp_band.py`), so a 3.4% AD deficit at level 7 moves how often a minion falls in the one-shot band -- the same mechanism as the original flat-AD bug, one order of magnitude smaller. It is also a candidate term in the outstanding per-hit damage residual. | **Fixed 2026-09-18**, alone, after the two concurrent corpus agents finished. `sim/init.MASTERY_AD_PER_LEVEL_BONUS = 0.55`, folded into the champion row's `ad_per_level` column in `profiles.py` -- **not** into `attack_damage`, which would add 0.55 at level 1 where the server adds nothing. Re-verified from source before landing rather than from the ledger: `Talents/Offense/Brute Force.cs` writes `StatsModifier.AttackDamagePerLevel.FlatBonus = statPerRank[rank-1]`, and `Stats.cs:270-271` grows `AttackDamage.BaseValue` and `AttackDamage.FlatBonus` from that `Stat`'s two terms through the same `GetLevelUpStatValue`. Pinned against the server's own dumped ladder at **all seven levels** the fixture reaches (`runs/tier15_noshop/drive_obs.jsonl`, champion `ad`): 78.14 / 81.05 / 84.11 / 87.31 / 90.65 / 94.13 / 97.76. Slope 4.05 reproduces every one to **<=0.005** (the dump's 2-decimal rounding); slope 3.5 is short by 0.395 at level 2 rising to **2.668 at level 7**. `sim/tests/test_lane.py::test_champion_ad_matches_the_servers_dumped_ladder` asserts the ladder *and* asserts that Content's 3.5 alone reproduces **only** level 1, so a regression names itself. `sim/tests/test_champion_level_scaling.py` had to move with it and is the cautionary note: it passed throughout, because it rebuilt its expectation from the same Content value the sim was using -- **a test that derives the expected value from the code under test cannot see a shared wrong constant.** The dumped ladder is what actually pins this. Still outstanding downstream: gate 3 and Tier 1.5 were measured at the old slope and must be re-run and re-attributed; champion AD moves minion kill timing, so treat every pre-2026-09-18 gate-3 CS number as measured on a 3.4%-light champion. |
| STAT-003 | `APPROX` | champion magic resist | The page's 9x `5289` Greater Glyph of Magic Resist give `FlatSpellBlockMod` 1.34 each = **+12.06**, applied as an item (`Champion.OnAdded` -> `AddStatModifier(runeItem)`, `ItemData.cs`), so it lands in `MagicResist.FlatBonus`. Garen's Content `SpellBlock` is 32.1, and the dump reads 44.16 at level 1 and 45.06 at level 2. At level 3 W is ranked and `GarenWPassive` adds `MagicResist.PercentBonus += 0.2` with `PercentBaseBonus -= 0.2`, giving `(32.1 + 1.25*growth_sum(L)) * 0.8 * 1.2 + 12.06 * 1.2` = 47.058 at level 3 against a dumped 47.06. | `profiles.py` sets `magic_resist` from Content alone (32.1) and `step.py` passes `flat_bonus=0.0` for MR -- its `armor_flat_bonus` column has no MR twin. The sim's champion therefore runs 12.06 MR light before W, and 14.5 light after it. | **Low impact in this slice, which is why it survived**: the matchup is an all-physical Garen mirror and the only magic damage in the port is Garen's own R, which targets champions only. It is not zero -- it is the resist R's mitigation reads -- and it becomes real the moment any magic source is admitted. | Needs an `mr_flat_bonus` column mirroring `armor_flat_bonus`, written in `sim/profiles.py` and read in `sim/step.py` exactly as the armour term already is. Adding the 12.06 to the `magic_resist` base instead would be exact at levels 1-2 and 2.9 MR short from level 3, because W's `PercentBaseBonus` would then wrongly scale it -- so it is recorded here rather than half-fixed. Supersedes `PORT_AUDIT_COMBAT`'s item 11, which flagged the per-level half of this and missed the flat half. |
| TURRET-001 | `VERIFIED` | turret acquisition vs retention radius | Acquisition goes through the quadtree's circle-vs-circle test, `dist2 < (Range + CollisionRadius)2` (`QuadTree.cs:61-64` via `CollisionHandler.cs:69-72`/`:119-128` and `ApiFunctionManager.cs:601-604`, from `TurretAI.cs:41`) -- **790** for a 40-radius minion, and the inequality is strict. Retention is a flat centre-to-centre **750** (`TurretAI.cs:29-32`), and selection is **priority only, distance never** (`TurretAI.cs:43-62`). | **Fixed 2026-09-18, and it needed both halves.** `sim/targeting.py::turret_acquire` now takes a required `collision_radius` and builds its candidate mask from `Range + max(0.5, candidate CollisionRadius)`; `sim/step.py` now scores the retention test against the target the turret holds **after** `CheckForTargets`, not the one it entered the tick with. Widening acquisition alone makes turret targeting strictly *worse* -- the sim would simply keep the annulus minion the server threw away -- so neither half is a partial fix. | The server's trap: a best-priority minion in the 40-unit annulus is picked by `CheckForTargets` and dropped by the retention test four lines later **in the same `OnUpdate`**, every tick. Measured: a caster at 764 outranks a melee at 196, and one blue turret sat idle at cooldown 0 for **664 consecutive ticks (11 s)**. Free-running, the pre-fix sim took ~21 extra blue-turret swings per 330 s at a median 168 damage; note the sign, which works **against** gate 3's signature rather than explaining it. | Corpus job 943: turret target misses 1,508 -> **30** (-98%), `aa_fire` 1,483 -> **43**, `is_attacking` 1,484 -> **44**, `has_auto_attacked` 1,290 -> **40**, `aa_cooldown` 1,743 -> **259**. Sim turret swings 1,527 -> **87** against a server 44. The priority table and stickiness were already right and were not touched. Regression guard: `sim/tests/test_update_order.py::test_turret_acquisition_reaches_further_than_its_attack_range` (700/770/789.9 in, 790 out -- the strict `<` -- and the radius is the candidate's own, not a constant) and `::test_a_minion_in_the_annulus_is_acquired_and_dropped_on_the_same_tick`, which is specifically the guard against shipping only the first half. `parity/turret_target_drill.py` still reproduces both phases. |
| HOLD-001 | `VERIFIED` | `RefreshWaypoints`' in-range stop | `RefreshWaypoints` ends its `AttackTo` branch with `UpdateMoveOrder(OrderType.Hold, true)` (`ObjAIBase.cs:651-655`), and `UpdateMoveOrder` (`:1362-1366`) calls `StopMovement()` for `Hold`/`Taunt`. `AttackableUnit.StopMovement` (`AttackableUnit.cs:1040-1052`) is `ResetWaypoints()` (`:996-1002`): `Waypoints = new List<Vector2> { Position }; CurrentWaypointKey = 1`. The route is **destroyed**, not suspended. Nothing rebuilds it while the swing runs: `UpdateTarget` early-returns on `IsAttacking` (`ORDER-002`), and `LaneMinionAI.ReevaluateBehavior` (`LaneMinionAI.cs:321-331`) returns `AttackTo` for a still-valid target **without** calling `SetWaypoints`. Census over the whole corpus dump: LaneMinion rows with order `Hold` are `Waypoints.Count == 1` on **110,285 of 110,285** -- the server never holds a live route -- and rows with order `AttackTo` and `Count == 1` (stopped mid-swing) number **33,856**, 8.6% of all minion rows. | **Fixed 2026-09-18.** `sim/step.py`'s block 3b wrote `move_order = HOLD` and left `waypoints`/`waypoint_key`/`n_waypoints` alone; it now performs `ResetWaypoints` as well. The omission was invisible for as long as the order stayed `Hold`, because `_can_move` blocks `Hold` anyway -- and stopped being invisible the moment the 250 ms sweep wrote `AttackTo` back, at which point the sim resumed walking down the stale two-point chase path for the whole windup and cooldown. | **This is the compounding channel gate 3 was looking for.** In `parity/tier15.py`'s `engaged` window the sim's minion `n_waypoints` was 2 on 4,979 of 4,980 disagreeing unit-ticks and **never 1**, against the server's 1 on 2,068 of them; the single largest shape was `(sim Hold, 2 waypoints)` against `(server Hold, 1 waypoint)`, 1,288 cases. In one step, position is unaffected -- movement (block 2b) runs before 3b and the corpus re-injects waypoints every tick -- which is exactly why it survived a whole-corpus position gate. | Corpus job 981 -> 999 on the identical log: minion **waypoints 800 -> 333** (-58%, below the 716 that preceded `ORDER-002`, so that regression is repaid), move order 4,807 -> 4,791, nothing worse. Tier 1.5 job 974 -> 1000 on the identical fixture: the tick-34 minion 1073744061 stops diverging entirely (first departure from the quantisation floor moves from tick 34 at 5.427 u to tick 78 at 0.140 u), ticks inside the 70 u collision trigger **38.08% -> 1.10%**, median nearest enemy minion 70.73 -> 98.11 u, max champion position error 91.09 -> 47.77 u. Regression guards: `sim/tests/test_update_order.py::test_hold_destroys_the_route_and_does_not_merely_block_it` (fails 2 == 1 on the pre-fix tree), `::test_a_unit_stopped_by_hold_stays_stopped_when_the_order_flips_back` (the consequence) and `::test_the_out_of_range_repath_still_produces_a_two_point_route` (the control against over-fixing). **Deliberately NOT changed:** `_can_move` still blocks `Hold`, which the server's `ObjAIBase.Move` (`:451-463`) does not. That is a safe super-set only because every server `UpdateMoveOrder(Hold)` carries `StopMovement()`, and the corpus census above confirms it empirically (0 of 110,285 Hold rows carry a live route). Removing it would require the reset at the other three `HOLD` writers in `step.py` (recall, channel cancel, R cast) too. |
| INJ-002 | `FIXED` 2026-09-18 | Tier-1 injector: `CurrentWaypointKey` | `CurrentWaypointKey == Waypoints.Count` is a legal, common and load-bearing server state -- it is precisely `IsPathEnded()` (`AttackableUnit.cs:1008-1011`), and `ResetWaypoints` (`:996-1002`) produces it deliberately on every `StopMovement()`. | `inject.inject_snapshot` clamped the injected key to `min(internal.waypoint_key, max(0, width - 1))`. For the stopped unit that is `min(1, 0) = 0`, which turns "path ended" into "one waypoint left, at the position I am already standing on". `movement_jax.step_move_units`' `k < n` gate then made the unit an active mover. | **This was a harness artifact reported as a simulator deviation, and it was the whole of the largest gate-1 residual.** Block 0's collision escape moves an overlapping minion up to 7.5 u (minion `PathfindingRadius` 35.7437: the resolution radii sum to 72.49 against an 80 u trigger, so an overlapping pair is pulled *toward* contact); block 2b's movement then walked the unit straight back to that phantom waypoint, and a minion's per-tick budget (325 u/s -> 5.417 u) is just larger than the escape, so it landed **exactly** on the pre-collision position and the escape read as zero. Every one of the 1,742 misses had a colliding neighbour and 1,541 of the 1,550 largest shape read `dump=1 injected=1 key=0`. Probed directly (`sim.collision.resolve_collisions` called on the injected state, with and without the production terrain and mover list): the sim's collision pass lands within **0.017 u median** of the server's own post-tick position, so neither the escape formula, the frozen `GetNearestObjects` membership (overlapping-live equalled in-frozen-list on 175 of 175 drilled cases) nor the terrain re-projection was ever at fault. | Fixed: the cap is `width`, not `width - 1`; `step_move_units` already clamps the gather index, so a key equal to the count is safe. Corpus job 981 -> 999 on the identical log: **position L-inf <=1/16 1,742 -> 15**, Euclidean <=1/16 1,843 -> 39, bit-exact p95 error 5.2250 -> 0.0625 u. By crowding bucket (0/1/2/3+ colliding neighbours) parity goes 100%/99.628%/99.329%/98.650% -> **100.000%/99.994%/99.987%/99.992%**, i.e. the crowding gradient that `COLL-003`'s docstring records is gone with it. Three drilled cases went 5.398/5.380/5.410 u -> 0.007/0.037/0.038 u, inside the dump's own 1/16 u. **Read together with `INJ-001`** (the injected champion healing in its own fountain): that is two injector artifacts found in one day, both of which had been carried in this ledger as simulator deviations. Any position or collision conclusion drawn from a Tier-1 run before 2026-09-18 must be re-derived, and a new residual should be probed against the harness before it is attributed to `sim/`. |
| ORDER-003 | `BOUNDED` (a design floor, **not** a defect -- do not fix) | serial vs fixed-phase evaluation | `ObjectManager.Update` (`ObjectManager.cs:74-81`) is a **serial** `foreach (var obj in _objects.Values) obj.Update(diff);`. `ObjAIBase.Update` runs `AIScript.OnUpdate` first, and damage resolved during another unit's turn in that same loop (`TakeDamage`, `ObjAIBase.cs:1127-1158`) can kill a unit and fire `OnCallForHelp` **synchronously, mid-tick**. Whether a given minion sees this tick's own kill or call therefore depends on whether the killer happens to fall earlier or later in that tick's iteration order. | `step.py` runs the minion controller at a **fixed phase** (block 2, `:528`) on tick-start state, strictly before damage (block 5) and call-for-help (block 5b). `TargetJustDied()`'s `cur_ok` and the CFH mask therefore always see *last* tick's outcome. The sim is structurally always "updated first" and can never react same-tick. | **A one-sided target residual, and the prediction is confirmed with a clean zero.** Of the 736 minion target misses (`parity/tier1_residual_drill.py`, job 1005, whole corpus), `server ACQUIRED from none, sim did not` is **253** and the mirror `sim ACQUIRED from none, server did not` is **0**. **Re-measure before quoting those: `INJ-003` (2026-09-18) showed the injected 250 ms sweep was firing on 0 of 395,366 minion tick-pairs, so the sim in that run could only re-evaluate on `TargetJustDied()`/call-for-help. Part of the one-sidedness this row reads as a design floor was that. With the sweep restored the row is 736 -> 522 on the identical log; the argument that the *remainder* is a floor is unchanged, but its size is not what it was.** Adding the directional buckets `server SWITCHED off the incumbent, sim held` (261) and `server DROPPED an incumbent the sim kept` (130) gives **644 of 736 (87.5%) in which the server moved and the sim did not**, against no case at all of the sim moving first. The trigger split says the same thing: the 250 ms timer alone predicts only **53.4%** of the 253 acquisitions, 9.2% of the 261 switches and 7.6% of the 145 drops -- the rest happen on ticks only the mid-tick event path (`TargetJustDied()` / `FoundNewTarget(true)`) can explain. Independently, the sim's own timer is never *early*: predicted-sweep-but-server-did-not is **0 of 395,366** minion-ticks. | Closing this would mean giving up the fixed-phase vectorised step, which is the whole basis of gate 4. It is therefore a **floor** on gate 1 in the same sense `RESET-002` is for the auto-attack clock, and it should be quoted as one rather than re-opened as a bug. Ruled out by source reading and not to be re-investigated: there is no acquire/retain annulus for minions (`LaneMinionAI.IsValidTarget`/`UnitInRange` narrow-phase both incumbent and candidates, unlike `TurretAI.CheckForTargets`); `UnitIsProtectionActive` is a no-op for minion targets; fog cannot false-negative here (minion vision 1100 > acquisition 700). **Still genuinely unported, and pointing the other way:** the generic `ObjAIBase.UpdateTarget` null-out pass runs on every `ObjAIBase` including lane minions, while `step.py:600-660` applies it to champions and turrets only. For a minion it can only ever *null* a held target, so its signature is "sim holds something stale, server holds nothing" -- which is the 130-case bucket, not the 253. **MEASURED 2026-09-22, and the floor is now a number rather than a claim.** Joining each target disagreement to the server's own `MinionReevaluate` branch stream -- which names which of the three arms opened the minion's gate -- over 161,763 LaneMinion unit-ticks: `Sweep250` fires 10,699 times (6.61% of ticks, against a theoretical 16.67/250 = 6.67%, which independently validates the join) and produces **1** disagreement. `TargetJustDied` and `CallForHelp` fire on 0.08% and 0.09% of ticks and produce **114 and 80** -- enrichment **x694** and **x434**. So 194 of 216 rows (89.8%) sit on exactly the two arms that depend on state arising WITHIN the tick (a target that died this tick; an ally damaged this tick), which a fixed-phase evaluation cannot see and a serial `foreach` can. The other 21 were NOT this floor and are now fixed -- see `TGT-NULLOUT`. **Consequence: the port's targeting logic is exact and the residual is the phase structure.** Two earlier readings of this residual were wrong for the same reason: the drill inferred the sweep from a reconstructed 250 ms timer, which is blind to both event-driven arms by construction, so '0% on a timer-predicted sweep' never meant the gate had stayed shut. |
| AA-002 | `FIXED` 2026-09-18 (it was the **harness**, not the simulator) | swing-completion (`HasAutoAttacked`) phase | Two source facts, and the second is the one that mattered. (1) `Spell.FinishCasting` (`Spell.cs:985-1006`) sets `CastInfo.Owner.HasAutoAttacked = true` **unconditionally for any `IsAutoAttack` cast**, and only then branches on `!IsMelee` to choose missile creation over immediate `ApplyEffects` + `AutoAttackHit`; `AutoAttackHit` never touches the flag, so the transition is windup completion for melee and ranged alike. (2) **The cast clock is an integer count of ticks, not a float.** `Spell.Update`'s `STATE_CASTING` branch takes the `IsAutoAttack` path (`Spell.cs:1643-1647`), which accumulates **upwards** -- `CurrentDelayTime += diff / 1000.0f; if (CurrentDelayTime >= CastInfo.DesignerCastTime / CastInfo.AttackSpeedModifier) FinishCasting();` -- from a `CurrentDelayTime` that `AutoAttackSpell.ResetSpellCast()` zeroed at the swing's start (`ObjAIBase.cs:1250`). `diff` is the fixed tick, so the elapsed cast time is always exactly `k*dt` and the remaining windup always lies on the grid `W - k*dt` for that unit's fixed threshold `W`. A swing therefore takes `ceil(W/dt)` ticks, a per-profile integer constant. | `sim/autoattack.py` carries the **remaining** windup, decrements it and fires on `windup - dt <= 0`, behind the gate `is_attacking & (aa_windup > 0)`. Free-running that is the same grid and is correct. **Injected it was not**: `parity/inject.py` wrote the diagnostic stream's `aawindup` straight through as `q_aa_windup / 1024`, and `LanerlStateDump` rounds that remainder to 1/1024 s (`Q(..., StatQ)`, half-away-from-zero). Losing the grid loses the hit tick whenever a profile's `W/dt` sits within 1/2048 s of an integer. | **The 18x melee/ranged asymmetry is a property of `W/dt`, and only two profiles have it.** Blue/Order **melee** lane minion: `W = 0.333360 s`, `W/dt = 20.0016`, so its final windup tick has **2.67e-5 s** left -- 18x *below* the dump's own 1/2048 rounding threshold, published as a flat `aawindup=0`. Red/Chaos **cannon**: `W = 0.550000 s`, `W/dt = 33.0000`, same shape. Every other minion profile lands mid-tick (red melee 20.7202, both casters 27.6496, blue cannon 20.7000) and was never affected -- which is the entire 18x gap, not a melee/ranged fact at all. Each affected swing cost **two** mismatched unit-ticks in **opposite** directions, which is why the sign split looked like clean zero-mean jitter: at `k = n-2` the rounded 17/1024 s = 0.0166016 s reads as just **under** `dt` and the sim fired a hit the server did not (12.0 damage applied a tick early), and at `k = n-1` the rounded 0 read as "no windup left", so `winding` was false, the hit was dropped, `skipped_ready` fired and `is_attacking` was cleared as well. Scanning job 882's log directly: **482** such swings on blue melee and **33** on red cannon = 515, against a measured `aa_hit` residual of 1,041 = 2x515 to within the corpus's own subsampling. | **Fixed in the injector** (`parity/inject.py::snap_windup_to_tick_grid`), not the sim: `k = round((W - dumped)/dt)` is unambiguous because the rounding error is 17x smaller than the tick stride, so the true remainder `W - k*dt` is recoverable exactly. The recovery is **rejected** (dump value kept, reason recorded in `InjectionNote.attack_recovery`) when `W - k*dt` sits further from the dumped value than the dump could have rounded, so a wrong `attack_windup` constant cannot hide behind it. Identical corpus, job **1018 -> 1039** (`--existing-log`, job 882's log, 19,800 pairs): `aa_hit` **1,041 -> 7**, hp **1,126 -> 263**, `has_auto_attacked` **1,968 -> 935**, `is_attacking` **1,534 -> 1,015**, `LaneTurret.hp` **33 -> 7**; HP-change agreement sim-only **651 -> 228** and server-only **439 -> 15**; minion death disagreement **11/33 -> 1/21**. `aa_fire` 976 -> 977 (+1, the only field that moved the wrong way) and `aa_cooldown` 11,700 -> 11,700, which relocates that residual: its >40-quantum tail belongs to `aa_fire`, not to `aa_hit`. Guards: `parity/tests/test_inject.py::test_the_dumped_windup_is_snapped_back_onto_the_server_cast_grid` walks **every profile's whole cast clock** and asserts the injected value reproduces the server's own `(k+1)*dt >= W` decision after the float32 round-trip the sim actually performs, and `::test_a_windup_the_profile_table_cannot_explain_is_left_alone` pins the rejection. **The asymmetry the row was opened on is gone** (`parity/tier1_residual_drill.py`, whole corpus, job **1005 -> 1046**): melee **972 mismatched unit-ticks against 906 server swing completions (1.073 per completion) -> 4 (0.004)**, ranged **69 against 1,144 (0.060) -> 3 (0.003)**. The two rates are now indistinguishable, which is what `FinishCasting`'s unconditional flag predicted all along; the 18x gap was the dump's quantiser, not the damage path. The 7 survivors are a different mechanism -- all `sim_hit=1 server_hit=0` with `fire_agrees=1` and `target_agrees=0` on 6 of 7, i.e. `ORDER-003`'s target residual, not phase. |
| ORDER-002 | `VERIFIED` | `RefreshWaypoints` during a swing | `ObjAIBase.UpdateTarget` hits an unconditional early `return` on the `IsAttacking` a unit carried into the tick (`ObjAIBase.cs:1192-1205`), so neither the in-range `UpdateMoveOrder(OrderType.Hold)` (`:651-655`) nor the out-of-range re-path (`:657-669`) runs while a swing is in flight. The order a lane minion is left with is the one `LaneMinionAI`'s 250 ms timer wrote earlier in the same tick: `AttackTo`. `BaseTurret.RefreshWaypoints` (`BaseTurret.cs:108-110`) is an empty override, so a turret's order is never touched by this path at all. | **Fixed 2026-09-18.** `sim/step.py` wrote `hold = has_tgt & in_rng` every tick, gating neither the windup nor the turret override; it now gates both. Verified tick-by-tick on minion 1073743551 before the fix: the dumped order flips to `AttackTo` on exactly the tick `aitimer` resets and stays there for as long as `aastate=1`. | This was the largest single gate-1 residual: 34,295 move-order disagreements, 5,390 of the 5,423 drilled cases the one shape `sim=HOLD, server=ATTACK_TO`. Both sides are stationary throughout the idle corpus so it was a label divergence there -- but the label is on the observation wire and it gates `can_move`. | Corpus job 943: minion move order 34,295 -> **4,807** (-86%), turret move order 10 -> **2**. The residual is now symmetric -- 120 `sim=HOLD, server=ATTACK_TO` against 116 of the exact opposite in the same drill window that held 5,390 -- i.e. one-tick jitter at the swing boundary, not a missing gate. **That reading was wrong, and it is worth saying why, because the number it rested on was real.** Drilled at corpus scale (`tier1_residual_drill`, 19,800 pairs) the 4,791 are *not* symmetric at all: against the shared injected pre-tick order the split is **server-wrote-sim-held 4,769 against sim-wrote-server-held 22**, and the in-range margin that a boundary-jitter story needs is a median **35.6 u** with the verdict flipping between tick N and N+1 on only 3.3% of rows. A 120-vs-116 count in one window is a count of *cells in a confusion table*; one-sidedness is only visible against a shared baseline, which the pre-tick order supplies and the post-tick pair does not. Two mechanisms were hiding there: `ORDER-005` and `INJ-003`. **Cost, stated rather than hidden:** minion waypoints regressed 716 -> **800**, because the same early return also skips the out-of-range re-path and the sim no longer overwrites waypoints with a two-point chase path during a windup. That is source-correct and worse; it is not tuned back. Regression guards: `sim/tests/test_update_order.py::test_hold_is_not_written_while_a_swing_is_winding_up`, `::test_hold_is_written_on_the_tick_the_swing_actually_starts` (the pin against over-fixing) and `::test_a_turrets_move_order_is_never_touched_by_refresh_waypoints`. |
| ORDER-005 | `VERIFIED` | `Spell.FinishCasting`'s move order | **An auto-attack is a cast, and every completed cast writes the caster's move order.** `Spell.FinishCasting` (`Spell.cs:1051-1065`) ends, *after* its `IsAutoAttack` branch has set `HasAutoAttacked`, applied the hit (melee) or created the missile (ranged) and put the spell back in `STATE_READY`, with a tail that is not spell-specific: `if (SpellData.Flags.HasFlag(SpellDataFlags.InstantCast)) { MoveTo; AttackTo } else { CastInfo.Owner.UpdateMoveOrder(OrderType.Hold, true); }`. `SRU_OrderMinionMeleeBasicAttack` carries `Flags = 232448`, whose bit 2 (`InstantCast`, `SpellDataFlags.cs:13`) is **clear**, so a lane minion takes the `else`: on the tick its wind-up runs out its order goes to `Hold`, and `UpdateMoveOrder(Hold)` is `StopMovement()` is `ResetWaypoints()` (`HOLD-001`), so the route is destroyed with it. This is the same three lines the sim already ported for Garen's R. | `step.py` gated all four of those writes on `r_cast_finished` (Garen R only) and never routed the auto-attack through them, so a minion finishing a swing kept whatever order it was carrying and kept its route. | **Fixed 2026-09-18** (`finish_casting = r_cast_finished | aa.hit`, one line plus the four call sites in `step.py`'s returned state). `aa.hit` implies the unit entered the tick `IsAttacking`, which is exactly `ORDER-002`'s early-return condition, so `hold`/`chase` are provably False here and the two cannot collide; it *does* override the 250 ms controller's write, and must, because `AIScript.OnUpdate` runs before `Spells.Update` in `ObjAIBase.Update`. | Corpus job **1020 -> 1043** on the identical log: LaneMinion **move order 4,791 -> 1,388 (-71.0%)**, **LaneTurret move order 2 -> 0 (100.00%)**, target 736 -> 522, waypoints 333 -> 317, `aa_fire` 976 -> 966, `is_attacking` 1,534 -> 1,525, `has_auto_attacked` 1,968 -> 1,964, `aa_cooldown` 11,700 -> 11,691; `aa_hit` 1,041, hp 1,126 and every position gate unchanged, champion still 100% on every field, death agreement and wave spawning identical. **Nothing regressed.** The evidence that named it: over 19,800 tick-pairs, **2,074 of the 4,791** misses were the single shape `pre=AttackTo, sim=AttackTo, server=Hold`, 2,050 of them on a tick whose wind-up completes, and the whole row was one-sided at **server-wrote-sim-held 4,769 against sim-wrote-server-held 22**. Regression guards: `sim/tests/test_update_order.py::test_finishing_an_auto_attack_holds_and_destroys_the_route` (asserts the route reset too, because `HOLD-001` is the precedent for exactly this being half-ported) and `::test_a_swing_still_winding_up_does_not_get_finish_castings_hold` (the control against firing on every mid-swing tick, which would trade 2,050 misses for 2,345 and look like progress). **Not measured by gate 1:** the idle corpus's champion never swings, so the champion-side consequence -- a completed auto-attack now destroying the champion's route, which is what the server does -- is unexercised here and will first show up in gates 2/3. |
| INJ-003 | `FIXED` 2026-09-18 | Tier-1 injector: `minionActionTimer` | `LaneMinionAI.minionActionTimer` is `= 250f` at construction (`:22`), `= 0` at every sweep (`:97`) and otherwise only `+= delta`, and the free-run `deltaTime` is `(float)REFRESH_RATE` (`Game.cs:333`), so it is an exact float32 accumulation of `1000f/60f`. The fifteenth accumulation is **250.0000305**, so the trigger `minionActionTimer >= 250.0f` (`:90`) is crossed by **0.0000305 ms**. | `LanerlStateDump` publishes the field rounded to 1/1024 ms. The fourteenth accumulation, 233.3333588, rounds **down** to `238933/1024 = 233.3330078` -- **0.00035 ms low, 11.5x the entire crossing margin** -- so `injected + one tick = 249.9996796`, below the threshold. The injector handed that value straight to `ai_timer`. | **The 250 ms sweep never ran at all under one-step injection.** Measured over the whole gate-1 corpus: the sim's own rule `q/1024 + float32(1000/60) >= 250` fires on **0 of 395,366** minion tick-pairs against the server's own 26,537 `aitimer` resets. Only the event branches (`TargetJustDied()`, call-for-help) ever re-evaluated anything, so **every Tier-1 minion-controller number taken before this was measured with the regular sweep switched off.** | Fixed: `snap_action_timer_to_tick_grid` recovers the value onto the server's own float32 ladder before injection. The grid is unambiguous -- neighbouring points are 16.67 ms apart against a 0.0005 ms dump error, a factor of 3.4e4 -- and a value further from the grid than the dump could have rounded is left alone rather than snapped, the same contract `snap_windup_to_tick_grid` uses. Corpus job **1020 -> 1043** (this and `ORDER-005` together, on the identical log): **move order 4,791 -> 1,388**, **target 736 -> 522**. Guards: `parity/tests/test_inject.py::test_the_dumped_action_timer_is_snapped_back_onto_the_server_tick_grid` (walks the server's accumulation and asserts the snapped value reproduces its trigger where the dumped value does not), `::test_an_injected_minion_one_tick_from_its_sweep_actually_takes_it` (the **wiring**: a guard that calls the function directly still passes with the snap absent from `inject_snapshot`, which is the shape of half the silent failures logged here) and `::test_an_action_timer_that_is_not_on_the_tick_grid_is_left_alone`. **This is the third injector artifact found in one day** (`INJ-001`, `INJ-002`), and the largest in reach: it did not perturb a value, it disabled a controller. Re-derive any pre-2026-09-18 Tier-1 conclusion about minion targeting, aggro or move order before re-arguing it. |
| METH-001 | `BOUNDED` 2026-09-18 | **measurement methodology**, not a sim behaviour | Not a server behaviour. Recorded here because every other row in this table rests on it: each one is "run the corpus, change one thing, re-run, attribute the delta". | Observed directly and then reproduced: a file edited on `danilogin` (the NFS server, local `/srv/nfs`) and immediately run on `desktop` (the same export at `/mnt/nfs`) can be imported **pre-edit**; an identical rerun minutes later picks the edit up. First sighting was a test reconstructing an expectation from the pre-`STAT-002` constant while the simulator in the same process used the post-fix one; second was two consecutive edits to `parity/provenance.py` both invisible. **The mechanism is not established.** NFS attribute caching was the obvious suspect and was tested and **refuted** -- a fresh `cat` on the compute node sees new content at t+0, +5, +15, +30, +45 and +70 s. It reproduces through Python `import` but not through `cat`, leaving `__pycache__` as the surviving suspect, unconfirmed. | **An "after" run that silently executes "before" code produces a perfectly self-consistent set of numbers -- the previous run's.** No field-level agreement rate can detect it, for the same reason `INJ-003` was invisible: the thing that is wrong sits upstream of everything being measured. The failure is **null-producing**, and that bound is the useful part: it can make a real fix look like a no-op and it makes every "field X was unchanged" claim untrustworthy, but it **cannot manufacture an improvement**. Every measured improvement in this ledger therefore stands; the unchanged-field claims beside them are the ones carrying risk. | `parity/provenance.py`. `print_provenance()` fingerprints the **bytecode actually loaded**, reached through the imported module objects rather than by re-reading files -- so it also catches a stale `.pyc`, a module shadowed earlier on `sys.path`, and a compute node reading a different tree, none of which a source hash would see. It additionally hashes each module's UPPER_CASE scalars, because module globals live in no function's `co_consts` and *a changed constant is the commonest shape of a fidelity fix* (`STAT-001`, `STAT-002`, `RUNE_AD_BONUS`) -- a bytecode-only digest would sit still through exactly the change it most needs to catch. Mutation-checked: mutating `MASTERY_AD_PER_LEVEL_BONUS` in-process moves the digest. **Rule: a before/after pair MUST differ in the digest; if it does not, the comparison is void -- re-run it, do not interpret it.** `git` provenance needs `-c safe.directory=*` *and* an explicit `--git-dir`: this is a worktree whose `.git` file holds an absolute `gitdir:` into `/srv/nfs/...`, absent on `desktop`, and the dubious-ownership check exits non-zero with **empty stdout**, so the naive call silently reads as "no git here". |
| METH-002 | `VERIFIED` 2026-09-18 | **instrumentation**: the server emits its BRANCHES, not just its state | `LanerlStateDump` answers "what is true after this tick". Nothing emitted what the server DECIDED, so a ported-branch bug had to be inferred backwards from its consequences. | Measured cost of that inference: `ORDER-005` (one unported line -- `Spell.FinishCasting`'s tail runs `UpdateMoveOrder(OrderType.Hold, true)` for any non-`InstantCast` cast, and an auto-attack is one) presented as a **4,791-count** move-order residual read as symmetric boundary jitter for weeks; it was neither symmetric (4,769 against 22) nor jitter, and took a dedicated agent a day. `AA-002` (the dump rounds windup to 1/1024 s; a blue melee minion's final windup tick has 2.67e-5 s left and publishes as a flat 0) surfaced as 1,041 `aa_hit` + 1,126 `hp` + parts of three boolean fields, and took a day to collapse back to one mechanism. | **The instrument reproduced `ORDER-005` on its first run, as one grep**: 528 `FinishCasting` events in 200 s of idle recording, **528/528 `auto=True instant=False`** -- i.e. the server stating that premise outright. | `GameServerLib/Lanerl/LanerlDecisionTrace.cs` (vendored tree), gated on `LANERL_DECISION_TRACE=1`; parser and full build recipe in `lanerl_jax/parity/decision_trace.py`. Call sites: `ObjAIBase.UpdateMoveOrder` (emitted after the script veto, so only accepted writes), `Spell.FinishCasting` (with `auto=`/`instant=`, the flag deciding whether the tail runs), and `ObjAIBase.SetTargetUnit` (both endpoints of every real transition, `!ReferenceEquals`-guarded). Re-verified after the third site: same digest, same 1,159,704 rows, 247 transitions -- neutral AND exercised, both checked. **Fourth site, `LaneMinionAI.OnUpdate`'s trigger, in the ISOLATED script tree only** (`lanerl/patch_decision_trace.py`; the canonical tree must stay clean, `METH-003`): the three arms all produce the same observable -- a re-evaluated move order -- so which one fired is unrecoverable from state, which is the blind spot `INJ-003` lived in. Restructured from the short-circuit `A && (B || C || D)` into nested ifs reproducing the identical call sequence on every path (`FoundNewTarget` takes a flag and is not known to be pure, so re-evaluating an arm to label the trace would be a behaviour change made in order to observe behaviour); correctness established by the state-row digest, not by review -- `Loaded all` + `ce6f118002541655e528`/1,159,704 + the site firing, all three checked. **Measured split over 200 s idle: `Sweep250` 7,671, `TargetJustDied` 75, `CallForHelp` 53 -- the 250 ms sweep is 98.4% of all minion re-evaluations.** That restates `INJ-003`'s severity: its "0 of 395,366 injected tick-pairs against the server's 26,537" was a count comparison; what it actually means is that injected minions were re-evaluating from only the **1.6%** event-driven arms, with 98.4% of the controller's decision traffic absent, for every Tier-1 minion number taken before the fix. Deliberately NOT a poller like its sibling `LanerlHooks.TurretTrace`, which samples every 250 ms and so reports transitions but never reasons and cannot see a transition shorter than its own interval -- most of them, at a 16.67 ms tick. **Behaviour neutrality is measured in the strong form**, because a diagnostic that perturbs what it observes would move every parity number here for a reason that is not a bug: state rows byte-identical with the trace ON, not merely compiled in -- 120 s `d39f16cba113024717e8`/639,542 rows, and 200 s `ce6f118002541655e528`/1,159,704 rows with 54,253 branch events alongside. **The 120 s check alone was nearly a false pass**: minions spawn at t=90 s at opposite ends of the lane and cannot meet before 120 s, so that window contains no combat and exercised **zero** `FinishCasting` call sites. A neutrality claim from it would have shipped an instrument whose second call site had never executed once. Any new call site needs a window that provably exercises it. Built to `GameServerConsole/bin/Trace/net6.0` and NOT over `bin/Release`, because the vendored tree carries several uncommitted local patches that are not part of this work and every parity run uses that binary; same depth is required because the server resolves Content relative to its executable. |
| METH-003 | `VERIFIED` 2026-09-18 | **a broken AI script does not stop the server** | `Content/LeagueSandbox-Scripts` is Roslyn-compiled at boot. A script that fails to compile is logged by `CSharpScriptEngine` and then SKIPPED: `ContentManager` reports `Loaded some C# scripts from package` rather than `Loaded all`, and the game runs without it. | Nothing in this harness noticed. Measured directly: a deliberately broken `LaneMinionAI.cs` produced a complete, well-formed **122-snapshot state dump from a server whose lane minions had no AI at all**. The trace parses, the hashes are self-consistent, the entity counts are plausible, and every number computed from it is garbage. | **This is `INJ-003`'s shape -- a disabled controller, not a perturbed value -- which is now the second time this project has been bitten by that class, both times invisible to every field-level agreement rate.** It also corrects a claim made while finding it: an earlier `LaneMinionAI` edit referencing a class absent from the canonical assembly was described as something that 'would have broken every server boot'. It would not. It would have silently removed minion AI from every run while producing plausible traces, which is strictly worse. | `lanerl_jax/parity/script_health.py`: `assert_all_scripts_loaded(log)` fails unless every package reports `Loaded all`, and names the scripts that failed to compile. Call it on any recording whose numbers will be quoted -- a recording made against a partially-loaded package is not a weaker measurement, it is a measurement of a different game. **Retroactively audited: `runs/tier1_full` (the gate-1 corpus), `runs/tier15_noshop/drive` (the Tier-1.5 fixture) and `runs/seed_probe/s0` all report `Loaded all`**, so no number quoted to date rests on a partially-loaded script package. |
| METH-004 | `VERIFIED` 2026-09-19 | **method**: for two DETERMINISTIC engines, measure the FIRST divergence, not the last | Both engines are bit-reproducible (server identical over 4 runs, sim identical over 8 seeds, no randomness consumed). Two deterministic simulators that agree for 12,000 decisions and then disagree once disagree about everything afterwards. | Gate 3 has been asserted on ONE end-of-episode integer, CS@10. That integer is a legitimate gate -- its noise floor really is zero -- but it is a one-observation summary of a cascade, not a sample of independent errors, so it carries almost no information about which mechanism is wrong. The visible cost: the gate-3 chain in this ledger has been rewritten repeatedly as the *direction* of the gap flipped (the sim under-farmed at CS 3 vs 4; it now over-farms at **7 vs 4**, with FEWER attacks, 71 vs 86) without the cause ever being isolated. | **The comparison is exhaustive, not a proxy.** Both drivers call the *same* `last_hit_oracle.decide`, so the oracle cannot be a source of disagreement: identical inputs give identical outputs by construction. Every behavioural difference is therefore a difference in `decide`'s INPUTS, and those are small and fully enumerable -- champ x/y/ad/range, and per visible minion x/y/hp/armor/collision_radius. Recording exactly those is the whole of the divergence. | `lanerl_jax/parity/gate3_first_divergence.py`, driven by an additive `on_oracle` hook on the drivers' own loops (default `None`, so it cannot perturb the run). A hook and NOT a re-implemented drive loop, because `PATH-006` is the record of what re-implementing costs -- `isolation.py` drove a raw two-point path for weeks while the gate ran routed, so its measured effect described a run that no longer happened. **First run relocated the problem**: the episode's first divergence is not in the farming phase at all, it is on decision ONE, in the scripted walk-in (`PATH-007`). |
| METH-005 | `VERIFIED` 2026-09-19 | **method**: one-step parity CANNOT see accumulated drift, by construction | Tier 1 injects the server's state, steps ONE tick, and diffs. That is the correct instrument for a per-tick formula, and it is why gate 1 can report LaneMinion position at 100.00% (15 misses in 395,397). | It re-injects every tick, so it removes accumulation by construction. A unit that is consistently a fraction of a unit slow scores as ~100% parity there and still ends a long free run hundreds of units away. | Gate 1 reports the **champion at 100.00% on every field**, while the same champion under free movement is up to **930 u** from the server's at the same game time (`PATH-007`). Both numbers are correct; they answer different questions, and only one of them was being read as 'the champion is fine'. | This is what Tier 1.5 (inject once, then free-run) exists for, and the champion's free-run movement had never been measured -- the walk-in is scripted and identical on both sides, so it is a ready-made controlled experiment that was being skipped over as setup. | Any §3 claim of the form 'field X is exact' that rests only on a Tier-1 number is bounded by one tick of error, not by the episode's. Quote the tier alongside the rate. |
| METH-006 | `VERIFIED` 2026-09-22 | **method**: a DRIVEN fixture read without its action stream manufactures residuals out of nothing | `lanerl_jax.parity.record` drives the champion with a deterministic scripted action stream and writes it to `<tag>_actions.json`. `tier1_residual_drill` had no `--action-log` at all: it injected the server's state, stepped the sim with NO orders, and diffed against a server whose champion had just executed a walk, an attack-move or a cast. Every champion row is then a measurement of the missing order, not of the port. | The sim's champion stands still while the server's moves and acquires, and the difference is scored as a port defect in every section of the report. | Measured on the same log and window (`runs/aa005_gate`, t=110-250 s, 2,000 pairs), without vs with the action stream: champion target disagreements **231 -> 2** (`server ACQUIRED from none, sim did not` **186 -> 0**; `server SWITCHED off the incumbent` **45 -> 0**), champion `move_order` `sim=HOLD server=MOVE_TO` **142 -> 0**, sim-fires-early rows **100 -> 87**. The action file carries **13,823 non-noop orders**. | **The number that mattered is that 186.** It has the exact shape of a large one-sided acquisition residual -- the most interesting kind, and the kind `ORDER-003` is about -- and it was entirely self-inflicted. This is the same failure class as `METH-003` (a script that silently stops being the AI) and `AA-004` (a field that silently stops being the value): the run completes, the report is well-formed, the numbers are wrong and nothing says so. | Fixed by construction rather than by remembering: `_resolve_action_log` globs for the fixture's action file and **refuses to run** if it carries non-noop orders and no `--action-log` was passed, naming the exact flag to use. `--no-actions` is the explicit override and prints that champion rows are meaningless. An order naming a unit absent from the snapshot SKIPS the pair and is counted; it deliberately does NOT substitute a noop, because a noop is precisely the artifact above, reintroduced per-pair and invisible. **Consequence for the record: every champion row in every residual-drill report run on a driven fixture before 2026-09-22 is void.** The canonical gate-1 corpus is idle and is unaffected. **Which fixtures this voids, measured rather than assumed** -- count the distinct champion x positions in a log and a driven fixture is unmistakable: `aa004_cfh/aa_cfh_on` **12,624**, `aa005_full` **14,795**, and the canonical gate-1 corpus `tier1_full/server` **2**. So the gate-1 corpus is genuinely idle and its 19,800-pair numbers are clean, while **`AA-004`'s instrumented rates were taken on a driven fixture with no action replay** and their champion rows are void on the same grounds as `AA-005`'s. `AA-004`'s MECHANISM is unaffected -- it is proved from source and holds on minion and turret rows -- but the 74/48/2 exact-residue split and the 10-window 453/453 rate both need re-measuring with actions replayed. Re-measured so far, one window with the stream: 85 of 87 sim-fires-early rows at the clamp. |
| TGT-STALE | `REFUTED` 2026-09-22 -- refuted BEFORE it was implemented | the port compares target candidates at post-movement positions while the server's candidate set is frozen at tick start | `CollisionHandler.UpdateQuadTree` rebuilds the spatial index in `Map.Update`, BEFORE `ObjectManager.Update` moves anything (`Game.cs:481` then `:483`), so the server's candidate SET comes from start-of-tick positions while `IsValidTarget` and the distance comparison use LIVE ones. The port uses live positions for both (`targeting.py:391-398`), and the frozen snapshot it would need is already in state (`collision_x`/`collision_y`) and already wired into `advance_lane_waypoints`. | Would let the sim acquire a unit one tick before the server can see it. | Proposed as a fix for part of the **32-row** LaneMinion target residual. | **Inert, by arithmetic, and the check cost less than the fix would have.** The quadtree query is circle-vs-circle against each candidate's own bounds, `max(0.5, CollisionRadius)` (`CollisionHandler.cs:69-72`, `QuadTree.cs:61-64`), so the server's stale set is WIDER than the port's live mask by at least one collision radius. Measured from the port's own params: **minimum widening 30.0 u against a maximum of 5.75 u of movement in a 60 Hz tick** -- a 24.25 u margin. For the server to exclude a candidate the port includes, a unit would have to close 30 u in one tick, which is 5.2x its top speed. The two masks cannot disagree. | Worth recording as a REFUTED rather than dropping silently: implementing it would have made the port measurably WRONG (narrowing the candidate set to a stale snapshot with no compensating widening), while appearing to be a fidelity improvement with a source citation behind it. A cited divergence is not the same as a live one, and the cheap discriminator is usually a magnitude check -- the same instrument that caught the `aitsa` units error. |
| GATE3-001 | `VERIFIED` 2026-09-19 | **gate 3 is the INTEGRAL of gate 1, not an independent gap** | n/a -- this is a structural finding about what gate 3 measures. | n/a | The gate-3 CS gap has been chased as its own bug for weeks, and its explanation rewritten each time the gap's direction flipped. | **The full chain, read off ONE saved run (`gate3_first_divergence --save`), with game times:** first wave contact ~110 s; **minion POSITIONS first diverge at t=118.6 s** (decision 3557, by 6.39 u, against a §3 target of 1/16 u); visible minion COUNT diverges at 119.4 s (2 vs 1); **minion HP first diverges at 126.6 s** (decision 3799, by 12); and only at **135.5 s** (decision 4065) does a CHOICE finally differ. **Position precedes HP by 8 seconds**, so the root is positioning at the wave clash and NOT the damage formula -- a distinction the end-of-episode totals cannot make and repeatedly got wrong here. The divergent choice itself is attributed exactly, for free, by replaying the shared pure `decide` on the recorded inputs with one field substituted at a time: of AD, champion position, attack range and the minion list, **only the minion list flips it** -- the sim's nearest minion is at hp=71 (lethal to a 78 AD champion) where the server's counterpart is at hp=133 (not lethal), and the matched minions are 88-178 u apart. | **Consequence for sequencing: gate 3 cannot close while gate 1 has residuals.** Its criterion is exact CS against a zero noise floor, which requires the lane dynamics to be exact; gate 2 already records that free running divergence is large and begins at wave interaction. So the CS gap is not a separate defect to hunt -- it is the time-integral of gate 1's per-tick residuals (target 522, `aa_fire` 967, move order 354, hp 263), and effort belongs there. Leading candidates for the position divergence specifically, because it appears exactly at the first wave clash and not before: the intra-tick serial evaluation order (`ORDER-003`) and the unmeasured collision bound `MAX_ESCAPES_PER_UNIT = 8`, which can only bind when minions pile up -- i.e. at a clash. |
| CFH-002 | `REFUTED` 2026-09-21 (was `UNOBSERVABLE`) | call-for-help map is cleared before the dump can see it | `LaneMinionAI` populates `unitsAttackingAllies` from `OnCallForHelp` during a tick, reads it in `FoundNewTarget` -- where a candidate in the map takes its priority from the MAP (values 2-3) instead of `ClassifyTarget` (melee 9, caster 8), which is a decisive override -- and then clears it at the end of the same `OnUpdate` (`LaneMinionAI.cs:100-104`, armed by `:159`). | `LanerlStateDump` reads that live dictionary (`aihelp=`, `LanerlStateDump.cs:221`) AFTER `OnUpdate` has run, so it can only ever observe the map post-clear. An injected tick therefore begins with no call-for-help information and **cannot reproduce a call-for-help re-target at all**. | Every such re-target is scored by the one-step differential as a target-selection FAILURE that the simulator had no way to get right. This is the bulk of gate 1's `target` residual. | **Measured, three independent ways.** (1) From source: the clear is armed by `FoundNewTarget` itself, so the map survives to the dump only on ticks where the minion did NOT re-evaluate -- and re-evaluation ticks are exactly the ticks on which a target changes. The blind spot therefore lands precisely on the ticks gate 1 scores. (2) From the corpus: of 395,486 minion-ticks carrying the field, **1,797 (0.45%) hold any entry** and 393,689 are `none`. (3) From the residual itself, over a 1,200-pair window spanning the wave clash, classified by a signature needing no new dump field -- the unit the SERVER picked was itself attacking an ALLY of the minion doing the picking, while that minion's own dumped help map was empty: **15 of 16 rows (93.8%)**, and **16 of 16** had an empty map. By verdict: 'both moved, to different units' 9/9, 'server SWITCHED off the incumbent, sim held' 5/5, 'server ACQUIRED from none, sim did not' 1/1. Exactly **one** row -- 'server DROPPED an incumbent the sim kept' -- is left as a candidate simulator error. **This supersedes the `ORDER-003` reading of the same rows**: the branch-log ground truth covers only 3/16 by intra-tick ordering and 0/9 of the dominant class, so serial evaluation order is NOT what these are. | Not a simulator defect and not fixable in the simulator: the sim's own port is faithful (`targeting.call_for_help_map` derives the broadcast from damage actually dealt, `step.py`'s 5b, and `minion_acquire` applies the map-vs-`ClassifyTarget` priority override correctly). The resolution is INSTRUMENTATION -- emit `unitsAttackingAllies` before the clear, in the isolated script tree (`METH-002`/`METH-003` already establish that mechanism) -- after which the residual becomes measurable rather than assumed. Until then the `target` residual must be quoted as bounded-above, not as a simulator error rate. **Rate now confirmed at corpus scale, and it is LOWER than the first window suggested -- quote 81.8%, not 93.8%.** Five 1,200-pair windows spanning the episode: t=118-140 s **15/16** (93.8%), 160-185 s **39/41** (95.1%), 220-245 s **23/35** (65.7%), 320-345 s **22/29** (75.9%), 420-445 s 0/0 (no disagreeing rows at all). Total **99 of 121 = 81.8%**. The first window was the most favourable one and reporting it alone would have overstated the coverage by 12 points. **RETRACTED 2026-09-21. The mechanism is real; the ATTRIBUTION was wrong, and the instrument built to confirm it refuted it instead.** The server now emits `CallForHelpClear` -- the contents of `unitsAttackingAllies` immediately BEFORE the wipe, i.e. exactly the map `FoundNewTarget` just read. Aligned to game ticks through the dump's own `ailocal` (verified: localTime 30666 for id 1073743694 lands on tick 120660 where `ailocal=31402226`, i.e. 30666*1024 to within 0.24 ms). Over the instrumented recording: **128,901 clear events, only 1,325 (1.03%) with a non-empty map** -- so the map is nearly always empty even BEFORE the clear, and the dump was not hiding much. And on the residual itself: of 43 disagreeing target rows, **43/43 located a clear event, 0 had a non-empty pre-clear map, and 0 had the server's pick in the map.** The heuristic agreed with ground truth on **13/43**. **Why the heuristic failed, stated plainly because the error was foreseeable:** `OnCallForHelp` fires from `TakeDamage`, so a call requires the victim to have actually TAKEN DAMAGE that tick, and requires the listener in range of both victim and attacker. The signature used only 'the unit the server picked was ATTACKING an ally', which at ~1.2 s swing cycles against a 60 Hz tick over-counts by several fold. That over-population was explicitly predicted earlier the same day when rejecting a heuristic RECONSTRUCTION of the map as needing its own validation -- and then the same heuristic was used as a CLASSIFIER without it. The superseded rates, none of which should be quoted: 93.8%, then 81.8%, then 72.3%. Each widening has LOWERED the rate (93.8% -> 81.8% -> 72.3%), which is the expected direction when the first window was chosen because the mechanism was visible in it. **72.3% is the number to quote.** **Net effect: the whole `target` residual (522 on the corpus) goes back to OPEN as a candidate simulator error.** What the ground truth does give is a sharper decomposition to work from -- over the 43 rows: `server SWITCHED off the incumbent, sim held` **23**, `server DROPPED an incumbent the sim kept` **11**, `both moved, to different units` **7**, `server ACQUIRED from none, sim did not` **2**. The two 'server let go / switched away' classes are 34 of 43, which points at the server's RELEASE conditions rather than its acquisition ones: `IsValidTarget` (vision, range, death) and the 4-second failed-to-attack `Ignore()` with its 500 ms ignore window. `aitsa=` (time since last attack) is already in the dump, so that is testable without any new instrumentation. **What survives of CFH-002 is only this:** the dump genuinely cannot see the map, so it remains an instrument blind spot in principle -- it is just not a blind spot that was costing anything, because the map is empty 99% of the time at the moment it is read. |
| GIVE-001 | `REFUTED` 2026-09-21 | the server's 4-second failed-to-attack give-up rule | `ReevaluateBehavior` (`LaneMinionAI.cs:323-334`) drops a target that is STILL VALID and `Ignore()`s it for 500 ms once `timeSinceLastAttack >= 4000f`. The counter resets whenever `LaneMinion.IsAttacking` or there is no target (`:66-73`), so it measures 'how long have I failed to land a swing on this target'. | Proposed as the cause of the `target` residual's dominant verdicts after `CFH-002` was retracted: 34 of 43 rows are 'server let go / switched away while the sim held', which is exactly the shape a give-up rule produces. | Would have explained the residual with no new instrumentation, since `aitsa=` is already dumped. | **Refuted: 0 of 43 rows (0.0%) are at or past the 4000 ms threshold.** Maximum observed is 1,033 ms, and the distribution is an unremarkable spread -- 17 rows under 1 ms (the counter had just reset, i.e. the minion was mid-swing), 1 in 1-100 ms, 9 in 100-500 ms, 14 in 500-1,000 ms, 2 in 1-2 s. Nothing is within a factor of four of the rule firing. | **Kept as a REFUTED row rather than deleted, because it is an obvious hypothesis that will be proposed again.** It also carries a units lesson worth the row on its own: `aitsa` is `Q(timeSinceLastAttack, StatQ)` and the field is in MILLISECONDS (`+= delta`; the rule's literal is `4000f` for four seconds), so `q / StatQ` yields ms, not seconds. Comparing it against `4.0` instead of `4000.0` initially reported **60.5% at or past the threshold** -- the exact opposite conclusion. What caught it was not the code but the MAGNITUDE: the 'max' came out as 1,033 s on a recording only ~210 s long, which is physically impossible. Sanity-check units against the length of the thing you measured. |
| AA-004 | `UNOBSERVABLE` 2026-09-19 | the dump's auto-attack COOLDOWN GATE CLAMP fires the injected swing early | `LanerlAim.AutoAttackCooldownRemaining` publishes `Q(Math.Max(0f, remaining), StatQ)`. The clamp runs BEFORE the quantisation, so a cooldown that is still strictly positive but within rounding of the gate publishes as a flat **0**, indistinguishable from a genuinely ready swing. | Injecting that 0 tells the simulator the swing is READY when on the server it was not, so the sim swings one tick early -- through no fault of its own model. | The three largest remaining gate-1 residuals: `aa_fire` **967**, `is_attacking` **1,006**, `has_auto_attacked` **931** -- about 2,900 corpus field-misses, which the drill already shows are ONE mechanism and not three (both booleans are contained in fire U hit: 80 and 79 covered of 80 and 79). | **Confirmed with a directional prediction and a negative control, which is what makes it more than a story.** The prediction was that sim-fires-EARLY rows should sit on a dumped 0 and sim-fires-LATE rows should not. Measured over the wave-clash window: **sim fires / server does not: 79 rows, 79 of 79 (100.0%) at the gate clamp**; **server fires / sim does not: 2 rows, 0 of 2 (0.0%) at the clamp** (their dumped cooldowns are 495 and 734 quanta). Perfect separation both ways, and the 40:1 skew in favour of firing early is exactly the asymmetry the clamp predicts. All 79 also have `target_agrees=1` and are 100% already in range, so target selection and geometry are excluded -- only the gate differs. | Same family as `AA-002` (windup rounding) and `AA-003` (the cooldown grid snap), and it is why `snap_cooldown_to_tick_grid` deliberately refuses to touch a dumped zero: the clamp destroys the information, so no per-tick formula can recover it -- separating an already-fired tick from a not-yet-fired one needs the PREVIOUS tick's dump, which is outside a one-step injector's contract. **Consequence: `aa_fire`/`is_attacking`/`has_auto_attacked` are an upper bound on simulator error, not a measurement of it, and the same goes for the one-sided HP-change (228 vs 15) and death (21 vs 1) asymmetries in the same run -- all are injected-tick artifacts.** Resolution is instrumentation: publish the UNCLAMPED remaining cooldown (a signed value) so a not-yet-ready swing is distinguishable from a ready one. Until then these fields cannot be used as evidence that the sim over-attacks -- and specifically must NOT be cited in support of gate 3's over-farming, which is free-running and cannot be touched by this clamp. **INSTRUMENTED AND PARTLY CORRECTED 2026-09-21.** The dump now carries `aacdbits=`, the float32 BIT PATTERN of the UNCLAMPED cooldown (additive; `aacd=` and a quantised `aacdraw=` are both untouched, because every recorded corpus and parser depends on `aacd=`). The bits are required: a quantised raw field does NOT settle this, since rounding destroys exactly the sub-quantum residue at issue. Neutrality re-verified in the strong form -- `LANERL_STATEROW` digest `7e24091cef70d2aeb319` and 1,219,899 rows identical with instrumentation ON and OFF -- which it must be by construction, since the field lives in `DescribeInternals` and `Describe()` never calls it. **Re-measured on the instrumented recording, the clamp explains 60%, not 100%.** Of 124 sim-fires-early rows, all 124 at the clamp: **74 (60%) carry a strictly POSITIVE exact residue** (5.51e-07 and 1.326e-06 s -- genuine sub-quantum, and the mechanism is now confirmed by direct observation rather than inference), **48 are exactly 0.0** and **2 are strictly negative**. The server's gate is `_autoAttackCurrentCooldown <= 0` (`ObjAIBase.cs:1267`), so **exactly zero IS ready** -- those 50 rows are not the clamp hiding anything, see `AA-005`. Corpus-scale rate over 10 windows: **453/453 = 100.0%** of early-fire rows sit at the clamp, and the negative control is **2/16 = 12.5%** of late-fire rows, not the 0/2 the first window showed -- still a strong separation, but not the perfect one originally reported. **SCOPE CORRECTION 2026-09-22.** Every instrumented rate in this row was measured on `aa004_cfh`, which is a DRIVEN fixture (12,624 distinct champion x positions) read by a drill that had no action replay at all -- see `METH-006`. The champion rows in the 124-row split and in the 10-window 453/453 are therefore measurements of the missing order stream, not of the clamp, and both numbers are withdrawn pending re-measurement. What survives unchanged: the MECHANISM, which is read from source (`Math.Max` before `Q`) and does not depend on any fixture; the `aacdbits` instrument itself; and the clamp's hold on minion and turret rows. First re-measurement with the action stream replayed (`aa005_full`, t=110-250 s, 2,000 pairs): **85 of 87 sim-fires-early rows blocked at the cooldown gate**, the other 2 at `CanAttack()`, and none with the chain fully open. |
| AA-005 | `REFUTED` 2026-09-22 -- the premise was wrong, the rows were never ready | the server is READY by every gate the dump exposes, and still does not swing | A swing needs the whole chain at `ObjAIBase.cs:1254-1290`: `TargetUnit != null && TargetUnit.Team != Team && MoveOrder != CastSpell`, then `DistanceSquared <= (Stats.Range.Total + TargetUnit.CollisionRadius)^2 && MovementParameters == null`, then `AutoAttackSpell.State == STATE_READY`, then `CanAttack()`, then `_autoAttackCurrentCooldown <= 0`, then `!_skipNextAutoAttack`. **Every gate on that chain is now PUBLISHED** as `aagate=`, one bit per condition (`LanerlAim.AutoAttackGateBits`), alongside the raw `StatusFlags` word as `status=`. Bits are the CONDITIONS, not the outcome, so the chain is re-evaluable by a reader and does not depend on the emitter's idea of which gate mattered; `first_shut_gate` walks them in the server's own evaluation order, because a gate after the first shut one was never reached and is not evidence. | The sim swings on a subset of that chain. | Was **50 of 124 sim-fires-early rows (40%)**. Re-measured on a gate-instrumented recording (`runs/aa005_gate`, 250 s, t=110-250 s, 2,000 pairs, actions replayed): **87 sim-fires-early rows, 85 blocked at `cooldown_elapsed`, 2 at `can_attack`, and 0 -- zero -- with every gate open.** | **The residual AA-005 was opened to explain does not exist on a correctly-driven fixture.** Two independent things were wrong with the 50. (1) The 48 'exactly 0.0' rows came from a drill run with NO action replay (see `METH-006`): the recording is a DRIVEN fixture, the sim received no orders while the server's champion followed the scripted drive, and champion rows were manufactured wholesale -- the same run reported ~190 phantom champion target disagreements that collapse to 2 once the action stream is passed. (2) The 2 rows that DO survive as a real gate are `can_attack`, and the status word says exactly which flag: `status=536872988` has `StatusFlags.CanAttack` (bit 1) CLEARED and `Ghosted` (bit 11) SET, against a healthy `536870942`. That is a disable the sim does not model, not a phase effect. | **Method note, and it is the whole point of this entry.** `AA-005` was opened with five named candidate gates and a plan to guess between them. Three earlier residuals had already been attributed by exactly that kind of reasoning and all three were refuted on first measurement (`CFH-002`, `GIVE-001`, and `AA-004`'s over-extension). Emitting the gates cost one afternoon and one rebuild and settled it outright, including settling it AGAINST the entry's own premise. The negative direction (`server fires, sim does NOT`, 43 rows) is deliberately NOT attributed from this field: `aagate` is dumped at the END of tick N, which is the state tick N+1 evaluates its gates from -- correct for a swing the server DECLINED in N+1, meaningless for one it MADE, and the drill now prints that caveat above the histogram rather than leaving a reader to infer it. Those 43 are 39 champion rows whose injected cooldown was already well clear of zero, i.e. the action-replay endpoint boundary, not a gate. |
| BOUND-001 | `BOUNDED` 2026-09-19 | the two silent-truncation constants, measured instead of quoted | The server's movement consumes waypoints in an unbounded `while(true)`, and its collision resolves overlaps until resolved. | JAX needs fixed trip counts, so both are unrolled to a constant: `movement_jax.MAX_STEPS_PER_TICK = 8` and `collision.MAX_ESCAPES_PER_UNIT = 8`. Both modules' docstrings admit the value was **chosen from precedent, not measured**. Too small truncates SILENTLY -- a unit moves less far, or an overlap is left unresolved -- and reads as a pathing or position bug. | This project has been bitten twice by silent-truncation/disabled-machinery bugs (`INJ-003`, `METH-003`), both invisible to every field-level agreement rate. | Driven through each module's OWN measurement hook (`max_steps_used`, `max_escapes_used`) over injected real server state, two 240-tick windows -- one mid-combat (t=150-154 s) and one during the minion march (t=95-99 s). **Collision: observed max 2** (combat: 70%/10%/20% at 0/1/2 over 4,800 affected unit-ticks; march: max 1, 93.7%/6.3% over 3,360). **0 unit-ticks at or above the bound of 8, and 0 where the probe itself saturated**, so the numbers are maxima and not floors. **Movement: 0 everywhere**, and that result is VACUOUS rather than reassuring -- it must not be read as 'the bound is safe'. Cause traced: the dumped waypoint list is only ever 1 or 2 points with `wpkey=1` (measured over 11,400 internal lines during the march: 7,800 of length 1, 3,600 of length 2, `wpkey=1` for all), so an injected unit never has a waypoint left to CROSS and the count is structurally 0 regardless of window. | **Both bounds have large headroom and neither is truncating today**, but for different reasons and with different strength. Collision is a real measurement (max 2 against a bound of 8) on a modest sample -- 8,160 affected unit-ticks over 8 s of game time, not the full corpus. Movement is bounded by STRUCTURE rather than by sampling: the server never emits more than 2 waypoints, so at most one can be consumed per tick, and the sim's own routed paths are few long segments (the champion's first approach leg is 4 waypoints over 1,792 u against 5.75 u of travel per tick). A free-running measurement would still be needed to bound the sim's own multi-point paths directly; the injected corpus cannot do it. Constants left unchanged -- this is a measurement, not a tuning. |
| GATE1-001 | `BOUNDED` 2026-09-19 | **gate 1 is INSTRUMENT-limited, not simulator-limited** | n/a -- structural finding about what gate 1 can measure. | n/a | Every §3 Tier-1 target is worded 'exact'. Nothing is exact, so gate 1 has read as OPEN with nine separate residual fields, and effort has been spread across all nine as if they were nine defects. | **They are not nine. Consolidating the corpus run (19,800 tick-pairs) against the decompositions measured the same day:** `aa_fire` **967**, `is_attacking` **1,006** and `has_auto_attacked` **931** are ONE mechanism -- `AA-004`'s cooldown gate clamp (79/79 of the early-fire rows on a dumped zero, 0/2 of the late ones) -- and the drill already shows both booleans contained in fire U hit. `target` **522** was attributed to `CFH-002`'s cleared call-for-help map; **that attribution is RETRACTED** -- ground truth from the pre-clear map shows 0 of 43 disagreeing rows had the server's pick in it, so `target` is OPEN in full. `move_order` **354** is **118/171 = 69.0%** co-located on a (tick, netid) that ALSO has a target or fire disagreement, and `waypoints` **317** is **86/155 = 55.5%** co-located with one (both widened 2026-09-21 from the first window's 6/6 and 3/4, which were far too small to quote and both came down substantially) -- which is mechanically expected, since `RefreshWaypoints` rebuilds the chase path from the target every tick and the same re-evaluation picks both. `hp` **263** is **100%** inside `RESET-004`'s missile blind spot (0 of 5,085 missile-free ticks disagree). Position L-inf <=1/16 is **15 of 395,409 = 100.00%** and the champion is **100.00% on every field**. | **REVISED 2026-09-21 after both blind spots were instrumented: gate 1's residual is dominated by TWO, and one of them only partly.** `RESET-004` holds (hp is 100% missile-confounded). `AA-004` holds for **60%** of the fire rows, not all of them (`AA-005` is the other 40%). `CFH-002` is **REFUTED** as an attribution -- the map is empty 99% of the time at the moment it is read, and contained the server's pick in 0 of 43 disagreeing rows -- so `target`, `move_order` and `waypoints` all go back to OPEN. **The lesson is the method one, and it cost two false attributions in one day: a signature CONSISTENT with a mechanism is not evidence that the mechanism fired. Both were only settled by emitting the state and looking.** What is left as candidate simulator error is small: roughly **95 target rows** (the 18% `CFH-002` does not cover), about **1 waypoint row**, and the **2,070** `aa_cooldown` remainder whose >40-quanta band co-occurs with a fire disagreement 100% of the time and so is largely downstream of `AA-004` too. **The honest verdict: gate 1 cannot be CERTIFIED at '`exact`' with the current dump, because the dump cannot observe the state the test needs** -- a cleared map, a clamped cooldown, an in-flight missile. That is a different statement from 'the simulator is wrong here', and the two have been conflated. | **Scope stated rather than buried:** the corpus aggregate is the full 19,800 pairs, but the containment and clamp decompositions come from 1,200-pair windows and are small-n (6 move-order rows, 4 waypoint rows, 81 fire rows); `CFH-002`'s rate is 5 windows. The MECHANISMS are proved from source independently of those counts; the RATES need the full corpus before being quoted as final. Next step is instrumentation, not simulation: publish the unclamped cooldown (`AA-004`) and emit `unitsAttackingAllies` before its clear (`CFH-002`), then re-measure -- both are script- or dump-side changes in the isolated tree that `METH-002`/`METH-003` already established. |
| GATE1-002 | `VERIFIED` 2026-09-22 -- this is the gate's DEFINITION OF DONE | **gate 1's pass criterion, chosen because the literal §3 wording has no reachable pass state** | §3 words every Tier-1 target as `exact`. Several of those quantities are not observable exactly THROUGH THIS DUMP -- a cooldown clamped to zero before quantisation (`AA-004`), a call-for-help map cleared before it is read (`CFH-002`), a missile in flight across the boundary (`RESET-004`), a buff whose phase is never published (`BUFF-001`). A test cannot certify a property its instrument cannot measure. | Against the literal wording gate 1 can never close no matter how correct the simulator becomes, and it has accordingly read `OPEN` through nine successive residual reductions. That is a specification defect, not a simulator one. | The gate stops being a measurement and becomes a permanently red light nobody can act on -- and effort goes into grinding residuals instead of naming the problem. That already happened, and it cost real time. | **THE CRITERION. Gate 1 passes when the residual attributable to the SIMULATOR is zero at the §3 tolerances, and every remaining row is attributed to a named mechanism that is measured rather than argued.** Five clauses, all required at once. **(1) Total attribution** -- every disagreeing unit-tick in every scored field is attributed to a named ledger mechanism, and the unattributed remainder is reported as an integer which must be **0**. A field that is 'mostly' explained does not pass; the leftover is the point. **(2) Each mechanism is classified and the class is earned** -- exactly one of `FIXED` (the simulator changed and the residual went away), `INSTRUMENT` (the dump cannot observe what the test needs, AND an emission was attempted, with either the emission landing or a stated reason it cannot), or `FLOOR` (a deliberate design decision, e.g. fixed-phase vectorisation against a serial per-object loop, with the divergence measured and bounded). No fourth class, and in particular no 'probably fine'. **(3) Attribution by observation, not inference** -- a mechanism counts only if it was measured by emitting the state and looking. Not methodological purity: **four inferred attributions have been made here and four have been refuted** (`CFH-002`, `GIVE-001`, `AA-004`'s over-extension, `TGT-STALE`), while every emission-based one has held. A signature consistent with a mechanism is not evidence the mechanism fired. **(4) The harness is validated first** -- the corpus is confirmed idle or its action stream is replayed (`METH-006`), and the per-stream census confirms every emission the attribution relies on is actually in the file. An attribution measured through a broken harness is worse than none, because it looks like an answer. **(5) Every bound is a re-measurable number** -- each `INSTRUMENT` and `FLOOR` mechanism carries a rate on the canonical corpus, so a regression moves a number rather than a paragraph. **Why this is not a rubber stamp:** it keeps `exact` as the standard for the simulator itself, and clause 1 admits **zero** unexplained rows, which is stricter than any percentage threshold and stricter than the gate has ever actually been held to. What it drops is the pretence that the INSTRUMENT is exact. A gate that passes here says something precise: every way the two engines differ on a re-injected tick is either fixed, or a published limit of what the server tells us, or a design trade made on purpose and measured. **Distance to it:** one artefact. The canonical corpus predates every emission, so its residuals can be counted but not observed. `record_idle_trace(instrumented=True)` regenerates it against the observability build; since all four emissions live in `DescribeInternals` and are measured byte-identical on `LANERL_STATEROW`/`LANERL_STATEHASH`, the regenerated corpus should reproduce the historical one EXACTLY and simply carry more, keeping every number quoted against it comparable. Verify that equality by hash; do not assume it. **BLOCKER CLEARED, same day.** The instrumented corpus is recorded (`lanerl_jax/runs/tier1_instr/server`, 689 MB, 420 s idle, same seed) and it reproduces the historical canonical corpus **byte for byte**: `LANERL_STATEROW` sha1 `cd13aad2cd09772cd5a2d9921f6b766f54a6ae7a` and `LANERL_STATEHASH` sha1 `ddfd14b2a9cfb797ccacb57a2084919e8d46dfbc` on both, over **2,607,728** rows each. So it IS the canonical corpus -- every number ever quoted against it (target 522, move order 354, waypoints 317, `aa_fire` 967, `aa_cooldown` 2,070, hp 263, position 15) stands unchanged and is now OBSERVABLE. Stream census on it: `aacdbits`/`aagate`/`status`/`aibuffs` 1,050,738 lines each, `CallForHelpClear` 395,506, minion branch stream 26,700, script health `Loaded all`. That is clause (4) satisfied for the canonical corpus, and it is idle (2 distinct champion positions), so `METH-006` does not apply to it. What remains is clauses (1)-(3), field by field. |
| TGT-NULLOUT | `FIXED` 2026-09-22 | **`ObjAIBase.UpdateTarget`'s null-out was never run for lane minions** | `ObjAIBase.UpdateTarget` nulls a target that is dead, untargetable-and-useable, or no longer visible to the unit's team (`ObjAIBase.cs:1203-1211`). It runs for EVERY `ObjAIBase` -- lane minions included -- after the AI script and on every tick, independently of `LaneMinionAI`'s own 250 ms re-evaluation gate. | `step.py` applied it to champions and turrets only (`jnp.where(is_champ, ..., jnp.where(is_turret, turret_target, ai.target))`), so a minion's target came straight from the script. A minion therefore KEPT a target the server had already dropped until its own sweep came round, which can be fifteen ticks later. | `server DROPPED an incumbent the sim kept` -- and, downstream, a stale chase path and move order for the same window, since `RefreshWaypoints` rebuilds from the target every tick. | **Found by observation, and the signature is exact.** Joining every target disagreement to the server's OWN branch stream (`MinionReevaluate`, which names which arm opened the minion's gate) over 161,763 LaneMinion unit-ticks: of 216 disagreeing rows, **21 fall on ticks where the gate never opened at all** (13 `<no-event>`, 8 `<unaligned>`) -- and **all 21 carry the same verdict, `server DROPPED an incumbent the sim kept`**. With the gate shut the script cannot have dropped anything, so the engine did, and the engine pass is the one thing the port was not running. 21 of 21 on one verdict is not a correlation, it is a fingerprint. **The same join bounds what is NOT this bug.** `Sweep250` fires **10,699 times and disagrees once** (0.009%), so the port's candidate scan, priority table, incumbent-distance sentinel and tie-break are exact and are not where the residual lives. The remaining 194 rows concentrate on the two arms that depend on information arising WITHIN the tick -- `TargetJustDied` at **x694** enrichment and `CallForHelp` at **x434**, against arms that fire on 0.17% of ordinary ticks -- which is `ORDER-003`'s fixed-phase floor, now measured rather than asserted. **Implementation note:** uses the ENGINE's predicate, deliberately not the script's `IsValidTarget`. That one also tests acquisition range; copying the wider predicate here would drop targets the server keeps and trade this residual for its mirror image. `lanerl_jax/sim/tests` 56 passed. Canonical-corpus effect pending the `tier1_instr` re-run. **CANONICAL-CORPUS EFFECT: ZERO, and that is the honest headline.** Re-run on `tier1_instr` (19,800 pairs, hash-identical to the historical corpus) with the fix in: **every field is bit-identical to the pre-fix run** -- `aa_cooldown` 2,070, `aa_fire` 967, `aa_hit` 7, `has_auto_attacked` 931, hp 263, `is_attacking` 1,006, move order 354, **target 522 (unmoved)**, waypoints 317, position L-inf 15; turrets and champion unchanged. So this fix does NOT reduce the canonical target residual. **The alternative explanation -- that the fix was never loaded -- was excluded by direct test rather than by assumption**, which is the one failure mode no agreement rate can detect (`provenance.py`): injecting a real corpus snapshot, killing one minion and pointing another at the corpse, one tick clears the target to -1. The fix is active; it simply has nothing to do on this corpus. **Why, most likely:** on an idle corpus a minion's target going invalid is almost always a DEATH, and the script's own `TargetJustDied` arm opens the gate on that same tick, so script and engine drop it together. The engine pass also keys on `!IsVisibleByTeam`, and visibility churn comes from champion movement -- which is exactly what an idle corpus does not have and the driven fixture does. Stated as the likely reason, not a measured one; the 21-row breakdown by invalidity cause has not been taken. **Scope, so this is not misread later: 21 of 216 rows on a DRIVEN fixture, 0 of 522 on the canonical idle corpus.** It is a structural fidelity fix -- the port now runs a pass the server runs -- with no measured effect on any number gate 1 is scored against. |
| BUFF-001 | `FIXED` 2026-09-22 (instrument added, then the harness corrected) | **the dump publishes buff NAMES but never buff PHASE, so an injected champion's Garen E has already ended** | `Describe()` renders `GetBuffs().Select(b => b.Name)` and nothing else. `Buff.Duration` and `Buff.TimeElapsed` are both public and neither is published. Garen E sets `StatusFlags.CanAttack` false and `Ghosted` true for the spin's 3.0 s, which is what `CanAttack()` reads at `ObjAIBase.cs:1264`. | The simulator models the spin correctly (`spells.py:819`, `suppress_attack = e_active & ~e_expired`). The INJECTOR cannot: with no phase it restores identity at duration 0, and `e_expired = e_active & (e_elapsed >= buff_duration[e_slot])` then expires the spin on the very tick it is injected. `suppress_attack` goes false and the sim swings. | `sim_fire=1 / server_fire=0` on the champion, charged to the simulator as a missing `CanAttack()` gate -- which is exactly how it was first written up, as `AA-005`. | **Found by widening, which is the point.** At 2,000 pairs `can_attack` was 2 of 87 early-fire rows (2.3%) and looked like noise; at 8,217 pairs it was **258 of 647 (40%)**, every one on the single status word `536872988` -- `CanAttack` clear, `Ghosted` set -- across 18 spans spaced at the spin's cooldown. **Fix measured as an A/B on byte-identical corpora** (all three recordings share `LANERL_STATEROW` sha1 `73d2bdc42f07b810e9209ae5fd5b72159da2cc8c`), same window, same 8,217 pairs: `can_attack` **258 -> 0**; `cooldown_elapsed` **378 -> 378 unchanged**, so no collateral; total sim-fires-early **647 -> 387 (-40.2%)**; champion `sim_fire=1/server_fire=0` **271 -> 11 (-96%)**. **Every LaneMinion fire row is bit-identical across the change (375 / 23 / 23 / 1 before and after)**, which is the isolation proof -- minions have no Garen E, so a fix that touched them would have been a bug. Reported honestly the other way too: the mirror direction rose **81 -> 87 (+6)**, a trailing-boundary effect at the end of the spin, and total fire disagreements went 774 -> 520 (-32.8%). **Emission design:** `aibuffs=name:elapsedBits:durationBits`, BIT PATTERNS not `Q()`, because `Buff.Elapsed()` is `TimeElapsed >= Duration` -- another knife-edge comparison that 1/1024 s rounding collapses exactly at the answer, which is the mistake `aacdraw=` already made once. It lives in `DescribeInternals`, never in `Describe()`, whose name list feeds the canonical hash; neutrality verified byte-for-byte on `LANERL_STATEROW` and `LANERL_STATEHASH`. `buff_power` is still NOT restored: it is a cast-time damage snapshot the dump does not publish, and recovering phase while inventing power would trade a visible residual for an invisible one. **Scope: inert on the canonical idle corpus**, where the champion never casts -- this cannot move `aa_fire` 967 and must not be conflated with `AA-004` when that rate is re-measured. |
| PATH-007 | `OPEN` 2026-09-19 | champion route out of the fountain | Given a MOVE order to `TOP_LANE_PATH[0]`, the server's own pathfinding leaves the fountain heading **+x**. | The sim's routed path leaves heading **-x**. The champions separate on decision 1 (9.7 u), reach **930 u** apart, and hold ~596 u apart for ~800 decisions before the closed-loop approach brings them back together. | The sim reaches lane **52 decisions (1.73 s) late** against a fixed minion spawn clock, so it starts farming later and meets a different wave state. Bounded: the walk-in is closed-loop on each engine's OWN position, so the lag does NOT persist -- separation is 0.000 once both arrive. | **It is one route walked late, not two different routes** -- the two readings of '596 u apart' have opposite causes and are separated by `gate3_first_divergence.align_lag`: matching each sim decision to its nearest server decision gives a lag that locks to exactly **-52 with a ~2 u residual sustained over 800+ decisions**. A wandering lag or a large residual would have meant the routes themselves differ. The detour costs 52 x 11.5 u ~= 598 u. **Two candidate causes are already refuted by measurement, not assumed away.** (1) NOT a speed difference: the dump gives move speed 353280/1024 = 345.0 u/s and the sim's 11.50 u/decision at 30 Hz is exactly 345/30. The server's suspiciously exact (6,10) per-decision steps are an artifact of the wire TRUNCATING position with `(int)` (`LanerlControl.cs:188`), which is also worth recording on its own: wire positions carry up to 1.0 u of biased error and cannot be compared at the §3 1/16 u target at all. (2) NOT an AD error: wire `ad=78.14` against the sim's `78.134765625` looks like a 5-quantum miss, but `Fmt` rounds to 2 dp and `Q` rounds to 1/1024, so the dump's 80010 means [78.13428, 78.13525] and the wire's 78.14 means [78.135, 78.145) -- they intersect at ~78.1351 and the sim is light by ~0.3 quanta, INSIDE the §3 target. What survives from that is instrumental, not a fidelity gap: the two oracles are fed AD differing by 0.0052 purely because one reads a 2-decimal wire. **Route length is measured, and it is NOT the explanation.** Replaying `build_local_waypoints` over all six approach legs from the champion's spawn: with `SmoothPath` ON (production) the total routed length exceeds the straight-line total by only **+94.9 u = 8.3 decisions**, against a measured lag of 52. With `SmoothPath` OFF it is +2,095.7 u = 182 decisions, so `SmoothPath` is on and doing its job. **~43.7 decisions (~500 u) of the lag remain unexplained by path length**, and since the two engines' speeds agree exactly the remainder must be walked distance the single-shot route does not predict -- the route is recomputed from the current position every decision, so the path actually walked is a sequence of partial routes and can differ from any one of them. That is the next measurement: cumulative walked distance from the recorded per-decision positions against the single-shot route. Also corrected here: leg 0's first waypoint at (-53.90, 464.81) was briefly read as off-map. It is not -- the terrain grid's origin is (-328.9, -110.2), the waypoint is inside it, and a walkability check at r=35 over every emitted waypoint returns 0 non-walkable. |
| PERF-001 | `APPROX` | routed-training throughput gate | The JAX rewrite must retain accelerator throughput high enough for RL. | **The table was the wrong suspect and the unroll sweep tuned the wrong loop.** Full profile 2026-09-18 (5 repeats x 60 steps): observation 10.005 ms, policy 14.486, `apply_orders` non-routing 0.926, routing 4.349, `step_decision` 44.737. Inside routing, `closest_terrain_exit` is 90.3% and the hop-table gathers are **0.3%**. `ROUTE_LOOP_UNROLL` moved nothing because it tunes a loop that runs **6** trips; the spiral runs up to **203**, on a per-lane distribution of p50 = 0, p90 = 0, p99 = 88 -- so 97.5% of lanes need none and one straggler makes the whole `vmap` batch pay. Three semantics-preserving fixes landed in `d47ab44` (spiral unroll, `CastCircle` reducing once instead of per-step scattering, and a `scan(unroll=32)` on the line walk whose cost was *perfectly* linear in its bound at ~18 us/step, i.e. essentially all launch overhead). Result: **58,255 dec/s without `SmoothPath` -- gate 4 PASSES** -- and 51,270 with it. Negative result worth keeping: unrolling `SmoothPath`'s own greedy makes it **worse** (82.97 ms at 1, 103.81 at 16), because a masked-off chained body there is a whole wasted `CastCircle` rather than amortised launch overhead. | Gate 4 passes on the pre-`SmoothPath` simulator. What remains is a stated trade, not an unknown. | Reproduce only with the canonical command; `--no-smooth` and `--smooth-line-steps` are labelled controls. Next, if the 12% is wanted back: measure the line-step distribution on the **real action lattice** (gameplay clicks are viewport-bounded, so 128 is likely far looser than needed), and surface `smooth_exhausted` in training the way `route_nonready` is. Unattributed and larger than all of the above: `step_decision` is 60% of the step, with `input_reduce_fusion_13` at 10.80 ms and ~12.8 ms of CUB radix sorts. |
| OPS-001 | `BOUNDED` | route asset distribution | A training checkout needs the exact artifact matching navgrid bytes, radius, and ABI. | Heavy route data live under ignored `data/jax_routes/`; production remains pinned to `map1_garen_r35_o50_v2` (231 MiB packed hops). The loader also accepts the measured `v3` same-direction-run sidecar experiment (693 MiB total), but its memory/compile cost has not yet produced a gate result, so it is not required. Unknown versions, hashes, shapes, or v3 sidecar semantics fail closed. | A fresh machine cannot start routed training until the pinned artifact is generated or distributed. **Generating and loading need different environments**, measured 2026-09-18: the baker is numba-parallel and raises rather than guessing, and `.venv-gpu` is a real venv with `include-system-site-packages = false`, so it cannot see the conda env's numba. Artifacts are therefore generated in `.venv-jax` (login) and only *loaded* in `.venv-gpu` (desktop/GPU). Loading is pure numpy, so routed training and gate 4 are unaffected -- the gate-4 run loads the 231 MiB v2 table in `.venv-gpu` without numba. Left that way on purpose: numba pins numpy, and `.venv-gpu` is the environment every gate-4 number is measured in. | `data/local_route_artifact.py`, `train/run_train.py`; publish the pinned artifact to the project artifact store before remote training. |

## SmoothPath: what it fixed, and what it turned out not to explain (2026-09-18)

`SmoothPath` was missing and is now ported (`sim/local_pathing.smooth_cell_path`,
commit `de5cc2b`). It was filed as the single gap behind three open gates. One
third of that is right; the rest was an inference that the measurement does not
support, and it is worth writing down which is which.

**What it is.** The server runs A*, then a single greedy pass that deletes any
cell the last kept one can see through a swept circle of the pathfinding
radius. Our reverse-BFS router removed only *collinear* cells. Hence 6-9
waypoints where the server emits 3.

**What it fixed.** 400 routes through the production `map1_garen_r35_o50_v2`
artifact, scored against the server's own `get_path` (host self-check 400/400):

| | raw cells | before | after | server |
|---|---|---|---|---|
| mean waypoints | 37.48 | 8.97 | 4.62 | 4.49 |
| count agrees with server | - | 24/400 (6.0%) | 220/400 (55.0%) | - |
| polyline length vs server (mean / median / p90) | - | 1.177 / 1.155 / 1.337 | 1.049 / 1.004 / 1.162 | 1.000 |

**What it does not explain: Gate 3.** The argument was "more waypoints means a
longer path, and distance is decisions". The first half is true and is now
measured: **+17.7% mean, +15.5% median, 1.41x worst case**. The second half
does not carry the weight it was given. Gate 3's sim spends **6,400** approach
decisions against the server's **3,197** -- a factor of **2.0**. Path geometry
can account for at most about a fifth of that, and the worst single route in
the corpus is still only 1.41x. **Something other than pathing dominates the
approach gap**, and looking for it inside the router is looking in the wrong
place. Candidates now promoted ahead of geometry: effective movement speed /
per-tick waypoint budget (`MOVE-001` caps transitions at 8), orders re-issued
mid-walk, the `route_status` histogram over the approach legs, and walks
truncated by death.

**What it does not fix: Gate 1.** Smoothing cannot close waypoint parity by
itself, because the input path is wrong before it is smoothed. Only **17/400**
baked itineraries are the server's own A* cell path -- the local artifact is a
reverse BFS over a CastCircle-valid neighbour graph, chosen so the bake is
tractable. That caps exact waypoint agreement near 40% however the list is
smoothed, and it is what the remaining 45% of count disagreement is. This is a
bake-algorithm gap (PATH-001, PATH-003), not a smoothing gap, and it cannot be
closed by re-baking with A*: the server's search is closed-on-enqueue, so
matching it needs its exact frontier order per (source, goal) pair, which is
484M searches for this artifact.

**What it does not touch: Gate 4.** Unchanged and independent. Smoothing adds
CastCircle work per route; its cost has not yet been measured.

**Two port bugs it exposed.** Both surfaced because the device and host
disagreed on 2 of 400 routes -- not from rereading the source.

1. `navgrid.cast_circle` built the offset endpoints in float64; the server
   builds them in `Vector2`, i.e. float32. Both disagreements resolved to the
   float32 answer. The rate is below 0.05% on random segments (0 in 40,000,
   including 20,000 bake-shaped adjacent-neighbour queries, so the 231 MiB
   artifact is not implicated) -- but SmoothPath's greedy pushes every cast
   until it fails, so it probes the clear/blocked boundary *on purpose*. A
   predicate can be right everywhere that does not matter.
2. `cells_in_line` then walked with a float32 error accumulator inherited from
   those endpoints, where the source declares `double`.

Device and host SmoothPath now agree 400/400. The device `CastCircle` was
already exact -- 4,000/4,000 against the host on segments up to 60 cells, zero
bound exhaustion -- which is the first time it had been checked at smoothing
length rather than A*-neighbour length.

## Local-click routing design record

The routing target is intentionally local even though the map data is global:

1. The champion is the follow-camera origin.
2. The policy chooses one cell of the 96x54 screen grid. Under the calibrated
   1280x720 perspective, the farthest non-minimap bin centre is 2,281.27 world
   units from the champion. Canonical screen/lane components reach 1,892.63
   and 1,273.63 units; after rotation into the top-lane world frame, an x/y
   component can reach about 2,245 units. `SCREEN_RADIUS=1800` is a
   visibility/UI fact, not the click-projection bound.
3. Static terrain chooses a bounded waypoint route for that source/destination.
   There is no need for an unrestricted all-pairs map table.
4. Dynamic minions and champions are **not** baked into that route. They move,
   so the sequential collision pass supplies creep block on each tick.
5. Static routing and dynamic collision are both required: the former handles
   walls and alcove edges; the latter changes spacing and trading patterns.

The earlier whole-map `K x K` estimate in `JAX_REWRITE_PLAN.md` describes one
possible artifact ABI, not the required production topology. For a 50-unit
grid, the measured canonical projection reaches about 38 by 26 cells and the
rotated world-grid components reach about 45 cells before endpoint, anchoring,
and radius details; the production artifact deliberately covers a conservative
+/-50-cell square. A source-indexed local representation is
therefore proportional to map cells times local offsets, rather than all map
cells squared. The implementation still has to preserve exact endpoint,
tie-break, smoothing, overflow, and no-route semantics or label each departure
`APPROX` above.

## Maintenance rule

When debugging a policy/server mismatch, record the smallest reproducible
state/action/tick in the relevant audit and link it from the ledger row. When a
new deviation is found, add it here before optimizing around it. “Probably
irrelevant” is not a valid reason to omit a row: tiny timing and geometry
differences can alter creep block, a trade, and then the entire trajectory.
